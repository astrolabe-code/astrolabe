# PROVENANCE · 本项目开发过程溯源

> **本文件做什么**：★ 如实记录**这个仓库是怎么被写出来的**，
> 并指明**每一项设计决定留在哪里**、**如何独立核查**。
>
> ⚠★ **本文件不是法律意见** —— 它能做的是**把事实摆清楚、把证据指向明确**；
> ★★ 「这些事实在法律上意味着什么」须由**律师**判断。

生成时间：**2026-09-19** · 最后更新：**2026-09-20**
（★ 本文件本身也会进入版本历史，★ 之后每一次修改都留下记录）

---

## 一、开发方式（★ 如实陈述）

本项目的开发是**人机协作**，两者的分工**边界清晰、且每一侧都有留痕**：

| 角色 | 负责什么 | 痕迹落在哪里 |
|---|---|---|
| **项目所有者（人）** | ★★★ **决定做什么、不做什么**；确定边界、优先级、取舍；**否决不合适的方案** | ★★ `docs/backend-decisions.md`（158 条）· `docs/frontend-decisions.md`（72 条）**—— 每条都记录了「要求」与「结论」** |
| **AI 助手** | ★ **按上述决定实现**：写代码、写测试、跑验证、发现问题并报告 | ★ 源码本身 · ★ 各模块 docstring 里对依据的引用 · ★ `app/scripts/smoke_*.py` |

★★★ **关键点**：★ **AI 没有自行决定产品方向、功能取舍或架构边界**。
★ 那些决定**逐条记在决策日志里**，且**每条都注明它落实到了哪些文件**。

---

## 二、★★★ 权威的决策记录在哪里

这是本文件**最要紧的一节**。

| 文件 | 内容 | 形态 |
|---|---|---|
| ★★ **`docs/backend-decisions.md`** | 后端设计决定 | ★ **158 条**，编号 `B1`–`B158` |
| ★★ **`docs/frontend-decisions.md`** | 前端设计决定 | ★ **72 条**，编号 `D<n>` |
| `docs/` 下 26 个设计文档 | 被上述决定**逐条修订**的设计正文 | ★ 每条决策都写明"落实位置" |

### 每条决策记录的形态

```
| B158 | 2026-09-19 19:37 | 决策/改进（★ 常含【用户原话】引用） | 结论 | 落实位置 | 状态 |
        ↑ 时间戳                 ↑ 要求是什么（谁提的）      ↑ 怎么定的  ↑ 落到哪些文件
```

★★★ 这条链条就是 **「要求 → 决定 → 实现」的可追溯路径**：

- ★ **「要求是什么」** —— 由项目所有者提出（★ 多处以 `用户原话：` 直接引用）
- ★ **「结论」** —— 决定的具体内容与理由
- ★★ **「落实位置」** —— 指到**具体文件**，可逐条核对

★ 决策日志的**记账规则本身**也写在文件开头（共 8 条），包括
「★ **先登记、再动代码**」—— ★ 即**每条实现之前，先有记录**。

---

## 三、★★ 如何独立核查（★ 不必相信本文件，自己跑）

### 1. 看决策总数与时间跨度

```bash
grep -c '^| B[0-9]'  docs/backend-decisions.md     # 期望 158
grep -c '^| D[0-9]'  docs/frontend-decisions.md    # 期望 72
grep -o '^| B[0-9]\+ | [0-9-]*' docs/backend-decisions.md | head -3   # 最早几条
grep -o '^| B[0-9]\+ | [0-9-]*' docs/backend-decisions.md | tail -3   # 最近几条
```

### 2. ★★ 抽一条决策，核对它是否真的落到了代码里

```bash
# 例：B156 声称实现了 graph/curate.py
grep -n 'B156' docs/backend-decisions.md | head -1
ls -l app/graph/curate.py
grep -n 'GRAPH-STORAGE.md G1' app/graph/curate.py | head -3   # ★ 代码里确实引用了依据
```

### 3. ★★ 核对「登记先于实现」

```bash
git log --oneline -- docs/backend-decisions.md | tail -5
```

### 4. ★★★ 核对内容哈希 —— ★ **这是一个【哈希链】，不是一个值**

★ 下表**按版本累积** —— ★★ **每发一版【追加】一行，❌ 不覆盖旧行**：

| 时刻 | commit | `docs/backend-decisions.md`（SHA-256） | `docs/frontend-decisions.md`（SHA-256） |
|---|---|---|---|
| **0.2.0** · 2026-09-19 | `724df2f` | `71cf062612d73deacffcf2d2ca433dc6a27e145ed9cd8b38a968edbf88a43f1b` | `1d446b6c3fb863227a01e4e42c7077c1ddb06b4623f09474256e8fcef4fd0407` |
| **0.2.1** · （待填） | | | |

★★★ **为什么必须累积，而不是每次覆盖**：

| | ❌ 每次覆盖成一个新值 | ✅ 累积成一条链 |
|---|---|---|
| ★ 旧的时间切片 | ⚠ **丢失** | ★ **留得住** |
| ★★ 能证明什么 | ⚠ 只能证明"最新那版长这样" | ★★★ **能证明"每一版都在何时存在过"** |

★ **0.2.0 时刻的另外三份文档**（作为该切片的补充）：

| 文件 | SHA-256 |
|---|---|
| `docs/USERS-AND-AUTH.md` | `d8bcfd0b204532f0480cbdf0be4f38b9e8650987a5604a2270b41ca558407ea1` |
| `docs/GRAPH-STORAGE.md` | `023bca1c9c4b098d5c4fdc267b40b6072f337579581fed25815f521249f3d235` |
| `docs/backend-design-full.md` | `4767b1920b771e6da6996fa8a37bdeb27e70cbd4dc87de298cff18870a5b1fa7` |

★ 自己复核（★ **任何一版都能复核**）：

```bash
# 最新工作区
sha256sum docs/backend-decisions.md

# ★★ 或指定历史提交 —— 这才能验证"那个时刻"的形态
git show 724df2f:docs/backend-decisions.md | sha256sum
```

⚠★ **重要**：★ 决策日志**会继续追加**（★ 这是正常的）⇒ ★ 哈希**本来就会变**。
★★★ 那时**不是"被篡改"**，而是**有了新版本** —— ★ **正确做法是往上面的表里【追加一行】**。

---

## 三之二、★★★ 一次「公开前的安全脱敏」—— ★ 以及它为什么**不削弱**这份记录

★ 本项目在**首次公开之前**做过一次安全审查（★ 完整记录见 `docs/backend-decisions.md` 的 **`B158`**）。
★★★ 这里要说清的是**它的性质**：

| 问题 | 回答 |
|---|---|
| ★ 改了什么？ | ★ **只改了【我方描述】里对服务器规格的表述**（如「可用内存约 2GB」⇒「相当有限（已脱敏）」） |
| ★★ 用户原话动了吗？ | ★★★ **一个字都没动** —— ⚠ 引用是证据，★ **改动引用 = 篡改证据** |
| ★ 技术结论动了吗？ | ★ **没有**（三库分工 / 「CPU 才是瓶颈」/ 内存预算表 / 告警阈值**全部原样保留**） |
| ★★ 改动**被记录**了吗？ | ★★★ **是** —— ★ 它**写在决策日志里**（`B158`），★ 而不是悄悄改掉 |
| ★ 什么时间改的？ | ★ **首次推送之前** ⇒ ★★ **不存在「篡改已公示内容」** |

### ★★★ 为什么这反而**增强**了可信度

- ★ **时间线是单向的**：★ 脱敏发生在公开**之前**；★ 公开之后，★ **git 提交与 Zenodo DOI 会把那一刻的内容固定住**。
- ★★ **改动是自证的**：★ 一份声称"从未修订"、★ 又恰好与所有外部痕迹完美吻合的记录，**才更可疑**。
  ★★★ 而这份记录**主动写下了自己何时、为何、改了哪一类文字** ⇒ ★ **可核对**。
- ★★★ **它证明了一个动作**：★ 项目所有者**在发布前做了安全审查并逐项裁定** ——
  ★ 这本身就是「**已尽合理注意**」的记录。

> ★ **一句话**：★★★ **"改过、并且承认改过、且改的范围写清楚了"** ——
> ★ 这比"看起来从未改过"更可信。

---

## 三之三、★★★ 第三方永久存档（★ 独立于 GitHub 的时间证明）

★★★ **这一节是本项目最强的证据** —— ★ 因为它的时间戳**不由我们掌握**：

| 存档方 | 标识 | 内容 |
|---|---|---|
| ★★★ **Zenodo**（CERN 运营） | ★ Concept DOI：`10.5281/zenodo.22827012` | ★ **永久 DOI**，且**始终指向最新版** |
| | ★ `0.1.0`：`10.5281/zenodo.22827013` | ★ 2026-09-18 发布（★ 仅含 README） |
| | ★★ `0.2.0`：`10.5281/zenodo.22844803` | ★★ 2026-09-19 发布（★ **含全部设计文档与 158 条决策**） |
| ★★★ **Software Heritage**（INRIA 运营） | ★ `swh:1:dir:3fe20a9c6c32893cd67355823b900d25e48206ec` | ★★ **直接锚定到 git commit `724df2f`** |
| ★ **OpenAIRE** | — | ★ 欧洲学术检索库（已索引） |

### ★★★ 为什么 `0.2.0` 那条最关键

★ 它的 Software Heritage 标识里带着这一串：

```
swh:1:dir:3fe20a9c6c32893cd67355823b900d25e48206ec
  ;origin=https://doi.org/10.5281/zenodo.22827012
  ;path=astrolabe-code-astrolabe-724df2f        ← ★★★ 就是这个提交
```

⇒ ★★★ **它证明的是**：

> ★★★ **「提交 `724df2f`（含 158 条决策日志）在 2026-09-19 已存在」**
> ★ **—— 而且这个时间戳由两个【独立第三方】持有，我们改不了。**

★ 复核方式：★ 访问 `https://doi.org/10.5281/zenodo.22844803` ✅

---

## 四、时间跨度与规模（截至 2026-09-19）

| 项 | 数值 |
|---|---|
| 最早决策 | `B1` · **2026-09-10** |
| 最新决策 | `B158` · **2026-09-19** |
| 后端决策条数 | **158** |
| 前端决策条数 | **72** |
| 设计文档 | **27** 个（`docs/*.md`） |
| Python 文件 | **101** 个 |
| Python 代码行数 | **17,910** 行 |

★ 累计**九天**、**230 条**决策。★ 决策密度本身就说明**方向是被持续控制的**，而不是一次性生成。

---

## 五、⚠ 关于 AI 参与程度的说明（★ 不隐瞒）

★ 本项目的**代码由 AI 助手生成**，这一点**明确记录在此**，不做任何淡化。

★★ 同时如实说明的是：

- ★ **AI 生成的每一部分，都对应决策日志里的一条或多条记录**（★ 含"落实位置"）；
- ★★ **AI 不曾自行决定**产品定位、功能取舍、权限边界、数据模型、法律与合规口径；
- ★★★ 遇到这类问题，AI 的做法是**提出选项、说明代价、然后由项目所有者裁定**
  —— ★ 这些裁定**逐条留在决策日志里**（★ 其中多处以 `用户原话：` 直接引用原话）。

★ 举例（`B156`）：AI 曾把"图修补"实现成**管理员 CLI**，
★★ **项目所有者指出这搞错了 —— 修补应是「项目创建者自己」在网页上做**；
★ 该纠正被记录为 `B157`，并**据此重写了实现**（★ 补上此前完全缺失的权限校验）。
★ 这类"**人纠正 AI**"的记录在决策日志里**反复出现**，且**都带时间戳**。

---

## 六、⚠★ 两个已知的待补强项（★ 如实列出）

### (1) 决策日志里的「用户原话」回填

★ 决策日志的「决策/改进」列，目前**只有一部分**以 `用户原话：` **直接引用**了原话，
★ 其余条目是**对要求的概述**。

★★ 补强方式：★ **从 AI 协作平台的对话历史中导出原始记录，逐条回填原话**。
★★★ 回填纪律（★ 必须严格遵守，否则**反而削弱**证据力）：

1. ★ 在平台里**导出**对话历史（CodeBuddy：侧栏对话面板右上 → `History` → 导出）
2. ★★ **导出后不要再编辑那个文件**（★ 任何编辑都会削弱其完整性）
3. ★ 逐条比对：★ 决策日志的哪一条 ↔ 对话里的哪一段原话
4. ★★ **原样复制** —— ★ **不改字、不改标点、不修错别字、不润色**
5. ★ 找不到原话的条目 ⇒ ★★ **留空，不要补写**
6. ★★ 由**项目所有者本人逐条确认**后才提交
7. ★ 提交后**在第三节的哈希链里【追加一行】**（❌ 不覆盖旧行）

★★★ **两条红线**：★（a）**只回填，不创造**；★（b）**不改写**。
⚠ 一旦被发现"事后加工过"，★ **整份记录的可信度都会受损** —— ★ 得不偿失。

### (2) ⚠★ 代码来源的逐条核对（★ 未做）

★ 本项目使用了若干**宽松许可**的第三方组件（★ 清单见
[`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md)）—— ★ 它们**允许**本项目以
「保留所有权利」的方式发布，★ 前提是**保留其版权声明** ✅

⚠★ **但尚未做的是**：★ 对**参考过思路**的地方**逐条标注出处**。
★ 计划：★ **随开发顺手记录**（★ 每引入一处，记一条决策），★ 而不做"事后整体审查" ⚠
★ 理由：★ 事后审查**成本高、且准确性差**（★ 很难想起"当时为什么这么写"）。

---

## 七、★ 进一步固化措施

| # | 措施 | 状态 |
|---|---|---|
| **1** | ★★ **推送到第三方托管**（★ 让托管方独立记录接收时间并持有副本） | ✅ **已完成**（GitHub） |
| **2** | ★★★ **发布到 Zenodo**（★ 见第三节之三） | ✅ **已完成**（0.1.0 / 0.2.0 已归档） |
| **3** | ★ 每次重要决策后**及时提交**（★ 不要攒很久再提交一次） | ★ 持续执行中 |
| **4** | ★★ 保留**平台对话历史的导出件**（★ 单独存放，★ 不要放进本仓库） | ⏭ 待办 |
| **5** | ★ 每次发版**在哈希链里追加一行**（★ 见三·4） | ⏭ 待办 |
| **6** | ★ 如需要**更强的第三方时间证明**，可咨询律师关于**可信时间戳（RFC 3161）**或**存证服务** | ⚠ 法律层面的选择 |

★★★ **第 1、2 条已完成** —— ★ 它们把"**本地自述**"变成了"**第三方可核**" ✅

---

*本文件由项目所有者确认后提交。★ 如有不符，以决策日志与版本历史为准。*
