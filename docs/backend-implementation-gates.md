# 附录 E · 实施门槛清单(随主文 v27 · 初版 v4)

> **对应第四轮审阅**(资深架构师 + 渗透安全工程师交叉审)。
> **用途**:把"P0 必改 12 项"与"P1 待收敛 项"集中成**可勾选的实施门槛**,避免实施时遗漏。
> **纪律(v22 P0-3:门槛已拆为**三道门**,措辞定稿 —— 消除 v21 的循环依赖)**:①**门 1 · 可评审通过** = P0 在**文档层面**全部关闭 + 本清单经评审确认;②**门 2 · 检查器就绪** = **F3 的 7.1–7.4 四个检查集在 CI 跑通 + 改名映射配置落地**(⚠ **构建任务属"门 2 的交付物",不是阶段 0 的任务** —— v21 曾把它同时写成"阶段 0 第 5 条"和"进入阶段 0 的前置条件",形成**死锁**);③**门 3 · 可进入阶段 0** = **门 1 与门 2 均已通过**;**未满足者不得开工**。主文 §9 的「开工门禁」与本表**逐字一致**。

---

> **当前门槛速查**(随主文 v26 更新;P2-5:**本表内含最新两轮,主文 §0 只引用本表,不再硬编码轮次**):P0 关闭情况 —— **E1 12/12 · E5 4/4 · E6 10/10 · E7 7/7 · E8 6/6 · E9 5/5 · E10 4/4 · E11 1/1 · E12 5/5 · E13 5/5 · E14 6/6 · **E15 6/6 · E16 1/1** · **E17 6/7 已落**(本轮变更,**范围=仅文档**;**第 7 项「合并文件同步」推迟到评审定稿后**,不计入本轮) · **E18 11/11 已落**(本轮变更:**效率优化入档**,**范围=仅文档**) · **E19 23/23 已落**(专项收口:**10 P0 + 10 P1 + Redis 落位**,**范围=仅文档**),文档层面**全部关闭**;代码级收口见 **F3 的 7 项**。**· E20 24/24 已落**(第三轮:**v19 自洽性专项**,**范围=仅文档**) **· E21 21/21 已落**(**v20 落地可行性核查**,**5 P0 + 10 P1 + 6 P2**,**范围=仅文档**) **· E22 22/22 已落**(**v21 交叉一致性核查**,**5 P0 + 13 P1 + 4 P2**,**范围=仅文档**) **· E23 18/18 已落**(**v22 门禁前最后核查**,**3 P0 + 10 P1 + 5 P2**,**范围=仅文档**) **· E24 22/22 已落**(**v23 新机制收口**,**5 P0 + 12 P1 + 5 P2**,**范围=仅文档**) **· E25 32/32 已落**(**v24 新机制与跨文档一致性核查**,**13 P0 + 15 P1 + 4 P2**,**范围=仅文档**) **· E26 30/30 已核**(**v25 收口核查**,**8 P0 + 12 P1 + 10 P2**,**范围=仅文档**;⚠ 其中 #1/#21 与 #25 经逐行核查**不成立**) **· E27 减法重构已落**(本轮:**v27 减法版**,**删 9 类机制 + 合并 fencing/状态/版本 + 后置性能项**,**范围=仅文档**;⚠ **E1–E27 全部降为历史归档,不作权威**)。**(E4 不再重复此计数,一律以本速查表为准)**
>
> **门槛判定(v21 P2-3:由"一段文字"拆为两行,与 E4 逐字一致 —— 避免"✅ 但…"式的混淆)**
>
> | 门 | 条件 | 当前状态 |
> |---|---|---|
> | **门 1 · 可评审通过(v27:即为可开工)** | 主文 **v27** + 附录 A–G 一致(**自检清单与 F3 检查集已取消,不再是门槛**) | ✅ **达成**(v27:**E27 减法重构已落**;v26:30/30) |
> | **门 2 · 可开工(v27 与门 3 合并)** | **原"检查器就绪"已取消**(治理纯减法:F3 检查器 + 自检清单 + 改名映射全部移除,见 E27)→ **门 1 通过即等于可进入阶段 0** | ✅ **达成**(与门 1 同) |
>
> ⚠ **v23 P0-3(删旧行)**:本表此前**残留一行旧的门 1**(`可评审通过 · 主文 v21`),与 `门 1 · 主文 v22` **并存** → 同一道门同时挂着 v20 / v21 / v22 三种版本号,**门槛判定自相矛盾**。**现删旧行**:同一道门**只允许一行**,且与 **E4 逐字一致**(已进主文自检清单);**门 1 可判"达成",门 2 / 门 3 不应推进**。
>
> **归档说明(P2-6 / P2-8)**:E1–E16 **全部是已关闭的评审处置**(E1–E5 为早期轮次,E6–E16 为后续轮次),保留**仅供审计追溯**;**E17 不是评审轮,而是"本轮变更处置"**(身份线分家 + 限流统一,**范围=仅文档、不动代码**),同样用可勾选表跟踪 —— 详见下方 **E17**;**E18 同样是"本轮变更处置"**(效率优化入档:Worker 池 / parse cache / 查询归一化 / 派生预计算,**范围=仅文档、不动代码**)—— 详见下方 **E18**;**E19 是"第二轮专项收口"**(10 P0 + 10 P1 + Redis 落位,**范围=仅文档、不动代码**;决策来源 = 用户提供的第二轮评审)—— 详见下方 **E19**;**E20 是"v19 自洽性专项"**(10 P0 + 13 P1 + F3 拆分,**范围=仅文档**;决策来源 = 用户提供的第三轮评审)—— 详见下方 **E20**;**E21 是"v20 落地可行性核查"**(5 P0 + 10 P1 + 6 P2,**范围=仅文档**;决策来源 = 用户提供的第四轮评审;**核心 = 把"实现层不可执行"的规则改成可执行**)—— 详见下方 **E21**;**E22 是"v21 交叉一致性核查"**(5 P0 + 13 P1 + 4 P2;核心 = 消除"修一处漏一处 / 只修一半 / 自我循环");**E23 是"v22 门禁前最后核查"**(**3 P0 + 10 P1 + 5 P2**,**范围 = 仅文档**;核心 = **3 个 P0 缺口 + 并发正确性**);**E24 是"v23 新机制收口"**(**5 P0 + 12 P1 + 5 P2**,**范围 = 仅文档**;核心 = **"结构性质 ≠ 行为限制" + 新机制 fallback + 跨口径转换**);**E25 是"v24 新机制与跨文档一致性核查"**(**13 P0 + 15 P1 + 4 P2**,**范围 = 仅文档**;核心 = **租约唯一权威 / 假设 A 定义 / `project_key` 改名 / 故障期口径二选一**);**E26 是"v25 收口核查"**(**8 P0 + 12 P1 + 10 P2**,**范围 = 仅文档**;核心 = **仲裁映射(`PageAssignment`)/ 故障期读每请求 / 首次抢租约 `INSERT` / IP 键空间统一 / 恢复后状态表**)—— 详见下方 **E26**;日常维护与实施只需看 **本速查表**(它**内含最新两轮**)——主文 §0 **不再硬编码轮次**(P2-5),一律引用本表。**不要把历史处置当权威口径** —— 凡与附录 D / A5.1 / F 冲突处,以最新口径为准。

## E1 P0 必改 12 项(按评审给出的顺序)

| # | 项 | 位置 | 类型 | v4 处置 | 状态 |
|---|---|---|---|---|---|
| 1 | VID 哈希改加密哈希 | D1.2 / §5.2 / C5 | 安全 P0 | 改为**加密哈希**;**v6 起算法改项目级**(`Project.hash_algo`,**默认 `sha256_16`**,`blake3` 可选)、**禁止运行时降级** —— **权威见附录 D1.5 / D1.5.1**(本行是 v4 历史记录,勿按本行实施,P2-5) | ✅ 已关闭(待三端测试向量落地) |
| 2 | `device_id` 由服务端签发 + 绑定 session | A3 / D4 | 安全 P0 | 新增 **A3.1**:服务端签发、绑定 `(user_id, session_id)`、客户端不可自选、换设备=换 session | ✅ 已关闭 |
| 3 | D4.3 异常分支改"作业优先回收租约" | D4.3 / §8 | 架构 P0 | **不再比较 `lease_seq` 与 `active_fence`**(不可比)→ **作业优先强制回收租约**;并加**同事务 `FOR UPDATE`** 消除竞态(D4.2) | ✅ 已关闭 |
| 4 | D2 租约交接时重置 `file_rev` 基线 | D2 / A3 | 架构 P0 | 新增 **D2.4**:接管后必须全量重传 + **所有 `file_rev` 重置为新基线**;老写者 `base_file_rev` 失效 → **`409 stale_baseline`** | ✅ 已关闭 |
| 5 | §5.5 删掉"outbox 成功 → `graph_rev+1`" | §5.5 | 架构 P0 | 采纳**选项 Y**:`graph_rev` = PG 侧图事务序号,**上传事务成功即 +1**,outbox 消费**不推进**;滞后用 `index_lag_s` 表达(§5.4/§5.5) | ✅ 已关闭 |
| 6 | §7 补 `CodeAnalysisDerived` 读路径 | §7 | 架构 P0 | §7 新增一行:写回 **`CodeAnalysisDerived`**(服务端管线为写者时例外);`analysis/`、`diagnostics/` 读 **两表合并** | ✅ 已关闭 |
| 7 | `storage_port.tx` 改必需 + 核心写路径单事务 | C4.1 | 架构 P0 | `tx` **改必需**;新增 **C4.2**(单事务三写 + 补偿规则 + 注入式验收) | ✅ 已关闭 |
| 8 | 路径穿越防护(realpath + 前缀断言 + 不跟随 symlink) | B3 / §5.4 | 安全 P0 | 新增 **B3.1**(NFKC + URL 解码 + 控制字符 + 盘符 + 视觉混淆 → `commonpath` 断言 → `O_NOFOLLOW` → TOCTOU 二次断言 → 逐条拒绝可见) | ✅ 已关闭 |
| 9 | nGQL 参数化规范写进 B2/B5 | B2 / B5 | 安全 P0 | B2 规则 2 改为"**禁止一切字符串拼接**、统一 `session.execute(query, params)`";B5 增"参数化"行 + **断言必须在构造查询之前** | ✅ 已关闭 |
| 10 | `analysis` 字段 JSON Schema + XSS 净化 + 大小上限 | §5.4 / D3 | 安全 P0 | 新增 **B9**(请求体 ≤10MB、Schema 白名单、字符串转义+长度上限、数值区间与**自洽校验**、引用完整性、净化后入库);`analysis` 落位见 **D3.4** | ✅ 已关闭 |
| 11 | OpenSearch `wildcard` 限流 + 超时 + 长度上限 | §4.3 / B4 | 安全 P0 | §4.3 增**检索滥用防护**行(q ≤128、通配符 ≤3、超时 500ms、限流 10 QPS、熔断降级、评估 ngram 替代);B4 标注"服务端强制" | ✅ 已关闭 |
| 12 | "源码永不上行"加语义级例外条款 | §6.1 | 架构 P0 | §6.1 增**例外条款(X-1)**:用户显式开启语义级 → **项目粒度临时上传 → 解析后立即删除 → 记审计** | ✅ 已关闭 |

### E1.1 E1 的收口条件(实施前必须完成,不能只停留在文档)

| 项 | 收口动作 |
|---|---|
| #1 | 三端各跑 **同一测试向量** 并比对(浏览器 / PG / 服务端),结果附 CI 日志 |
| #2 | 租约获取接口增加"`device_id` ↔ session 绑定"校验 + 越权用例(伪造 device_id → 403) |
| #3 | 并发测试:同时抢租约 + 投递作业,断言**不出现**"作业 + 租约"共存 |
| #4 | 用例:A 上传 → 租约过期 → B 接管 → A 重传 → 断言 `409 stale_baseline` |
| #6 | 用例:私有 `backup` 项目跑服务端读路径 → 断言 `CodeAnalysis` **行未被改写** |
| #7 | 崩溃注入测试:三写之间强杀 → 断言"全有或全无且可恢复" |
| #8 | 用例:`../../etc/passwd`、`src/%2e%2e/`、`src/link→/etc`、`src/․․/x` 全部被拒且**逐条返回** |
| #9 | 静态检查:仓库内**禁止**出现拼接 nGQL 的代码路径(CI grep 规则 + code review 清单) |
| #10 | 用例:`<script>` 注入 → 落库为转义形态;20MB → 413;计数不自洽 → 400 |
| #11 | 压测:100 并发通配符查询 → 集群不雪崩(超时/熔断生效) |
| #12 | 用例:开启语义级 → 审计有记录 → 解析后源码目录**已删除** |

---

## E2 P1 清单(不阻塞评审,阶段 0/1 收敛)

评审正文逐条列出 **16 项**(架构 A-P1 6 项 + 安全 S-P1 10 项)。**上一轮"共 20 项"系评审笔误,以 16 项为准**(第五轮已更正 —— P2-3);本清单不再保留"余下 4 项"留位。

### E2.1 架构 A-P1(6 项)

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| A-P1-1 | §5.4 的 `analysis` 与 D3 唯一写者未对齐 | 新增 **D3.4** 按模式指定落位(`off`/`backup` → `CodeAnalysis`;`collab` → 主字段 + 派生;公开/超大 → **忽略客户端 analysis**) | ✅ 已改 |
| A-P1-2 | `CREATE SPACE IF NOT EXISTS` 不校验 `vid_type` | **D1.3** 加 `SHOW CREATE SPACE` 显式比对,**不符即中止部署** | ✅ 已改 |
| A-P1-3 | D1.2 与 §4.1 字段名不一致 | §4.1 `graph_node_ref` 统一为 **`uid_hash_128 BYTEA(16)`** + `UNIQUE(project_id, uid_hash_128)` | ✅ 已改 |
| A-P1-4 | 哈希表述三处不统一 | §5.2 / D1.2 / C5 统一为参数化 **`HASH(project_id + "\0" + uid, out_len=16)`**(**v8 修正**:`HASH` = `Project.hash_algo`,**默认 `sha256_16`**,不再写死 BLAKE3);禁止再出现 `xxhash128` | ✅ 已改 |
| A-P1-5 | §8「写权互斥」未提 D4.3 异常分支 | §8 该行补"**异常分支裁决以 D4.3 为准(作业优先)**"+"获取必须同事务" | ✅ 已改 |
| A-P1-6 | §11 缺 D4.3 裁定行 | §11 新增第 10 行(D4.3 异常分支)+ 第 11/12 行(BLAKE3 / `device_id` 签发) | ✅ 已改 |

### E2.2 安全 S-P1(10 项)

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| S-P1-1 | `local_conflict.payload_local` 未净化 | 落 `local_conflict` **前做 HTML 转义**(与 B9 同一净化函数);UI 渲染仍保留转义 | ✅ 已改(§6.2 第四件 + B9) |
| S-P1-2 | share token 未绑定 `user_id` | B8-B1 补 **绑定 owner(`user_id`)** → 可追溯、可吊销 | ✅ 已改 |
| S-P1-3 | `index_lag_s` 泄露集群负载 | 改为**粗粒度档位**(`ok` / `behind` / `unknown`)对外返回,精确秒数只进**管理端**指标 | ⏳ 待实现时收敛 |
| S-P1-4 | `UNIQUE(project_id, uid_hash)` 不防跨项目重映射 | §4.1 已加**跨项目 uid 冲突检测**;并明确碰撞检测为**全局** | ✅ 已改 |
| S-P1-5 | `op_rev` 单调性依赖 PG 序列(主从切换可能回退) | 改用 **PG 序列 + 回退检测**(或 `pg_current_wal_lsn()` 作单调源);检测到回退即告警并拒绝旧写 | ⏳ 待实现时收敛 |
| S-P1-6 | `Project.file_rev_seq` 是全局热点 | 改为 **PG 序列(`CREATE SEQUENCE`)**,避免单行 CAS 热点 | ⏳ 待实现时收敛 |
| S-P1-7 | §6.3「7 天降级」依赖客户端时钟 | `last_synced_at` **由服务端写入**,前端只读(WAL/时间以服务端为准) | ✅ 已改(§6.3 + A6-A2) |
| S-P1-8 | `CREATE TAG INDEX ... project_id(64)` 前缀长度未论证 | 标注**需实测后调整**(基数高时应评估全字段索引或复合前缀) | ⏳ 待压测收敛 |
| S-P1-9 | fork 后继承连接问题可能在 Redis/OpenSearch 复现 | 把"**fork 后必须丢弃继承连接**"写成**所有外部客户端的通用约束**(worker 启动清单) | ⏳ 待实现时收敛 |
| S-P1-10 | 审计日志写入放大(越权请求可写爆磁盘) | B7 增**审计限流/采样**:同一 `user_id` 每分钟 ≤60 条明细,超出只累加聚合计数 | ✅ 已改(B7) |

---

## E3 跨领域 X 项(3 项)

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| X-1 | "源码永不上行"与"服务端语义级解析"硬矛盾 | §6.1 增**例外条款**:显式开启语义级 → 项目粒度临时上传 → 解析后删除 → 记审计 | ✅ 已改 |
| X-2 | 分级阈值与"语义级需求"的判定权 | §5.7 增**判定权表**:默认语法级(系统) / 用户开关(显式确认) / 公开协作超大由服务端;阈值只回答"能否在浏览器跑" | ✅ 已改 |
| X-3 | `.webapkg` 完整性校验 | §6.2 第 1 项重写:逐文件 `sha256` + `MANIFEST.json` + 导入全量校验 + schema 校验 + **来源不信任(用户确认)** + 拒绝规则 | ✅ 已改 |

---

## E5 第五轮评审处置(v5)

> **编号说明(P2-3)**:E5 / E6 / E7 各自的 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用一律带前缀(如 `E5-P0-1`),避免"两个 P0-1 指不同问题"。

### E5.1 P0 4 项(必改,已全部落文)

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | BLAKE3"降级路径"会造成同 uid 双 VID | **取消运行时降级** → 算法**项目级、服务端确定、不可变**(`Project.hash_algo`),客户端从项目元数据读取;缺 BLAKE3 **拒绝加载**而非换算法;一致性用**服务端重算校验**(等价于前缀的防护);VID 前缀方案**部分不采纳**(VID 已含 `project_id`、同项目算法唯一 → 无歧义),若评审坚持则 `vid_type` 须在建 space 前定为 `FIXED_STRING(40)` —— 附录 D1.5 | ✅ 已关闭(待评审确认前缀决策) |
| P0-2 | `graph_rev` 语义切换后下游悬空 | 新增字段口径:`nebula_graph_rev`、`indexed_graph_rev`、**`index_lag = graph_rev − indexed_graph_rev`(版本差)**、`CodeAnalysisDerived.based_on_graph_rev`;**附录 A5 的"Nebula 写成功 → `graph_rev+1`"已删除**(与 A-P0-3 冲突) | ✅ 已关闭 |
| P0-3 | `tx` 的 IndexedDB 约束未写清 | C4.2 增**硬约束**:回调内禁 `fetch` / `setTimeout` / 非 IDB `await`(**含 `crypto.subtle`**);**VID 计算与净化必须在进入 `tx` 前完成**;违者 `TransactionInactiveError`;并加**构建期静态检查** | ✅ 已关闭 |
| P0-4 | `writer_epoch` / `lease_seq` 递增规则未定义 | 采**严格递增**(续租也 +1);写权标识 = `(writer_epoch, lease_seq)` —— 先比 epoch、再比 seq,**两项都不同时严格更大即拒绝**(可拦同写者乱序重放);客户端令牌**由抢租约/续租响应回传并本地缓存**(附录 A3) | ✅ 已关闭 |

### E5.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | 冲突"保留两份"的数据模型缺失 | 两份**不入图、不生成节点**,只落 `local_conflict`;**用户选定后**才生成 uid 并上传(§6.2 第四件) | ✅ |
| P1-2 | `ProjectSyncState.server_epoch` 语义未定义 | **删除** → 改 **`last_synced_graph_rev`**(该设备最后同步到的服务端 `graph_rev`);跨端差距 = `graph_rev − last_synced_graph_rev`(§4.1) | ✅ |
| P1-3 | `op_rev` 是全局还是每 VID 序列 | 改名 **`vid_op_rev`**,仅同 VID 内比较;跨 VID 无序为设计。**v8 定稿:值 = `graph_rev`(不是独立序列)**,前提是"同一 `graph_rev` 内同 VID 单行"(附录 D5.1) | ✅ |
| P1-4 | share token 走 URL 的 Referer 泄露 | 优先 `Authorization: Bearer`;必须走 URL 时用 **fragment `#token=…`**;响应头 **`Referrer-Policy: no-referrer`**;有效期 7 天 → **24 小时**(B8-B1) | ✅ |
| P1-5 | `analysis`"净化后入库"破坏代码语义 | B9 改**分层**:结构性字段保原值;仅**会插入 HTML 的字段**服务端强制转义;渲染层始终转义 | ✅ |
| P1-6 | 审计限流可被多账号绕过 | 限流维度改 **`user_id` + `IP` + `session_id`**;匿名按 IP;**注册接口限流**(B7) | ✅ |
| P1-7 | "全局碰撞检测"语义模糊 | 明确四项:VID 唯一性检查 / 同项目重复 uid 由 `UNIQUE` 阻止 / **跨项目同 uid 允许**仅记录 / 算法一致用 `hash_algo` 重算校验;**"重映射表"不常规化**(§5.2) | ✅ |
| P1-8 | `file_rev` 重置后的碰撞 | `file_rev` **项目级单调、永不复位**;**交接检测改以 `writer_epoch` 为准**,老写者 → `409 stale_baseline`(附录 D2.4) | ✅ |
| P1-9 | 迁移与回滚策略缺失 | §9 阶段 1 增**完整迁移表**:停服窗口 / 双写校验期 / 迁移期上传处理 / 数据校验 / 回滚预案 / 切换判据 / 演练验收 | ✅ |
| P1-10 | 监控阈值与告警通道未定义 | 新增 **附录 F1**:12 项指标绿/黄/红 + 通道 + 升级(5min/15min)+ 静默窗口 + 最小仪表盘 | ✅ |

### E5.3 遗漏项 M1–M7

| # | 缺失 | 处置 | 状态 |
|---|---|---|---|
| M1 | 数据迁移与回滚策略 | §9 阶段 1(与 P1-9 同处) | ✅ |
| M2 | 监控阈值与告警通道 | **附录 F1** | ✅ |
| M3 | 配置分发与生效机制 | §8 增"配置分发"行:DB 单一真源 + 短 TTL(≤30s)+ **领作业前读配置版本** | ✅ |
| M4 | 备份与容灾 | **附录 F2**(PG/Nebula/OpenSearch 各自 RPO/RTO + 恢复优先级) | ✅ |
| M5 | API 版本化策略 | §7 增"API 版本化"行:只增不改 / 破坏性变更新开 `/api/v2` / 保留一个发布周期 / 前端上报契约版本 | ✅ |
| M6 | 测试策略 | **附录 F3**(单测/集成/E2E/安全/契约 + CI 五项硬门槛) | ✅ |
| M7 | 文档与代码版本同步流程 | 文首"写入纪律"增②③:**同一提交内更新文档** + 决策日志追加"文档-代码同步"条目 | ✅ |

### E5.4 v5 后仍待拍板

| # | 事项 | 说明 |
|---|---|---|
| 1 | **VID 是否加算法前缀** | 本稿判"不需要"(理由见 D1.5);**若评审坚持**,须在**建 space 前**把 `vid_type` 定为 `FIXED_STRING(40)`(事后不可改) |
| 2 | "跨 VID 无序"是否可接受 | 若业务需要**跨 VID 因果顺序**,`vid_op_rev` 需升级为全局序列(代价:写热点) |
| 3 | 告警通道具体落地 | F1 只给级别与升级规则,具体 IM/邮件服务需按部署环境选型 |

---

## E6 第六轮评审处置(v6)

> **编号说明(P2-3)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E6-P0-n`**(避免与 E1 的 `P0-1` 等混淆)。E7 同理。

### E6.1 P0 10 项(必改,已全部落文)

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | Nebula 顶点缺 `vid_op_rev`,"下游只接受更大 rev"无法落地 | **顶点加 `vid_op_rev int64`**(D1.3 DDL);落地采**方案 A**:写前 `FETCH PROP … YIELD vid_op_rev` 比较 → 拒绝更旧者;并明确**同一 VID 的 outbox 行必须由同一消费者串行处理**(D5.2) | ✅ |
| P0-2 | BLAKE3 默认 + 拒绝加载 = 锁死老浏览器 | **默认改 `sha256_16`**(WebCrypto `crypto.subtle`,零依赖、全浏览器可用);`blake3` 为**可选**(创建时 capability 探测);**设备能力降级 = 只读浏览**(可读服务端数据,禁本地解析/上传 `409 hash_algo_unsupported`,UI 说明原因),**不强迫上传源码**;顺带定死"拒绝加载"的语义(D1.5.1) | ✅ |
| P0-3 | `vid_op_rev` 的生成机制未定义 | **`vid_op_rev = graph_rev`**(免维护)+ 强制 **"同一 `graph_rev` 内同 VID 只允许一条 outbox 行"**(上传按 VID 去重);四个候选方案的取舍见 D5.1 | ✅ |
| P0-4 | `graph_node_ref` 缺 `UNIQUE(project_id, uid)`,算法不可变只是口头保证 | 加 **`uid_sha256 CHAR(64)`(固定 SHA-256,与 VID 算法无关)+ `UNIQUE(project_id, uid_sha256)`** 作为 DB 级兜底;`hash_algo` 与 `Project.hash_algo` 不一致 → 拒绝 + 定期巡检(P1-1) | ✅ |
| P0-5 | B9"净化分层"在后端不可判定 | 改为**按字段用途三类**:**标识类**(白名单)、**展示类**(保原值,渲染层转义,写进前端契约)、**富文本类**(服务端白名单净化 —— 唯一应做服务端净化处) | ✅ |
| P0-6 | `CodeAnalysis` 与 `Derived` 的字段级合并规则未定义 | 新增 **D6**:字段归属表(9 类字段逐条指定归属与合并规则)+ 不一致处理(`analysis_conflicts` 可选字段,不阻塞展示)+ 空值不用 0 冒充 + `derived_stale` 标记 | ✅ |
| P0-7 | `dead` 队列处置流程未定义 | 新增 **A5.1**:三个管理接口(列表/重试/丢弃)+ 重试前重新校验(B6 那套)+ **丢弃 nebula 必须触发重建**并标记 `inconsistent` + 批量处置 + **SLA 4 小时** | ✅ |
| P0-8 | `index_lag` 定义对"部分索引"不成立 | **`indexed_graph_rev` 改为"连续完成水位"** = `min(未完成 opensearch outbox 行的 graph_rev) − 1`(队列空 = 当前 `graph_rev`);**明确不能取 max** | ✅ |
| P0-9 | `lease_seq`/`writer_epoch` 序列作用域未定义 | A3.1 显式写死:**三者(`writer_epoch`/`lease_seq`/`active_fence`)全部为每项目序列,不得跨项目比较,无全局发号器** | ✅ |
| P0-10 | `api/v2` 保留"一个发布周期"无量化 | 改为 **"至少 90 天且至少 2 个发布"**(取长),**逐版本记进 `frontend-contract.md`**;并加 **前端合约协商**(`X-Contract-Version`,不支持 → 426) | ✅ |

### E6.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | `graph_node_ref.hash_algo` 与 `Project.hash_algo` 不一致 | 同步入口校验一致(否则拒绝+审计)+ **定期巡检** + `hash_algo` 冗余存储(免 JOIN)(§4.1) | ✅ |
| P1-2 | 多标签页共享 `device_id` | `device_id` 加**标签页级 entropy**(`page_id`)→ **一个标签页 = 一个写者**;多标签页互抢租约是设计,UI 需提示(A3.1) | ✅ |
| P1-3 | 审计按 IP 限流可被 VPN/IPv6 绕过 | 匿名改按 **`session_id` + IP 组合**;注册加**设备指纹/验证码 + 限流**;注册后以 `user_id` 为主 IP 辅助(B7) | ✅ |
| P1-4 | `node_ref` 与 VID 冗余 | 明确 **`node_ref = vid`**,由服务端填、不接受客户端传值(D5.3) | ✅ |
| P1-5 | `rebuild_index` 期间一致性 | 新增 **A5.2**:期间**暂停写**(503+`Retry-After`)、**读降级** PG/Nebula、幂等可重入、**别名切换**发布、完成后**文档数 = 节点数**校验 | ✅ |
| P1-6 | `spec` 冲突域未定义 | 明确:**项目级 `spec`(行级 LWW)+ 节点级 `planned_sig`/`baseline_sig`(行级 LWW)**,LWW **作用于字段级**(§5.6) | ✅ |
| P1-7 | `ProjectSyncState` 写入热点 | **拆表**:高频 `ProjectWriterLease`(续租 30s)+ 低频 `ProjectSyncState`;`lease_seq` 用**项目级 PG 序列**(§4.1) | ✅ |
| P1-8 | 解析器升级导致 uid 变化 | **uid 规则版本化**(`uid_rule_v`)+ 升级时**全量重传 + 旧 VID 墓碑** + 新老并存读合并 + 迁移后清理墓碑(§9) | ✅ |
| P1-9 | Nebula `epoch` 与 PG `graph_node_ref.epoch` | 明确:**权威在 PG**;Nebula 侧 `epoch` 是**消费时快照,允许滞后**(§4.2) | ✅ |
| P1-10 | `fs_backend` 运行时断开检测 | **领作业前自检 + 心跳写测试文件**;断开 → **快速失败**并将作业放回队列,不阻塞其它作业(§8) | ✅ |

### E6.3 遗漏项 M8–M13

| # | 缺失 | 处置 | 状态 |
|---|---|---|---|
| M8 | 数据导出/删除的 GDPR 合规 | **新增附录 G**:数据分类处置表、导出(可携带权)、**删除执行顺序 8 步**(含幂等可重入与验收) | ✅ |
| M9 | 限流对用户不可见 | §8 增"限流可见性":**429 + `Retry-After` + 错误码 + 前端自动退避**,同步上传遇 429 **不退化为失败**(进 outbox 排队) | ✅ |
| M10 | 多租户资源公平性 | §8 增三层:每用户配额/并发、**单项目资源上限**、**公平调度**(按用户轮转,大项目走低优先级队列) | ✅ |
| M11 | 日志中的敏感信息 | **附录 B10**:`uid`/`file_path`/`signature`/`device_id` 视为项目内敏感;日志默认**哈希前缀**;临时排查开关;错误不回显明细 | ✅ |
| M12 | 供应链安全 | **附录 C7**:依赖锁定 + **WASM 哈希校验** + 来源审查进 CI + 构建可复现 + 语法包升级走 uid 版本化流程 | ✅ |
| M13 | 前端合约版本协商 | §7 增 `X-Contract-Version`:**支持/可降级/不支持(426)** 三态 + 前端提示刷新 | ✅ |

---

## E7 第七轮评审处置(v7)

> 编号在本附录内唯一;跨附录引用写作 `E7-P0-n`。

### E7.1 P0 7 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | D5.2 的 `FETCH → 比较 → INSERT` **非原子**,顺序保证不成立 | 承认反例成立("取模分区 ≠ FIFO")→ 改为 **每 VID FIFO 串行队列**;**重试也必须排进同一队列**;若 Nebula 支持 CAS 则优先用 CAS;并给出**并发注入压测**作为落地检查(D5.2) | ✅ |
| P0-2 | `indexed_graph_rev` 会被 `dead` 行**永久阻塞** | 水位改为 `min(**未完成且非 `dead`** 的 opensearch 行) − 1`;**统一口径:nebula 与 opensearch 都会进 dead**(超 N 次失败,建议 8)→ 取消 v6"opensearch 不进 dead";进 dead 即**排除出水位 + 记审计**(§5.5 / A5.1) | ✅ |
| P0-3 | 能力检测**没有协议** | 新增 **D1.5.2**:①先读项目元数据(无需哈希)②请求带 `X-Hash-Capabilities`③需时 `import(wasm)+init()` **3s 超时**④失败缓存 `sessionStorage`,刷新前不重试⑤明确文案⑥服务端仍重算校验兜底。**v8 修正:取消 `write_supported`**(服务端无法断言客户端能力,改为写路径重算校验 —— 见 D1.5.2 ⑤ / E8-P1-2;**P2-2**) | ✅ |
| P0-4 | D1.4 样例**缺 `vid_op_rev`**,与 DDL/D5.2 矛盾 | **样例已补**(列与值均含 `vid_op_rev=41`),并在样例与 D5.2 双处标注"**必须写入,漏写即被后续比较逻辑拒绝**" | ✅ |
| P0-5 | "丢弃 nebula → 强制重建"对**私有项目不可行** | 改为**按 `sync_mode` 分流**:私有(`off`/`backup`/`collab`,源码只在用户机器)→ **禁止丢弃**(drop 返回 **403**),只能重试或置 `graph_sync_status = pending_repair` **等客户端全量重传**,SLA = 用户下次上线;仅**服务端持源码**(公开/协作/超大)允许丢弃 + 重建;opensearch 任何模式可丢弃(A5.1) | ✅ |
| P0-6 | F2"恢复时 outbox 处理"**内部不自洽** | 给出 **8 步流程**(暂停消费 → 恢复 PG → **从 Nebula/OpenSearch 反查真实水位** → 重写 PG 水位 → **清 `state != acked` 的 outbox 行** → 回退 `last_synced_graph_rev` → 解除 paused → 校验),并明确**触发者=运维手册手动**、**窗口判据=仅 RPO(≤5min)内走恢复,超 RPO 走全量重建**(F2) | ✅ |
| P0-7 | `426` 无升级路径,形成**死锁** | 弃用 426(语义错误 + "刷新仍是旧代码")→ 改 **`409 contract_incompatible`**;宽限期内**降级可用**(不拒绝服务);前端收到 409 提示"关闭标签页重新打开"+ 请求 **`GET /api/version`**;**静态资源必须版本化**(否则刷新无效)(§7) | ✅ |

### E7.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | 冲突副本"谁生成 uid" | **uid 由解析器生成**(解析产物的一部分);冲突副本存**两份完整解析结果**;§6.2 改为"**上传选中的解析结果**",不重跑解析 | ✅ |
| P1-2 | Nebula `epoch` 无对齐场景 | **删除 Nebula 侧 `epoch`**(权威在 PG;写路径只比 `vid_op_rev`) | ✅ |
| P1-3 | `indexed_graph_rev` 的队列空判定有竞态 | **上传事务必须原子地同时写 `graph_rev+1` 与 outbox 行**(§5.4/§5.5),消除"永久 lag = 1" | ✅ |
| P1-4 | `node_ref = vid` 纯冗余 | **删除该字段**(`id(n)` 即 VID;省 32B/顶点 + 少一处不一致)(D5.3) | ✅ |
| P1-5 | B9"白名单"无具体字符集 | 新增 **B9.1 逐字段规范**:枚举类(严格枚举)/ 标识类(`[A-Za-z0-9_:.\-#@/]`,≤1024)/ 路径类(NFKC + 禁控制字符/`..`/绝对路径,≤4096)/ 展示类(仅长度+控制字符)/ 富文本类(白名单标签) | ✅ |
| P1-6 | 解析器升级**缺触发机制** | 明确:发布即打 `parser_v` → 客户端读元数据比对 → 不匹配则**本地全量重解析并重传** → 服务端写旧 VID 墓碑 → 墓碑期内**两版都查** → 清理条件(**保留 30 天 + 连续 7 天无 `parser_v` 变化**)(§9) | ✅ |
| P1-7 | G1"法定期限"未定义 | 明确**默认 2 年、超期物理删除**;去标识化范围(`user_id`/`project_id` → 匿名;`vid`/`uid`/`file_path` 保留);**每周清理作业** + 记审计(附录 G) | ✅ |
| P1-8 | F1 越权阈值**可被绕过**(卡在阈值下持续探测) | 增加 **单日总量**告警(<1000 绿 / 1000–10000 黄 / >10000 红)、**异常模式**(同一 `user_id` 短时探测多个 `project_id`)、**非公开项目探测一次即告警**(F1) | ✅ |
| P1-9 | "续租失败即降级"定义模糊 | 分类:网络/超时 → **短重试 1–2 次不降级**;重试仍失败 → **宽限态**(停写、等租约自然过期);**401/403/409 → 立即降级** + UI 显式提示原因(A3) | ✅ |
| P1-10 | 审计限流未分事件类型 | 改为**分级**:安全事件**不限流全落库**;常规操作多维限流 60/min;批量管理操作**按操作次数**限流(避免管理员批处理导致审计缺失)(B7) | ✅ |

### E7.3 P2 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | 附录版本号蔓延 | 全部附录标题统一为"**随主文 v7**(初版 vN)" | ✅ |
| P2-2 | `index_lag` 重复定义 | §5.5 表后加"**唯一计算口径**"声明,其他附录引用一律指向该处 | ✅ |
| P2-3 | E1 与 E6 的 P0 编号冲突 | E6/E7 加编号前缀说明(跨附录写作 `E6-P0-n`) | ✅ |
| P2-4 | A5 观测行与 F1 字段名不一致 | F1 补 **最老积压时长** 与 **各 target 失败率**;A5 标注"以 F1 为准" | ✅ |
| P2-5 | 算法降级缺出路(死锁) | 允许 owner **单向降级** `blake3 → sha256_16`(后台作业 + 全量重哈希/重传 + 旧 VID 墓碑 + 可回滚 + 审计)(D1.5.3) | ✅ |
| P2-6 | `tx` 静态检查"允许例外"不可判定 | 收窄为"**禁用标识符的直接引用**"(AST 可判);**间接引用由 code review 兜底**,不再试图判定 Promise 包装(F3) | ✅ |

---

## E8 第八轮评审处置(v8)

> **编号说明(P2-3/P2-7)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E8-P0-n`**(避免与 E1 的 `P0-1` 等混淆)。E7 / E9 同理。

### E8.1 P0 6 项(全部为**文档内部矛盾**,已逐条对齐)

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | A5 主表与 A5.1 关于"opensearch 是否进 dead"**直接矛盾** | A5 主表的"OpenSearch 可重建"行改为与 A5.1/§5.5 一致:**超 N 次失败同样进 `dead`**;进 dead 后**排除水位 + 记审计**;`rebuild_index` 只处理"未 dead"部分,drop 后触发重建 | ✅ |
| P0-2 | §5.2 / D1.2 / D1.5 与 D1.5.1 关于**默认算法**矛盾 | 公式改**参数化**:`vid128 = HASH(project_id + "\0" + uid, out_len=16)`,**`HASH` = `Project.hash_algo`(默认 `sha256_16`)**;D1.2 编码表同步参数化;D1.5 裁定表补"**默认值(必读)**"行 | ✅ |
| P0-3 | §4.1 `Project` 表**没有 `hash_algo`** | 已补 `hash_algo`(枚举,默认 `sha256_16`,创建时确定、允许 owner 单向降级)+ 顺手补 `uid_rule_v`;此前"`graph_node_ref.hash_algo == Project.hash_algo` 校验"因字段不存在而无法实现 | ✅ |
| P0-4 | §4.1 的 `vid_op_rev` 描述与 D5.1 **语义不一致** | §4.1 与 A5 统一为"**= `graph_rev`,不是独立序列**",并显式写出前提"**同一 `graph_rev` 内同 VID 只允许一条 outbox 行**" | ✅ |
| P0-5 | `ProjectSyncState.last_synced_graph_rev` **作用域未定义** | 主键写死 **`(project_id, device_id)`** —— 每 (项目, 设备) 一行;"跨端差距"按设备各算 | ✅ |
| P0-6 | §4.2 **边属性仍列 `epoch`** | 边属性删除 `epoch`(P1-2 的删除范围是"Nebula 侧",**不分顶点/边**) | ✅ |

### E8.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | 算法降级**需要客户端在线**才能完成 | 降级**仅允许在客户端在线且持租约时发起**(无租约 → `409 no_write_lease`);`migrating` **最长 7 天**,到期**自动回滚**为原算法 + 记审计;UI 提示"关闭标签页会中止"(D1.5.3) | ✅ |
| P1-2 | 能力上报**语义含糊**(静态上报 blake3 = 撒谎) | 拆开:**header 只声明同步可判能力(`sha256_16`)**;`blake3` 走**异步探测**(import WASM,3s 超时),结论由**前端本地**得出,可选**事后上报**(供观测);**服务端不再返回 `write_supported` 供继承**,改由**写路径重算校验**兜底(D1.5.2) | ✅ |
| P1-3 | 恢复时取 `max(vid_op_rev)` 是**乐观估计** | 改为**与 §5.5 同一套逻辑**:outbox 在 → `min(未完成且非 dead) − 1`;outbox 已清空 → **保守取 `min(各 VID 的 vid_op_rev)`,不用 `max`**(max 会高估→漏放→丢更新);原则**宁可重放不可漏放**(F2 步骤 3) | ✅ |
| P1-4 | B9.1 的"标识类"对 `vid` **重复且过松** | `vid` 单列一行:**严格 `^[0-9a-f]{32}$`**;`uid` 保留字符集白名单 + 长度上限(B9.1) | ✅ |
| P1-5 | §0 的"决策 6 项"与 §11 的 15 项**不一致** | §0 改为 **§11(15 项)** + 附录 A6/B8/C6/G,并指向 E1/E5/E6/E7 | ✅ |
| P1-6 | `dead` 阈值 N=8 应**可配** | 改为 **`AppSetting.outbox_max_attempts`(默认 8,可运维调整)**(A5.1) | ✅ |
| P1-7 | F3 的 CI 五项**未包含 E1.1 收口动作** | F3 增第 **6** 项:列全 **E1.1 收口用例**(`stale_baseline` / `CodeAnalysis` 不被改写 / 崩溃注入 / 并发抢锁 / 语义级临时上传后删除) | ✅ |
| P1-8 | "同事务"**未明确事务边界** | §5.4 明确为**同一 PG 事务**(`Project.graph_rev` 与 `graph_outbox` 同在 PG,**不涉及跨库**) | ✅ |
| P1-9 | "宽限态"**对 UX 影响未说明** | A3 补 4 条 UX 要求:时长 **≤60s**、UI **明确提示**、成功则**自动恢复可写**、期满转只读并**显示原因与恢复办法** | ✅ |
| P1-10 | G1"审计 2 年"与 F2"备份 14 天"**看似冲突** | 新增 **G5** 讲清分工:**审计历史由表 + 每月归档(2 年)保证;PG 备份(14 天)只保证业务连续性**,F2 无需把备份拉到 2 年 | ✅ |

### E8.3 P2 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | §4.2"算法记录位置"与 D1.5/§4.1 不一致 | 三处已一起对齐(P0-3 同批修复) | ✅ |
| P2-2 | F1 "dead>0 即 P0" 与 A5.1 的 `pending_repair` 语义冲突 | F1 增 **`pending_repair` 独立指标**(私有项目等待用户上线**不计 P0**;超 7 天转黄) | ✅ |
| P2-3 | E5/E6/E7 的 P0 编号混淆 | E5/E6/E7 文首统一加编号声明,**跨附录引用强制带前缀** | ✅ |
| P2-4 | §4.3 与 B4 的检索防护**重复定义** | **以 §4.3 为唯一口径**;B4 只强调"服务端强制、不接受客户端覆盖",不再复述参数 | ✅ |
| P2-5 | B10 日志脱敏与 `AuditLog` 关系未讲清 | B10 增"**审计表例外**":`AuditLog` 可存明文但访问受限 + 保留期受附录 G 管理;脱敏约束对象是**运维日志/stdout/外部日志系统** | ✅ |
| P2-6 | 合约"降级可用"**具体行为未定义** | §7 定死:缺失字段**不出现在响应中**(非 `null`);降级信息走响应头 **`X-Contract-Degraded: f1,f2`**;前端契约要求收到即**提示"部分功能可能不可用,建议刷新"** | ✅ |

> **机制性改进(本轮新增)**:主文文首新增**「文档自检清单」**(13 条"单一表述"概念,列出唯一正确写法与历史错误写法),并在 **F3 第 7 项**把它做成**自动检查**(命中"另一种写法"即 CI 失败)—— 针对本轮 6 个 P0 全部源于"同一概念两处写法"做根因防护。

---

## E9 第九轮评审处置(v9)

> **编号说明(P2-3)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E9-P0-n`**(避免与 E1 的 `P0-1` 等混淆)。

### E9.1 P0 5 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | **主文 §5.5 仍写"opensearch 不进 dead"**(v8 只改了 A5) | §5.5 改为"**opensearch 失败同样计入 `dead`(超 N 次)**;进 dead 后排除 `indexed_graph_rev` 水位 + 记审计";并**把 F3 第 7 项的扫描范围扩到主文全文** —— 这正是"清单列了但主文未扫到"的实例 | ✅ |
| P0-2 | **§11 第 11 行**仍写"改 BLAKE3-128、缺实现降级 SHA-256" | 重写为"**算法项目级确定(`Project.hash_algo`,默认 `sha256_16`);运行时禁止降级;缺实现 → 拒绝写入(只读浏览)**",并**补指 D1.5.1**(P2-8 同批) | ✅ |
| P0-3 | 迁移期 `Project.hash_algo=migrating` 与 `graph_node_ref.hash_algo` 校验**互相死锁**(重传写不进任何行) | 采**方案 A**:`migrating` 时**显式放行**一致性校验;新写入行带新算法标识;切换后恢复常规校验 + 跑全量巡检(D1.5.3) | ✅ |
| P0-4 | **边的 upsert/delete 顺序保证缺失**(v6 起只做顶点) | 采**方案 A**:边属性**也加 `vid_op_rev`**;写/删前 `FETCH PROP ON <edge> "<from>" -> "<to>" YIELD vid_op_rev` 比较;FIFO 分组键 = **`(vid_from, vid_to, type)`**(D1.3 / D5.2.1) | ✅ |
| P0-5 | outbox 消费的**跨 `graph_rev` 顺序**未明确(FIFO ≠ 版本顺序) | 采**方案 3**:消费者**按 VID 分组 → 组内按 `vid_op_rev` 升序排序后执行**;**禁止**按物理插入顺序 / 自增 id / `created_at`;并发注入用例:故意先插 42 再插 41,断言最终值 = 42(D5.2.2 / F3 第 6 项) | ✅ |

### E9.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | 宽限态 ≤60s 与"保留租约至 90s 过期"矛盾(只读却占租约 30s) | 转只读时**主动释放租约**(`writer_device_id=NULL` + `writer_epoch+1`);释放请求也失败时只能等 TTL 自然过期(A3) | ✅ |
| P1-2 | F2 的 `min(vid_op_rev)` **未加 `project_id` 过滤** | 改为"**在该 `project_id` 下**取 `min`,并经 DAO 强制注入"(单 space 多项目,否则水位被其他项目旧 VID 拉低)(F2 步骤 3) | ✅ |
| P1-3 | `pending_repair` **状态机未定义** | 新增状态机:**重传成功 + 校验通过 → `ok`**;7 天未传 → 升🟡(仍为 `pending_repair`);owner 删除 → `deleting`;变化记审计(F1) | ✅ |
| P1-4 | §4.1 "不可变 + 允许单向降级"措辞自相矛盾 | 改为"**创建时确定,仅允许 owner 单向降级 `blake3 → sha256_16`,反向不允许**"(§4.1) | ✅ |
| P1-5 | D1.5.2 步骤③ 未写判定条件 | 补"**仅当 `hash_algo == blake3` 且会话未缓存探测结果**才探测"(避免无条件白等 3s) | ✅ |
| P1-6 | `tx` 的类型签名允许 `await`,与"禁 await"约束冲突 | 回调改**同步签名** `fn: (ctx) => T`,Promise 只留最外层;确需异步时用显式 `txAsync?`(仅允许 IDB 包装)—— 类型系统成为第一道防线(C4.1) | ✅ |
| P1-7 | 墓碑清理缺**告警/备份** | F1 增**墓碑数量 / 单项目墓碑数**指标(>10 万 或连续 3 次清理失败即红);清理**前导出清单**;作业**幂等可重入**并与恢复演练同批(§9 / F1) | ✅ |
| P1-8 | D1.4 读样例未含 `vid_op_rev`,易被误当写前 FETCH | 样例加注"**读路径不需要**";并**补写前 FETCH 样例**(顶点 + 边)(D1.4) | ✅ |
| P1-9 | §5.4 "同事务"未明确**隔离级别** | 明确必须 **`SELECT … FROM project WHERE id=$1 FOR UPDATE`** 锁 `Project` 行(或改用 PG 序列),否则并发上传读到同一 `graph_rev` → **版本号重复、重放语义变脏**(§5.4) | ✅ |
| P1-10 | F3 第 7 项**扫描范围未写死** | 写死 = **主文全文 + 附录 A–G 全文(含修订轨迹、注释、代码块、决策表、处置表)**;唯一豁免 = **自检清单自身**(F3) | ✅ |

### E9.3 P2 8 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | §0 引用未含 E8 | 改为"E1 / E5 / E6 / E7 / **E8** / **E9**" | ✅ |
| P2-2 | E7.1-P0-3 未标 `write_supported` 已取消 | 已加"**v8 修正:取消 `write_supported`**"标注 | ✅ |
| P2-3 | §4.2 未提"可降级" | §4.2 补"该值可被 owner 单向降级(§4.1 / D1.5.3)" | ✅ |
| P2-4 | A5 的 OpenSearch 行**混入历史修订记录** | 主表只留**规则**,历史记录移至 **E8-P0-1** | ✅ |
| P2-5 | A5.1 与 F1 未互引 | A5.1 注"告警级别见 F1、状态机在 F1";F1 注"处置接口与 SLA 见 A5.1" | ✅ |
| P2-6 | `storage_port` 的 `LocalMeta` **未定义** | 补 **`LocalMeta` 字段清单**(= §3.3 `local_project` 去掉大负载) | ✅ |
| P2-7 | E8 文首格式与 E5/E6/E7 不一致 | 已统一为"编号说明(P2-3):…跨附录引用写作 `E8-P0-n`(避免与 E1 的 `P0-1` 等混淆)" | ✅ |
| P2-8 | §11 第 11 行未指 D1.5.1 | 已补指(与 P0-2 同批修复) | ✅ |

> **机制性改进(v9)**:①「文档自检清单」新增 **6 条**(**边顺序 / 消费排序 / `hash_algo` 变更措辞 / 能力判定权 / 自检扫描范围 / `pending_repair` 恢复定义**)②F3 第 7 项**扫描范围写死**为"主文全文 + 附录 A–G 全文",杜绝"只扫附录"。

---

## E10 第十轮评审处置(v10)

> **编号说明(P2-3)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E10-P0-n`**(避免与 E1 的 `P0-1` 等混淆)。

### E10.1 P0 4 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | **`DELETE VERTEX` 与墓碑语义矛盾**,使 `vid_op_rev` 在删除路径**彻底失效**(v9 把边接进来后被放大) | 统一为**软删除**:D5.2 步骤 4 改 `UPDATE VERTEX … SET lifecycle='deleted', vid_op_rev=$rev`;**边同理**;**物理删除仅限项目下线**(B5);D1.4 的 `DELETE VERTEX` 样例**已改写**并附反例;**F2 水位显式声明"墓碑计入"** | ✅ |
| P0-2 | §4.2 边属性**仍未列 `vid_op_rev`**(v9 漏改主文 —— 与 v7 漏删 `epoch` 同型) | §4.2 边属性补 **`vid_op_rev` + `lifecycle`**;自检清单新增对应条目 | ✅ |
| P0-3 | 迁移期与 **`UNIQUE(project_id, uid_sha256)` 冲突**(新行 `INSERT` 必然违约束 → 迁移卡死) | 采**方案 A(原地更新)**:`graph_node_ref` 用 **`UPDATE` 不用 `INSERT`**;`uid_sha256` 不变;`uid_hash_128`/`hash_algo` 原地更新;完成后全量巡检(D1.5.3) | ✅ |
| P0-4 | 迁移的**中断恢复 / 回滚 / 旧墓碑 `vid_op_rev`** 未定义 | 补齐:①**中断 → 从头重传**(幂等,不做断点续传)②**超时回滚** → 删新 VID 行、留旧行、**旧墓碑恢复到 `prev_lifecycle`** ③**旧墓碑 `vid_op_rev` 必须显式写入**(取该 uid 最后一次的 `graph_rev`),否则墓碑退化为 -1、**旧写复活** ④全程记审计(D1.5.3)。**周边连锁缺口已在 v11 补齐**(P2-4):**如何识别新旧行** → `graph_migration_log` + `graph_node_ref.migrating` 标记;****边怎么处理** → 回滚**先删新边再删新顶点**(P1-7);**outbox 怎么办** → 迁移前置**必须排空**(否则 `409 outbox_not_drained`,P1-6)。**注意:v11 自身又引入两个同源缺口**(`graph_migration_log` 等新表/列**未登记进 §4.1**、**迁移期请求体未定义**),见 **E12-P0-2 / E12-P0-4**(P2-2) | ✅ |

### E10.2 P1 8 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | 墓碑阈值(1 万/10 万)**缺依据**(一次解析器升级就打红) | 改**相对阈值**(> **3 倍现存**才红);真正的红信号是**连续 3 次清理失败**;墓碑数只做**趋势观测**(F1) | ✅ |
| P1-2 | "FETCH 不存在视为 -1"**不是 Nebula 默认行为** | D5.2 步骤 1 补注:**`FETCH PROP` 对不存在对象返回空结果集,DAO 层必须显式转 `-1`** | ✅ |
| P1-3 | 跨批次乱序**未声明** | D5.2.2 显式声明:**跨批次乱序是预期的**;"组内排序"≠"可省略比较逻辑",`FETCH → 比较` 是**最终防线** | ✅ |
| P1-4 | F3 第 7 项**实现机制未定义** | 补:**简单可判 → 正则黑名单(命中即阻断)**;复杂表述 → **CI 提取候选句 + 人工确认**,不追求全自动;整体定位为**辅助** | ✅ |
| P1-5 | "释放租约失败"语义(只读却占租约最长 90s,其他设备被挡) | ①客户端**持续重试释放**直到成功或 TTL 过期 ②**服务端提供「租约主动作废」接口**(凭 `device_id` + 会话 token 单方撤销自己的租约)(A3) | ✅ |
| P1-6 | `FOR UPDATE` 锁 `Project` 行**与 outbox 热点** | 改**首选 PG 序列分配 `graph_rev`**(不锁行 —— `file_rev_seq` 同在 `Project` 行,锁它会让同项目并发上传全串行);`FOR UPDATE` 降为**备选**(§5.4) | ✅ |
| P1-7 | 迁移期**并发上传**未定义 | 明确**只允许全量重传、禁止普通增量**(返回 **`409 migrating_full_resync_required`**);客户端进入"仅重传模式",UI 显示"**迁移中不可编辑**"(D1.5.3) | ✅ |
| P1-8 | 边**没有软删除标记字段** | 采**选项 A**:12 类边 DDL 统一补 **`lifecycle string`**;**不用 `dangling` 兼作删除标记**(语义混淆)(D1.3 / D5.2.1) | ✅ |

### E10.3 P2 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | 自检清单未含 v9 新概念与本轮新 P0 | 清单扩到 **23 条**:新增**删除语义(墓碑 + 保留 `vid_op_rev`)**、**迁移原地 `UPDATE`**、**迁移期只允许全量重传**、**顶点与边都有 `vid_op_rev` + `lifecycle`** | ✅ |
| P2-2 | §4.2 未提 `migrating` 取值 | §4.2 补"取值含迁移期临时值 `migrating`,该状态下**放行一致性校验**" | ✅ |
| P2-3 | E9 的 P0-4/P0-5 未指向 CI 用例 | F3 第 6 项扩展为"**E1.1 + E9 收口用例**"(含**墓碑不复活**、边乱序、跨批次乱序、迁移期 409) | ✅ |
| P2-4 | "全量巡检"未定义检查项 | D1.5.3 补**巡检清单 4 项**(无 blake3 残留 / 旧墓碑到位 / `edge_type_count` 一致 / 抽样 vid 校验) | ✅ |
| P2-5 | E1 的 P0-1 行仍是旧口径 | E1-P0-1 加注"**v6 起改默认 `sha256_16`,权威见 D1.5.1;本行是 v4 历史记录**" | ✅ |
| P2-6 | D5.2.1 / D5.2.2 重复写分组键 | D5.2.2 改为"**分组键见 D5.2.1**",不再重复列举 | ✅ |

> **机制性改进(v10)**:自检清单 **19 → 23 条**,新增项集中在**删除语义与迁移**(本轮两个最深缺口);F3 第 7 项补**实现方式**(正则黑名单 + 候选句人工确认),避免"想做但做不出来"。

---

## E11 第十一轮评审处置(v11)

> **编号说明(P2-3)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E11-P0-n`**。

### E11.1 P0 1 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | **`INSERT VERTEX` 在 Nebula 里不是 upsert**(VID 已存在会报错);`UPSERT VERTEX` 只更新"指定列" | 全部改写为 **`UPSERT VERTEX/EDGE … SET <全部列>`**:D5.2 步骤 3/4、D5.2.1、D1.4 样例(写 + 删);**删除路径也必须用 UPSERT 全列**(`UPDATE` 对不存在的 VID 静默无效 → P1-4);并新增 **`WHEN` 原子守卫**作首选(把"比较 + 写"合成原子操作,顺带消除 v10 的 FETCH 竞态);自检清单加第 24 条 | ✅ |

### E11.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | F2 的 `min(vid_op_rev)` **在 Nebula 无高效路径** | 改为**查 PG**:新增 `graph_node_ref.last_graph_rev`(outbox 消费成功后回写),用 `min(last_graph_rev)` 聚合(**毫秒级**);避免"**图不可用时反而无法恢复**"的悖论;Nebula `LOOKUP` 聚合降为**备选**并写明耗时上界与超时回退(F2 / §4.1) | ✅ |
| P1-2 | 墓碑阈值**单位歧义**(分母是活跃节点还是全部?) | 采**方案 A**:墓碑数**只做面板趋势观测、不参与告警**;告警改看**清理作业状态**(连续 3 天不下降 / 连续 3 次失败)(F1) | ✅ |
| P1-3 | §3.3 本地模型**未同步**(`deleted: bool` vs `lifecycle` 枚举) | `local_node` / `local_edge` 统一为 **`lifecycle`**;`local_edge` 补 `lifecycle`(v10 加边墓碑时漏同步 —— **v7/v9 同型问题第三次**) | ✅ |
| P1-4 | 软删除用 `UPDATE` **无法处理"VID 不存在"** | 删除路径改 **`UPSERT VERTEX/EDGE` 全列**(不存在的 VID 也会建立墓碑);并给反例:delete 先于 upsert 到达 → 墓碑没建 → 节点被建成 `active` 且**永不删除**(D5.2 / D1.4) | ✅ |
| P1-5 | 回滚**无法识别"哪些行要回滚"** | 新增 **`graph_migration_log(project_id, uid, vid_old, vid_new, target_kind, op, prev_lifecycle, created_at)`** + `graph_node_ref.migrating` 标记;log 保留至完成后 N 天(D1.5.3) | ✅ |
| P1-6 | 迁移期**旧 VID 的 outbox 未排空** → 旧 VID 复活、**图分裂** | 迁移**前置条件 = outbox 全部 `acked`**;未排空 → **`409 outbox_not_drained`**;迁移期**暂停消费**;24h 内无法排空 → 兜底**丢弃旧 VID 行 + 审计**(D1.5.3) | ✅ |
| P1-7 | 回滚时**新 VID 的边未处理** → 孤儿边 | 回滚顺序定死:**先删新边 → 再删新顶点 → 恢复旧墓碑 → 一致性校验**;`graph_migration_log` 记录**边**操作(D1.5.3) | ✅ |
| P1-8 | "旧墓碑恢复为 `active`"**丢状态** | 墓碑化时记录 **`prev_lifecycle`**;回滚**恢复到原状态**(`hidden` 不会被变成 `active`)(D1.5.3) | ✅ |
| P1-9 | "从头重传"与"7 天超时"**交互未定义** | 超时从 **`last_client_activity_at`** 起算(不是"迁移发起");每次上传刷新;距超时 24h 提醒 owner;超时后回滚且**可重新发起**(D1.5.3) | ✅ |
| P1-10 | **`graph_sync_status` 字段与状态机未定义**(A5.1/A5.2/§5.5 多处引用) | §4.1 补字段;**附录 A5.3 补完整状态机**(6 状态 + 进入/退出条件 + 转移记审计 + UI 显示原因) | ✅ |

### E11.3 P2 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | §4.1 `hash_algo` 枚举未含 `migrating` | 改为 **`sha256_16 \| blake3 \| migrating`** | ✅ |
| P2-2 | 自检清单未覆盖 §3.3 / §4.1 的字段一致性 | 清单**声明检查范围**(见下):schema 类条目要求 **§3.3 + §4.2 + D1.3** 三处一致 | ✅ |
| P2-3 | F3 的"正则黑名单"未列具体词 | 给出**黑名单词清单**(`INSERT VERTEX` / `DELETE VERTEX` / `426` / `write_supported` / `index_lag_s` / `op_rev` / `UPDATE VERTEX…lifecycle` …),命中即阻断,说明性引用走白名单上下文 | ✅ |
| P2-4 | E10.1-P0-4 与 P1-5/6/7 的交叉引用缺失 | 已在 E10.1-P0-4 补"**识别新旧行 / 边处理 / outbox 排空**"三项引用 | ✅ |
| P2-5 | §0 附录列表未含 E10 | §0 改为"…E9 / **E10** / **E11**" | ✅ |
| P2-6 | 文档膨胀(约 100 页,历史占 80%)**无收敛机制** | **附录 E 顶部新增"当前门槛速查" + 归档说明**:E1–E5 标注**归档**(保留供审计,不作权威);主文 §0 只指向**最近两轮** | ✅ |

> **机制性改进(v11)**:①自检清单新增"**同步路径写入语句 = `UPSERT` 且列齐全**"、"**迁移审计必须独立于 `graph_node_ref`**"、"**`migrating` 超时从 `last_client_activity_at` 起算**")②清单**声明检查范围**③F3 黑名单**列出具体词**④引入**归档机制**控制文档膨胀。

---

## E12 第十二轮评审处置(v12)

> **编号说明(P2-3)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E12-P0-n`**。

### E12.1 P0 5 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | 删除路径的 **`<其余列>` 不可实现**(删除请求只带 uid,服务端没有列数据) | 采**方案 C**:步骤 4 明确"**只写 `lifecycle` + `vid_op_rev`**",与步骤 3 的"全列"要求**明确区分**;VID 存在 → 其余列保留旧值;VID 不存在 → 建"仅两列"的墓碑(**允许**,后续 upsert 覆盖);D1.4 删样例同步改写 | ✅ |
| P0-2 | **`graph_migration_log` 与 `graph_node_ref.migrating`/`migrated_at` 未登记进 §4.1**(第四次"新增字段漏改主文档") | §4.1 已补 **`graph_migration_log` 表** + **`migrating` / `migrated_at` 列**;自检清单新增"**D 附录新表/列必须登记 §4.1**";F3 第 7 项加**交叉登记检查**(脚本断言"附录 D 的 PG 表/列都能在 §4.1 找到") | ✅ |
| P0-3 | F2 的 `min(last_graph_rev)` **未定义 NULL 处理** | 改为 **`COALESCE(MIN(last_graph_rev), 0)`**;并写明原因:`last_graph_rev` 只在**消费成功**时回写,未消费/pending/dead 都是 **NULL**,而 **PG 的 `MIN()` 会跳过 NULL** → 会**高估水位 → 漏放 → 丢更新**(违背"宁可重放不可漏放")(F2) | ✅ |
| P0-4 | **迁移期请求体 schema 未定义**(客户端如何同时给旧/新 VID) | 定稿:①`upserts` 用**新 VID** ②删除/墓碑用 **`(uid, old_vid)`** ③整体按 **`(uid, old_vid, new_vid)` 三元组** ④每写一行新 VID 即记一条 `graph_migration_log`;并新增 **`Project.hash_algo_migrating_from`**(否则**回滚不知道恢复成什么**)(D1.5.3 / §4.1) | ✅ |
| P0-5 | **自检清单说 26 条、实际不符**(清单自己数错) | **改为"以表格数据行为准(当前 29 条)"**,并加 **F3 脚本断言"声明条数 == 数据行数"** —— 清单是其他一致性检查的依据,自己数错就失去可信度 | ✅ |

### E12.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | `graph_migration_log` **保留期未给具体值** | 明确 **30 天(可配)**,**从迁移结束(完成或回滚)起算**(D1.5.3) | ✅ |
| P1-2 | 回滚后 **`migrating` 标记未定义清除** | 回滚完成 → **清除 `migrating`(置 FALSE)**,**保留 `migrated_at`** 作为"曾迁移/已回滚"证据(D1.5.3) | ✅ |
| P1-3 | `WHEN` 在 **VID 不存在时**的行为未定义 | 明确**必须实测目标版本**;"确定是新 VID"→ **不带 `WHEN`**,"可能已存在"→ 才带 `WHEN`;若实测为 NULL 语义 → **回退 `FETCH` 比较 + FIFO** 路径(D5.2) | ✅ |
| P1-4 | 黑名单的"说明性引用白名单"**无可执行定义** | 定规则:**只检查非注释行**(反例一律放代码块注释/引用块,`--`/`//`/`>` 开头跳过)+ **该行或其前一行含 `[禁止\|不得\|反例\|错误\|弃用]` 则豁免**(F3) | ✅ |
| P1-5 | F3 第 6 项未含 **E11 的收口用例** | 已扩:E11 部分加 **⑨ 项** —— 写入语句 lint / `WHEN` 并发对比 / 迁移链路(含回滚顺序)/ `graph_sync_status` 状态机(F3 第 6 项) | ✅ |
| P1-6 | `last_graph_rev` **更新时机与 outbox 状态的关系**未定义 | 补**四情形表**:成功 → 回写;被 `vid_op_rev` 拒绝 → **回写 + 记审计**;写失败 → **不回写**;进 `dead` → 不回写;PG 回写失败 → 下次重放(幂等可接受)(F2) | ✅ |
| P1-7 | §0 与 E4 引用的"26 条"需同步修正 | §0 / E4 均已改为引用清单实际编号与**顶部速查表**,不再手写条数 | ✅ |
| P1-8 | 主文 §5.4 **残留 `index_lag_s`**(v5 已弃用) | 已改为 **`index_lag`(版本差)**,并注明是遗留写法 —— 再次说明"扫描范围"必须**真正覆盖主文全文**,而不是只宣布覆盖 | ✅ |
| P1-9 | 自检清单缺 **`graph_sync_status` 取值一致性** | 清单新增"**取值集合 = `ok \| degraded \| paused \| pending_repair \| inconsistent \| migrating`,三处一致**(§4.1 / A5.3 / F1)" | ✅ |
| P1-10 | `last_synced_graph_rev` 与 `Project.graph_rev` 的**关系未定义** | §4.1 明确:"**上传成功那一刻 = 新的 `graph_rev`,与 outbox 消费进度无关**";并声明它是**客户端视角**维度,与 `nebula_graph_rev`/`indexed_graph_rev`(**服务端落库进度**)**不可混用** | ✅ |

### E12.3 P2 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | 修订轨迹的 ④⑤⑥ 与 E11 的编号**不同步** | v12 起,修订轨迹**只写"见附录 ENN 的逐条处置"**,不再另立编号,避免读者两套编号对照 | ✅ |
| P2-2 | E10.1-P0-4 的交叉引用**缺 v11 新引入的 P0** | 已补"**v11 自身又引入两个同源缺口**(`graph_migration_log` 未登记 §4.1、迁移期请求体未定义)→ 见 E12-P0-2 / E12-P0-4" | ✅ |
| P2-3 | B5 的"删除语义"未强调**用 `UPSERT` 不用 `UPDATE`** | B5 已补该句(实施者只读 B5 时不会退回 `UPDATE`) | ✅ |
| P2-4 | 自检清单检查范围未覆盖 **§4.1** | 范围扩为**六处**:§3.3 + **§4.1** + §4.2 + D1.2 + D1.3 + D1.5.3 | ✅ |
| P2-5 | E 顶部速查与 E4 **重复**且可能不一致 | **只保留顶部速查**;E4 改为"计数以顶部速查为准,此处不重复" | ✅ |
| P2-6 | **两个版本线**(v3.25 内容版本 / v11 评审轮次)关系未说明 | 在文首"文件名说明"处**明确两线关系**,并统一:附录标题跟**评审轮次**、正文标题同时给出两线 | ✅ |

> **机制性改进(v12)**:①自检清单**条数改为"以表格为准"+ 脚本断言**(治"清单自己数错")②新增**交叉登记检查**(附录 D 的新表/列必须在 §4.1 出现)③新增"**删除路径写入列**"与"**`last_synced_graph_rev` 维度**"两条清单项④F3 第 7 项补齐**豁免的可执行规则**与**清单自计数校验**。

---

## E13 第十三轮评审处置(v13)

> **编号说明(P2-3)**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用时请写作 **`E13-P0-n`**。

### E13.1 P0 5 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | **§5.4 请求体未反映迁移期协议**(v12 只改了 D1.5.3 —— **第五次"漏改主文档"**) | §5.4 已补:请求体加顶层 **`migration: true`**、`upserts.nodes[].old_vid` / `.new_vid`、**`deletes.nodes[]` 迁移期改为 `[{uid, old_vid}]`**;并新增**常规期 vs 迁移期字段对照表**;自检清单新增"**D 附录的协议字段必须体现在 §5.4**"(与"D 附录新表/列必须登记 §4.1"**配套:一个管表/列,一个管协议/字段**) | ✅ |
| P0-2 | D5.2 的"**NE 语义**"断言与"必须实测"**自相矛盾** | **删除 NE 断言**(它是未经测试的猜测;若实为 **NULL 语义**则新 VID 首次写入被拒 → **outbox 全面失败、系统不可用**);**明确"实测前禁止使用 `WHEN`"**,默认路径 = `FETCH` 比较 + 每 VID FIFO(D5.2) | ✅ |
| P0-3 | F3 的"`WHEN` 并发对比测试"**依赖未定义行为** | 拆成**两阶段**:**实测前**该测试**仅用于探测**语义(结论写 `AppSetting.when_guard_semantics`),**不算收口用例**;**实测后**按结论分流(`ne` → 做对比测试;`null`/`unsupported` → 禁用 `WHEN`,只测 `FETCH + FIFO`)(F3 第 6 项) | ✅ |
| P0-4 | A5.3 状态机**没有优先级顺序** | 补**全序**:`migrating > inconsistent > pending_repair > paused > degraded > ok`;并明确"**同时满足多个进入条件取最高者,退出回到仍满足条件的最高优先级状态**"(A5.3) | ✅ |
| P0-5 | 迁移期请求体**未定义端点** | 定稿:**复用 `/api/projects/<key>/graph/sync/`**(不新开路由,避免路由爆炸),差异**只在请求体字段 + 服务端按 `hash_algo` 分支校验**;**迁移期缺字段 → 400,常规期误带 `migration` → 400**(双向严格)(§5.4 / D1.5.3) | ✅ |

### E13.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | 保留期"从迁移结束起算" —— **"结束"未定义** | `graph_migration_log` 增 **`migration_ended_at`**,由迁移作业在"完成/回滚"时写入;**保留期从该字段起算**(D1.5.3 / §4.1 字段已对齐) | ✅ |
| P1-2 | `hash_algo_migrating_from` **清空动作由谁触发**未定义 | 明确:由 **`migrate` 作业的"完成/回滚"分支在同一事务内**清空(与 `migrating` 标记清除、`migration_ended_at` 写入**同批**),**不依赖运维手工**(D1.5.3) | ✅ |
| P1-3 | 缺"**§4.1 与 D 附录字段名一致**"的反向检查 | 自检清单新增"**跨附录字段名逐字一致**"(`hash_algo_migrating_from` / `last_graph_rev` / `prev_lifecycle` / `migration_ended_at` / `target_kind`) | ✅ |
| P1-4 | F2 的"四情形表"**嵌在表格单元格里** | 已提为**独立小节 F2.1**(同时修掉"步骤表被表中断开"的结构问题,并把被打散的"墓碑必须计入"说明归位) | ✅ |
| P1-5 | `WHEN` 实测结论**记在哪里**未定义 | 明确写入 **`AppSetting.when_guard_semantics`**(枚举 `ne` \| `null` \| `unsupported`),并由 **worker 启动自检**校验"存在且与运行时行为一致",不通过**拒绝启动**(D5.2) | ✅ |
| P1-6 | 迁移日志与 Nebula 写入的**原子性**未定义 | 改**两阶段"意图 → 应用"**:**先写日志**(`applied=false`)→ 写 Nebula → **标记 `applied=true`**;**回滚只删 `applied=true` 的行**,未应用的行可直接丢弃(D1.5.3 / §4.1) | ✅ |
| P1-7 | 客户端**可能提供不出 `old_vid`**(旧图已清空) | 采**方案 B**:**服务端反查**(用迁移前保留在 `graph_node_ref` 的旧 `uid_hash_128` 推旧 VID),客户端**只传 `uid`**、可选上送 `old_vid` 作交叉校验(不一致即拒绝)(D1.5.3 / §5.4) | ✅ |
| P1-8 | 交叉登记检查的**扫描范围**未定义 | 写死:**只扫附录 D 的代码块 + D1.3 的 `CREATE TAG/EDGE` 内声明列**,**不扫正文与注释**(避免误报);另加 **⑥协议字段检查**(断言 §5.4 出现 D 附录定义的协议字段)(F3 第 7 项) | ✅ |
| P1-9 | 黑名单**未含迁移期协议字段** | 已补:`deletes.nodes = ["uid…"]` 裸字符串形态(**仅常规期合法**)、`old_vid` / `new_vid`(**仅迁移期上下文合法**,豁免规则:该行或前一行含 `[迁移\|migration\|migrating]`)(F3 第 7 项) | ✅ |
| P1-10 | "上传成功即已同步"**与用户体验矛盾**(服务端图可能还没更新) | §4.1 明确**徽章分两级**:①「**已上传**」= `last_synced_graph_rev` 已刷新 ②「**服务端已可见**」= `indexed_graph_rev` 追平该设备版本 —— **两级都显示**,用户才能区分"数据已送达"与"他人可查询" | ✅ |

### E13.3 P2 8 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | "扫描范围"与"检查范围"**术语易混** | 改名:**「F3 脚本扫描范围」**与**「概念一致性范围」**(主文文首) | ✅ |
| P2-2 | 修订轨迹仍**引用 v11 的内部编号** ④⑤⑥ | 改为直接引用 **E11-P1-10 / E11-P1-3 / E11-P2-6**(与"v12 起不再另立编号"一致) | ✅ |
| P2-3 | §0 引用未更新(仍指 E10 / E11) | 已改为 **E12 / E13**,并显式声明"**v12 / v13 为修补轮,无新增待评审决策**";§11 的 15 项不变 | ✅ |
| P2-4 | `target_kind` 的**字段写法不一致** | 统一为 **`target_kind`(枚举:`node` / `edge`)**(去掉易误读的冒号+转义竖线写法) | ✅ |
| P2-5 | F3 第 6 项**编号序列错乱**(⑥–⑨ 接在历史 ①–⑤ 后) | 改为**三组并列**:「E1.1 部分 / E9 部分 / **E11 部分**」,**组内各自编号** | ✅ |
| P2-6 | `last_graph_rev` **未说明初值** | 明确 **初值 `NULL`**(未消费即 NULL),并说明"**这正是 `COALESCE` 存在的原因**"(F2.1 / §4.1) | ✅ |
| P2-7 | A5.3"`migrating` 要求暂停消费"与 D1.5.3"迁移期用 `paused`"**矛盾** | 统一:**迁移期状态就是 `migrating`,`migrating` 隐含"暂停消费"**;**`paused` 只用于 `rebuild_index` 场景**(A5.3) | ✅ |
| P2-8 | 附录 E 顶部**未提 E12**(归档说明只到 E5) | 已改为"**E1–E13 全部是已关闭的处置**,最近两轮为 E12 / E13" | ✅ |

> **机制性改进(v13)**:①**"协议/字段"级交叉登记**与原有"表/列"级配套 —— 治"漏改主文档"这类第五次复发的问题 ②**`WHEN` 从"首选"降级为"实测后才允许"**(默认路径回到 `FETCH + FIFO`)③**状态机补优先级全序**,消除"多状态同时成立"的歧义 ④**迁移日志两阶段化**("意图 → 应用"),让日志同时是意图记录与回滚清单。

---

## E14 第十四轮评审处置(v14)

> **编号说明**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用写作 **`E14-P0-n`**。

### E14.1 P0 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | §5.4 迁移期请求体**只覆盖 `nodes`,漏了 `edges`**(**第六次同型**) | §5.4 请求体样例与对照表**补边**:`upserts.edges[].old_vid_from` / `.old_vid_to` / `.new_vid_from` / `.new_vid_to` **四个均必填**;`deletes.edges[]` 迁移期为 `[{from_uid,to_uid,type, old_vid_from, old_vid_to}]`。**理由**:边**没有独立 VID**,身份 = `(vid_from, vid_to, type)`,端点 VID 迁移后会变 → 不显式给旧端点就**不知给哪条边写墓碑**、给不出新端点就**12 类边全部无法迁移(图里只剩顶点)**;协议字段检查改为**从附录 D 动态提取**(手写清单必漏) | ✅ |
| P0-2 | "服务端反查 `old_vid`"与"`uid_hash_128` 原地 `UPDATE`"**直接矛盾** | 采**方案 A(预填日志)**:**迁移开始前**把旧 `uid_hash_128` **批量快照进 `graph_migration_log`**(`vid_old` / `vid_old_from` / `vid_old_to`),**之后**才做原地 `UPDATE`;回滚与"服务端补齐 `old_vid`"**一律以日志为准**,**不依赖 `graph_node_ref` 保留旧值**;与"两阶段:先写意图"**天然契合**(预填即第 0 步) | ✅ |
| P0-3 | `applied` / `migration_ended_at` **未登记 §4.1**(**第六次**) | §4.1 的 `graph_migration_log` 补齐 **`applied`(布尔)**、**`migration_ended_at`**,并**同时补边端点 4 列**(`vid_old_from`/`vid_old_to`/`vid_new_from`/`vid_new_to`);F3 交叉登记检查**扫描范围扩到"D 附录的代码块 + 表格"** —— 因为这两个列是**写在表格里的**,只扫代码块会**再次漏** | ✅ |
| P0-4 | 迁移期**掉线 / 重登录 / 换设备 / 刷新**未定义 | 定稿:**迁移会话是项目级的**(不绑发起设备),任何**持有效租约**的设备可**接力**;**续传起点以 `graph_migration_log.applied = FALSE` 为准**(不重头传,进度权威只在服务端日志);**新设备流程** = 抢租约 → 读日志 → 从 `applied=FALSE` 继续;**旧设备复活** → `writer_epoch` 更小 → **`409 stale_writer` 降级只读**,不会破坏迁移;并**修正 D1.5.3 的"从头重传"**(只适用单设备,多设备下不必要且可能与接力者冲突) | ✅ |
| P0-5 | `migrating` 时客户端**不知道目标算法** | 写死:**迁移目标算法固定为 `sha256_16`**(与"仅允许 `blake3 → sha256_16` 单向"一致);客户端迁移期**一律用 `sha256_16` 算 `new_vid`** —— 这是**协议约定**,不依赖元数据;元数据**可选**返回 `hash_algo_target` 作冗余提示,客户端**不得**据此分支;**禁止**"双向 / 目标可配置" | ✅ |
| P0-6 | 迁移的**端到端发起路径**未定义 | 补**九步链路**(只读提示 → 二次确认 → `POST /api/projects/<key>/hash-migration/` → 服务端校验并置 `migrating`+**预填日志** → 返回进度 → 客户端仅重传模式 → 服务端校验数量一致 → 切 `sha256_16` + 写 `migration_ended_at` → 客户端恢复正常)。**端点分工**:迁移**动作**走 `hash-migration/`(含 `DELETE` 取消/回滚,仅 owner + 仅 `migrating` 期),**图谱数据**仍走 `/graph/sync/`;**进度**由元数据接口的 `migration: { total, applied, last_client_activity_at }` **轮询**获取(不引入 SSE) | ✅ |

### E14.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | "自计数校验"与"不声明条数"**语义矛盾** | 采**方案 A**:脚本**只统计清单数据行数并在 CI 打印**(供人工核对),**不做"与声明值相等"的断言**;正文**去掉手写数字**(v13 的"当前 31 条"已删) | ✅ |
| P1-2 | `WHEN` 语义测试**边界不清** | 明确:**探测部分必跑**(必须在 CI 跑通,即使结论是 `unsupported`),**写入 `AppSetting.when_guard_semantics` 是收口动作**;**对比测试是条件收口**——仅 `ne` 时必需,其他结论**自动跳过**(不是"不通过") | ✅ |
| P1-3 | `deleting` **未在状态机中定义** | 定为 **`graph_sync_status` 第 7 个状态**,优先级**最高**(`deleting > migrating > …`);进入后**拒绝所有写**(含全量重传);**`migrating` 中的项目不能删除**(必须先取消迁移);A5.3 表 + 全序 + F1 均已补 | ✅ |
| P1-4 | 迁移期缺 `migration` 字段返回 **400 还是 409** | 改为 **`409 migration_required`**(请求格式本身没错,是**与项目状态冲突**)并说明客户端应**重读元数据、切换到迁移期请求体**;常规期误带 `migration` 仍 **400**(请求体确实错) | ✅ |
| P1-5 | 迁移期与 **F2 恢复互斥**未定义 | 采"**允许恢复但恢复后强制回滚迁移**":备份可能回退到迁移前(`graph_migration_log` / `graph_node_ref` 都是旧值),故恢复结束后由 **`migrate` 作业走回滚分支**并**记审计**;恢复期间**不置 `paused`**(沿用 `migrating`) | ✅ |
| P1-6 | "排空 outbox"与"全量重传"的 **UX 死锁** | **发起迁移即冻结普通上传**(进入 `migrating` 后不再接受增量,返回 `409 migrating_full_resync_required`);"排空"因此是**一次性动作**;**UI 必须在发起前告知"迁移期间不可编辑,预计 X 分钟"** | ✅ |
| P1-7 | F3 协议字段检查**没含边的字段** | 改为**从 D 附录动态提取所有"请求体字段名"**(当前含 `migration`、`old_vid`、`new_vid` + **边的 4 个**),再断言**都出现在 §5.4**;禁止手写清单 | ✅ |
| P1-8 | "24h 无法排空则丢弃旧 VID 行"**没说谁触发** | 明确:由**发起迁移时的前置校验**判定(自上次"发起迁移"起算超 24h)→ **`409 outbox_not_drained_24h`** + 响应里给出"可选择丢弃";**必须用户显式选择"丢弃"才执行**(不自动兜底);丢弃后**旧 VID 图数据保留在 Nebula**,**迁移立即开始** | ✅ |
| P1-9 | 迁移期 `write_supported` / `409 hash_algo_unsupported` 语义 | 明确:服务端**不再返回 `write_supported`**;**`409 hash_algo_unsupported` 只在终态**(`sha256_16` / `blake3`)**且客户端能力不足时**触发;**`migrating` 期一律走迁移协议**(客户端用 `sha256_16` → 服务端放行一致性校验 → **不会**触发);迁移期唯一写限制错误 = `409 migrating_full_resync_required` 与 `409 migration_required` | ✅ |
| P1-10 | `pending_repair` 与 `deleting` 的交互 | A5.3 明确 **`pending_repair → deleting` 是合法出口**;outbox 未消费行由**附录 G 第 2 步"停消费"+ 第 5 步"删 outbox"天然清理**,**无需先"取消 pending_repair"** | ✅ |

### E14.3 P2 8 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | §4.1 与 D1.5.3 的 `graph_migration_log` 字段**不一致** | 已对齐(§4.1 补 `applied` / `migration_ended_at` / 边端点 4 列),并在两处**逐字同列** | ✅ |
| P2-2 | "方案 B 反查"与"原地 UPDATE"**措辞冲突** | 已二选一,**定为方案 A(预填日志)**;D1.5.3 中"服务端反查 `graph_node_ref` 旧值"的写法**已删除** | ✅ |
| P2-3 | F3 的 ④ `WHEN` 是否**阻断**未说明 | 已明确:**探测必跑**;**对比**在 `ne` 时必需、其他结论**自动跳过** | ✅ |
| P2-4 | A5.3"`migrating` 隐含暂停消费"与 D1.5.3"`paused`"**仍不一致** | D1.5.3 的 "(`graph_sync_status=paused`)" 已改为 "(`graph_sync_status = migrating`,该状态**隐含**暂停消费)" | ✅ |
| P2-5 | §0 硬编码"最近两轮 E12/E13",**每轮都要改** | 改为**动态引用**:"处置记录与收口状态见**附录 E 顶部的「当前门槛速查」**(内含**最新两轮**)" | ✅ |
| P2-6 | 清单"31 条"是**手写数字** | 正文**去掉数字**(只写"以表格数据行为准");如需硬断言,改用 HTML 注释元数据而非正文数字 | ✅ |
| P2-7 | `applied = FALSE` 的行**保留多久** | 明确:**`applied = FALSE` 的行在回滚完成后立即清空**;`applied = TRUE` 的行保留 **30 天**(自 `migration_ended_at`) | ✅ |
| P2-8 | D1.5.3 的"**三元组**"与 §5.4 实际字段名不一致 | 统一表述为"**每个节点项携带 `uid` + `old_vid` + `new_vid` 三个分离字段**(迁移期)",不再用"三元组" | ✅ |

> **机制性改进(v14)**:①**协议字段检查从"手写清单"改为"从 D 附录动态提取"** —— 手写必然漏(本轮就漏了边的 4 个字段)②**交叉登记扫描范围加入"D 附录的表格"** —— 列常写在表格里,只扫代码块会漏 ③**"旧值从哪来"统一为"预填日志"** —— 把"反查依赖"变成"日志依赖",与两阶段设计合并 ④**迁移会话去设备绑定** —— 让长周期(7 天)迁移可接力、可恢复 ⑤**`deleting` 进入状态机全序**,补上"删除"这个压倒性状态。

---

## E15 第十五轮评审处置(v15)

> **编号说明**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用写作 **`E15-P0-n`**。

### E15.1 P0 6 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | `graph_migration_log` **没有 `edge_type`** → 回滚无法定位边 | §4.1 与 D1.5.3 **补 `edge_type`(边行必填,顶点行 NULL)**:日志自称"边身份 = `(vid_from, vid_to, type)`"却**没记 `type`** → 回滚时读到两个端点却**不知道是 `CALLS` 还是 `IMPORTS`**,而 nGQL **边类型是必填参数** → **12 类边回滚全部无法执行**。**回滚语句定稿**:`DELETE EDGE <edge_type> "<vid_from>" -> "<vid_to>"`(**三个参数全取自日志**);**§5.4 的 `type` 必须原样写进 `edge_type`**;F3 交叉登记覆盖"边行必需列 = 4 个端点 VID + `edge_type`" | ✅ |
| P0-2 | §5.4"客户端必填"与 D1.5.3"服务端从日志取"**直接矛盾** | 统一为"**服务端必需、客户端可选上送(仅对账)**":`old_vid*` **服务端从预填日志取**、`new_vid*` **服务端自算**(目标算法固定);表格"必填"列改为"**服务端必需**";客户端值不一致 → **拒绝 + 审计**。这样也解掉"客户端本地旧图已清空 → 拿不出权威旧值"的死结 | ✅ |
| P0-3 | F2 步骤 1 / 步骤 7 与 A5.3"迁移期不置 `paused`"**冲突** | F2 步骤 1 改为**分两种情形**:`migrating` → **沿用 `migrating`**(它已隐含暂停消费,**不另置 `paused`**);否则置 `paused`。步骤 7 改为:恢复前是 `migrating` → **交由 `migrate` 作业走回滚分支**(不是简单解除) | ✅ |
| P0-4 | `deleting` × `migrating` 的**交互不完整** | A5.3 新增规则 7(场景表):`migrating` 期删除 → **`409 migration_in_progress`**;**取消失败 → `inconsistent`**(不允许继续删);取消成功 → 回 `hash_algo_migrating_from` 后再删;**删除中途不可取消**(反悔入口在进入前);`deleting` 中途重启 → **幂等可重入**(附录 G 步骤标记);两者同时成立 → **取 `deleting`**(正常路径不应出现) | ✅ |
| P0-5 | `applied = TRUE` 的**语义粒度未定义**(批量部分失败) | 定死为**批次粒度**:每批先写日志(`applied=FALSE` + `batch_id`)→ 整批写 Nebula → **全部成功才标 `TRUE`**;**部分失败保持 `FALSE`**(不逐行标 TRUE:会误删不存在数据并污染审计;也不逐行确认:与批量原子性冲突);**回滚只删 `TRUE`**(`FALSE` 重放更安全);审计带 `batch_id` | ✅ |
| P0-6 | 迁移期**手动 `hidden` 状态会丢失** | **方案 1**:迁移期 `lifecycle` **由客户端显式上送**,服务端**不得用解析产物覆盖**;客户端**必须保留旧图 `lifecycle`**(`local_node`/`local_edge`),§3.3 补"**迁移期保留义务**";回滚仍用 `prev_lifecycle`;兜底(客户端已丢状态)→ 写 `active` 但**UI 发起前告知** | ✅ |

### E15.2 P1 10 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | F1 **未含 `deleting` 与迁移进度** | F1 补 **4 行**:`deleting` 计数与时长(>2h 黄 / >24h 红)、迁移进度(`applied` 批次 / 总批次,2h 无推进红)、`migrating` 时长(接近 7d 自动回滚阈值前告警)、`graph_migration_log` 增长异常(疑预填失败/循环写日志) | ✅ |
| P1-2 | 墓碑指标与 `deleting` **不联动**(会误导运维) | 明确**墓碑统计只含 `code_node`/边(`lifecycle = deleted`)**;`deleting` 是 `Project` 字段,**不进入墓碑统计** | ✅ |
| P1-3 | `hash_algo_target` 是**死 API 字段** | **已删除**(保留"目标算法固定 `sha256_16`"的协议约定;若需观测,由管理端从 `hash_algo_migrating_from` + 固定规则推导,不新增接口字段) | ✅ |
| P1-4 | "从 D 附录动态提取"**提取规则未定义** | **新增附录 D7「请求体字段清单」** 作为 F3 的**唯一提取来源**(全篇提取会把 nGQL 属性 / PG 列 / 本地字段一并捞出 → 误报且无法验证遗漏);F3 ⑥ 改为**只读 D7** + **双向断言**(清单 ⊆ §5.4,且 §5.4 迁移期字段 ⊆ 清单);边的 5 个字段**必须成组出现** | ✅ |
| P1-5 | 常规期带 `old_vid*` **未定义**(忽略还是拒绝) | 明确:**常规期请求体禁含 `migration` / `old_vid*` / `new_vid*` / `edge_type`**,JSON Schema 用 **`additionalProperties: false`**(否则拼错字段名会被**静默吞掉**) | ✅ |
| P1-6 | 迁移期 `deletes.edges[]` **为何必填两个 old VID** | 与 P0-2 同源:**不再必填**,服务端**从日志取**(客户端可选对账);常规期与迁移期**字段形态保持一致** | ✅ |
| P1-7 | **优先级与进入条件的交互**未说明 | A5.3 新增规则 6:**优先级全序只在"多个状态已同时成立"时裁决**;**进入条件本身先做互斥检查**(如"请求删除"先查 `migrating`,是则**拒绝且不进入 `deleting`**) | ✅ |
| P1-8 | **取消迁移的完整流程**未定义 | 明确:**手动取消 = 手动回滚**,与**超时自动回滚走同一套代码路径**;取消**成功** → 回 `hash_algo_migrating_from` 后再删;**失败 → `inconsistent`**(不允许继续删);**D1.5.3 的"取消/回滚"行已补全链路** | ✅ |
| P1-9 | `DELETE hash-migration/` 与项目删除**可能冲突** | 明确:①`deleting` **拒绝一切其他操作**(含取消迁移)②两个端点**互斥**(同项目同刻只允许一个)③**均幂等**(重复调用返回同一结果) | ✅ |
| P1-10 | 迁移期 `confidence` 等字段是否重算 | 明确:**迁移期不允许同时升级解析器** —— 前置校验要求 `parser_v` / `uid_rule_v` **与发起时一致**,否则**拒绝发起**;`confidence` / `resolution` / `site_count` / `level` / `origin` **按解析产物原样上送,服务端不重算** | ✅ |

### E15.3 P2 8 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | `applied` 的**初值/必填**未说明 | §4.1 明确 **`applied BOOLEAN NOT NULL DEFAULT FALSE`** | ✅ |
| P2-2 | `migration_ended_at` 与 `applied` 的**交互**未说明(写在哪) | 明确:写在**保留的 `applied = TRUE` 行上**(不能在会被清空的 `FALSE` 行里)—— 已在 §4.1 写明 | ✅ |
| P2-3 | §4.1 与 D1.5.3 的 `graph_migration_log` **列序不一致** | 两处统一为**同一列序**(自检清单要求"同列同序"):`project_id → target_kind → uid → edge_type → op → vid_old → vid_new → vid_old_from → vid_old_to → vid_new_from → vid_new_to → prev_lifecycle → applied → batch_id → created_at → migration_ended_at` | ✅ |
| P2-4 | `edge_type` **未登记到任何地方** | 已在 §4.1、D1.5.3(审计表 + 边行日志字段行)、回滚语句、D7、自检清单**全部登记** | ✅ |
| P2-5 | §0 把"是否新增决策"塞进"决策数量"括号里 | 拆开表述:"§11 列出 **15 项待评审决策**(v12–v15 均为修补轮,**未新增决策项**)" | ✅ |
| P2-6 | F2.1 与 F2 主表**关系不清**(像"附录") | F2.1 标题已标注"**本小节是对主表步骤 3 的补充说明**,不是独立流程" | ✅ |
| P2-7 | 全序**未显式说明** `deleting` × `migrating` 同时成立 | A5.3 规则 7 表格最后一行显式写明:**同时成立取 `deleting`**,但正常路径**不应出现**(进入条件已拒绝) | ✅ |
| P2-8 | B3 的 `hash-migration/` **权限描述不完整** | B3 补:**协作者、`share token`、公开匿名访问一律 403/404**;**GET(查进度)走元数据接口,不开独立端点**(故 B3 只有 `POST` / `DELETE` 两行) | ✅ |

> **机制性改进(v15)**:①**"日志身份 = 完整主键"原则** —— 日志声明的身份字段必须**全部落列**(v14 漏 `edge_type` 就是"身份写全了、列没写全")②**"必填"归属写清"服务端必需 vs 客户端可选"** —— 避免"要求调用方提供它拿不到的值"③**`applied` 从"行"粒度升级为"批次"粒度**,并给出"部分失败 → 重放优于删除"的裁定 ④**新增 D7 使"动态提取"变得可实现、可验证**(提取规则唯一)⑤**"进入条件先于优先级"** —— 把状态机的"进入"与"裁决"分成两层。

---

## E16 第十六轮评审处置(v16)

> **编号说明**:下表 `P0-n` / `P1-n` **仅在本附录内唯一**;跨附录引用写作 **`E16-P0-n`**。

### E16.1 P0 1 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P0-1 | **`lifecycle` 从未被列为请求体字段**(自 v1 起的隐藏缺口) | **三处存储都有它、唯契约层没有**:D1.3 的 `CREATE TAG` 有 `lifecycle`、§4.2 的 Nebula 顶点属性有它、§3.3 的 `local_node` 有它 → **用户手动标记的 `hidden` 在常规同步与迁移重传中都永远传不上去**(服务端恒为默认值)。补全:①**§5.4 样例**加 `upserts.nodes[].lifecycle` / `upserts.edges[].lifecycle` ②**§5.4 对照表**加两行:**常规期必填** / **迁移期必填** ③**D7.1 常规期清单**加 `lifecycle`(节点 + 边) ④**D1.5.3 的 P0-6 措辞改为**"`lifecycle` 是**常规期就有的字段**,不是迁移期新增" ⑤**自检清单**新增"**`lifecycle` 必须出现在请求体契约**" ⑥**F3 第 6 项**补端到端用例(常规 + 迁移两条路径)。`deleted` 例外:走**软删除路径**,不出现在 `upserts` | ✅ |

### E16.2 P1 5 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P1-1 | §5.4 对照表**有重复行 `upserts.nodes[].uid`** | 已删重复行(第二行的说明"服务端据此自算 `new_vid`"**合并进第一行**);否则 F3 会把该字段算两遍、**掩盖真实缺失** | ✅ |
| P1-2 | F3 第 6 项**未覆盖 v15 新机制** | 新增 **「E15 部分」四用例**:①**带 `edge_type` 的边回滚**(12 类边各跑一遍,三参数全取自日志,回滚后无孤儿边)②**`applied` 批次部分失败**(批内 3 条、2 成功 1 失败 → **3 条全 `FALSE`** → 重放后全 `TRUE`)③**`deleting` × `migrating` 拒绝语义**(`409 migration_in_progress` / `409 project_deleting`)④**`lifecycle` 端到端**(常规 + 迁移两条路径都断言) | ✅ |
| P1-3 | `applied` 的**类型与默认值**只在 §4.1,D1.5.3 只写"布尔" | D1.5.3 已改为 **`applied BOOLEAN NOT NULL DEFAULT FALSE`**(与 §4.1 **逐字一致**) | ✅ |
| P1-4 | F1 的 `migrating` 时长**红线没定死**("接近 7d") | 定死:**黄 = 24h–6d;红 = `> 6d`**(给运维 **1 天缓冲**应对 7d 自动回滚) | ✅ |
| P1-5 | B3 **未说明 `deleting` 期调用 `DELETE hash-migration/` 的行为** | B3 补:**`deleting` 期返回 `409 project_deleting`(不允许取消)** —— 与 A5.3 规则 4"`deleting` 拒绝一切其他操作"一致 | ✅ |

### E16.3 P2 8 项

| # | 项 | 处置 | 状态 |
|---|---|---|---|
| P2-1 | §5.4 的 `deletes.nodes[]` 样例把 `old_vid` 写成"看起来必填" | 样例改为 **`[{ "uid": "uid…" }]`** + 注释"`old_vid` **可选,仅作对账**";与对照表一致 | ✅ |
| P2-2 | D7 把**日志列 `edge_type`** 混进请求体清单 | **D7 拆为两节**:**D7.1 请求体字段清单(F3 唯一提取源)** 与 **D7.2 非请求体字段(日志列 / 本地字段 / Nebula 属性,不参与断言)**;`edge_type` 移入 D7.2,否则 F3 会拿它去 §5.4 找、**必然失败** | ✅ |
| P2-3 | B3 的 GET 表述易被误读为"该端点没有 GET" | 改为"**进度查询由元数据接口 `GET /api/projects/<key>/` 返回 `migration` 字段,不另立端点**"(故 B3 只有 `POST` / `DELETE`) | ✅ |
| P2-4 | `last_graph_rev` 初值两处是否都写了 | 核实:**§4.1 与 F2.1 一致**,无需改动 | ✅ |
| P2-5 | §0 的"v12–v15 均为修补轮"未解释"为何决策数不变" | §0 补:"**这 15 项自 v6 之后未再新增**(v7–v16 均为对已决策的细化/修补)—— 故'决策数量一直不变'是**预期**而非遗漏" | ✅ |
| P2-6 | F1 的 `pending_repair → deleting` 与 A5.3 是否同步 | 核实:**一致**,无需改动 | ✅ |
| P2-7 | §4.1 用 `/` 分隔 4 个端点、D1.5.3 用 `、`,严格字符串比对会**误判** | **统一为顿号 `、`**(两处同列同序、同分隔符) | ✅ |
| P2-8 | B3 与 E15.3 P2-8 的权限表述是否有差异 | 核实:**一致**,无需改动 | ✅ |

### E16.4 三项系统性观察

| # | 观察 | 处置 | 状态 |
|---|---|---|---|
| 4.1 | D7 的"请求体 vs 日志 vs 存储"**三分类不清晰** | 已拆 **D7.1 / D7.2**(见 P2-2),并在 D7.2 显式列出"日志列 / 本地字段 / Nebula 属性"三类,**与 F3 断言范围解耦** | ✅ |
| 4.2 | 自检清单**未覆盖 v15 的核心裁定** | 清单补 **3 条**:①**`lifecycle` 必须出现在请求体契约**(§5.4 样例 + 对照表 + D7.1,常规期即必填)②**"必填"归属必须写清**(服务端必需 vs 客户端必填;**不得要求客户端必填"服务端自己就能算/能查"的字段**)③**"进入条件"与"优先级"分两层**(进入条件先互斥检查 + 全序只在已同时成立时裁决) | ✅ |
| 4.3 | D1.5.3 与 §5.4 **各有一份请求体 schema**,维护点重复 | **§5.4 为唯一权威**;D1.5.3 的该行改为**引用式**("契约的唯一权威在 §5.4,本行只做要点提示"),避免两份漂移 | ✅ |

> **机制性改进(v16)**:①**"三层一致"检查** —— 存储层(D1.3 / §4.2)、客户端层(§3.3)、**契约层(§5.4 / D7.1)** 必须同字段;本轮 `lifecycle` 就是"三层有两层、契约层缺"的典型 ②**D7.1 / D7.2 把"提取源"与"注解"分离**,让 F3 的断言范围**可机械界定** ③**"必填"归属**成为清单项(治"要求调用方提供它拿不到的东西") ④**契约单一权威**(§5.4)+ 其他文档**引用不复制**。

---

## E17 本轮变更处置(v17:身份线分家 + 限流统一;⚠ 范围 = 仅文档)

> **这不是评审轮** —— E1–E16 是"外部评审意见 → 处置";**E17 是本轮主动变更的落地跟踪**(需求来源:用户对"设备号与限流是否重复冲突"的核查结论)。**本轮只改文档,不改任何代码**;代码改动留待**评审通过后另起一轮**。

| # | 变更项 | 目标文件 | 状态 |
|---|---|---|---|
| 1 | **登记先行**:主文升 **v3.31 · 评审稿 v17** + 修订轨迹新增 v17 条 + 本表 + `backend-decisions.md` 记 **B58 / B59** | 主文 §0 / 本附录 / decisions | ✅ |
| 2 | **附录 A 改名 + 隔离约束**:**A3 表**与 **A3.1 全节** `device_id` → `writer_token`;新增**两条身份线对照表**与**三条隔离约束**(命名/存储/校验) | `backend-authority-matrix.md` | ✅ |
| 3 | **主文改名与登记**:§4.1 的写权字段、§5.4 请求体字段、§11 决策 12 标题改名;§4.1 **登记 `DeviceSession.writer_token`** | `backend-design.md` | ✅ |
| 4 | **其余附录改名**:D(D7.1 + 契约引用)、B(B3 注入清单)、F(F1 指标)、C;附录 E **自检清单新增三条**;E1–E16 加**改名对照**行 | D / B / F / C / E | ✅ |
| 5 | **登录错误码矩阵落位**(对外可区分 vs 故意合并;含审计 reason) | `backend-tenancy-security.md`(**已落 B11**;与主文 §8.1 分工,不重复) | ✅ |
| 6 | **限流统一规范落位**(统一响应契约、键策略、实例范围、故障降级 fail-open + 告警、可接受取舍) | 主文 **§8.1** + M9 行(已落) | ✅ |
| 7 | **合并文件同步与收尾断言**:`cat` 流式重拼 `backend-design-merged.md` + 行号目录对齐 + 断言旧名仅剩历史记录 | `backend-design-merged.md` | ⏸ **推迟到评审定稿后**(用户决定:**不做合并、不增加本轮工作量**)。当前合并文件**仍是 v16**,已在文件头标注"**已过期,勿用**";评审请以 **8 份分册**为准 |

**改名对照(供历史记录阅读)**:附录 E1–E16、`backend-decisions.md` 的 B1–B57 中出现的 `device_id`,**即本轮改名后的 `writer_token`**;`dev_id` 与它们**无关**(那是登录限流用的浏览器 Cookie 标识,名字与语义都不变)。

**本轮不动代码(硬边界)**:`core/*.py`、`accounts/`、`authapi/`、`projects/`、`codetools/`、`mysite/settings.py`、`requirements.txt`、`frontend/**` 一律不改;不重建 `dist`;不改 `docker/`、`deploy/`。

**机制性改进(本轮)**:①**"同名不同义"必须显式分家**(命名 / 存储 / 校验三条隔离约束)②**"权威归属"写清**(服务端签发 vs 客户端可清的差异必须落纸)③**"故意不区分"也是契约**(防枚举口径要显式声明,不能留给实现者自由裁量)④**变更也登记**(E17 与评审轮一样可勾选,保证文档任意时刻自洽、可直接送评审)。

---

## E18 本轮变更处置(v18:效率优化入档;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**"效率优化"评估(对标 GitNexus)**。**命题**:方案本质是**"多端同步的确定性图"** —— 效率优化**必须在保证两端 VID 计算一致、离线可合并的前提下**进行。**本轮只改文档、不动代码**;代码另起一轮;**合并版仍冻结**(已标注"已过期勿用")。

**优先级与阶段映射(写死,避免实施时自由裁量)**

| 优先级 | 优化项 | 阶段归属 |
|---|---|---|
| **P0** | Worker 池 + chunk 策略 | **阶段 1**(v19 P0-6 修正) |
| **P0** | 内容寻址 parse cache | **阶段 1**(v19 P0-6 修正) |
| **P1** | Worker 级内存上限 + 熔断 | **阶段 1**(v19 P0-6 修正) |
| **P1** | 派生指标异步预计算 | **阶段 1**(v19 P0-6 修正) |
| P2 | 查询接口归一化 | 阶段 2 扩展期 |
| P2 | 边查询流式 / 分片返回 | 阶段 2 扩展期 |
| P3 | Worker 池预热 | 阶段 2 扩展期 |

**确定性约束(不可突破,先于优化项读)**

1. 解析缓存**绝不缓存 VID**,只缓存 **AST + 符号提取结果**;VID 每次由 `uid` + `Project.hash_algo` **现算**(因为 `hash_algo` 可能迁移,迁移后需全量重算)。
2. 缓存 key 必含 **`hash_algo` + `uid_rule_v` + `parser_v` + chunk 内容哈希**,任一变化即失效(与"解析器升级触发全量重传"对齐)。
3. **Leiden / Process 必须用确定性种子**(如 `Project.key` 的哈希或固定常量),否则两次计算产出不同分区 → `CodeAnalysisDerived.based_on_graph_rev` 校验失败。
4. 缓存命中**直接重放、不启动 Worker**;缓存写入可在 **`storage_port.tx` 之外**完成(不违反既有 `tx` 硬约束)。

**处置表(11 项)**

| # | 变更项 | 层级 | 目标文件 | 状态 |
|---|---|---|---|---|
| 1 | **登记先行**:主文升 **v3.32 · 评审稿 v18** + 修订轨迹 + 本表 + `decisions` 记 **B60 / B61** | — | 主文 §0 / 本附录 / decisions | ✅ |
| 2 | **Worker 池规范**:池大小 **`min(8, max(1, cores-1))`** 起步可配;chunk 按**字节预算**(20MB 起步)不按文件数;**两层分发**;**池懒创建**(温缓存全命中则不加载 Worker 脚本与 tree-sitter 绑定);**熔断**=重试一次 → 隔离该文件 + 审计 | **P0** | 主文 **§3.1.2**(已落) | ✅ |
| 3 | **`parse_cache` store**(IndexedDB 独立 store):key = `hash_algo` + `uid_rule_v` + `parser_v` + chunk 内容哈希;value = Worker 原始解析结果;**命中即重放、跳过 Worker 分发**;**不缓存 VID** | **P0** | 主文 **§3.3**(已落) | ✅ |
| 4 | **Worker 级内存上限**(512MB 起步可配):超限**中止当前文件解析 + 记审计**,而非整体降级到服务端;与项目级阈值**两级并存** | **P1** | 主文 **§3.1.2**(已落) | ✅ |
| 5 | **派生指标异步预计算**:`outbox` 全 `acked` → 触发派生作业(Leiden 聚类 / Process 提取 / 环检测 / 影响面)→ 写 `CodeAnalysisDerived`;**幂等键 `(project_id, graph_rev, derived_kind)`**;复用既有 `Job`;**不得与写者租约冲突**(遵循"两把锁 + 作业优先") | **P1** | 主文 **§5.5.1** + **附录 D9**(已落) | ✅ |
| 6 | **查询归一化 `graph_query(req, ctx)`**:统一出口按 `req` 路由到 Nebula / OpenSearch / PG;所有查询强制带 `ctx.project_id` + DAO 出口断言;前端与未来智能体**同一代码路径** | P2 | 主文读路径 + **附录 D8.1**(已落) | ✅ |
| 7 | **边查询流式 / 分片**:`graph/edges/` 增 **`limit` + `cursor`**(服务端强制夹取上限);一跳/多跳返回**子图摘要**(节点数/边数/类型分布),前端按需展开 | P2 | 主文 + **附录 D8.2**(已落) | ✅ |
| 8 | **Worker 池预热**:worker 启动自检新增;预热时执行既有 **WASM 哈希校验**(与供应链安全不冲突) | P3 | 主文 **§3.1.2**(已落) | ✅ |
| 9 | **对标登记(附录 C)**:GitNexus 四类做法 + **不可借鉴项**(单端无同步 / LadybugDB 绑死选型 / 前端直接 Cypher / 无需 VID 确定性);**来源 = 用户提供,机制与实测数字标"待核实"** | — | **附录 C8**(已落) | ✅ |
| 10 | **连带缺口修复(附录 G)**:补**客户端 store 删除范围**(新增 `parse_cache` + 既有 `local_project` / `local_node` / `local_edge` / `local_pending` / `local_snapshot` / `local_conflict`)—— 附录 G 此前**完全未覆盖**浏览器侧 | — | **附录 G1**(已落) | ✅ |
| 11 | **观测指标(附录 F)+ 自检清单 + 阶段映射**:解析耗时与 **cache 命中率** / Worker 池利用率 / chunk 命中率 / `derived` 作业时长与失败重试 / 边查询返回体积上限;自检清单新增"**本地存储新增 store 必须登记 §3.3 且纳入附录 G 删除范围**";§9 落 P0/P1→阶段 0、P2/P3→扩展期;**收尾断言**术语一致 + 旧名 `device_id` **零新增** | — | **附录 F1 / 自检清单 / 主文 §9**(已落) | ✅ |

**本轮不动代码(硬边界)**:同 E17 —— `core/*.py`、`accounts/`、`authapi/`、`projects/`、`codetools/`、`mysite/settings.py`、`requirements.txt`、`frontend/**` 一律不改;不重建 `dist`;**不重拼 `backend-design-merged.md`**。

**待评审可否决的假设**:①池大小 **8** / chunk **20MB** / Worker 内存 **512MB** 均为**"起步值(可配)"**,非硬承诺 ②`parse_cache` **只用 IndexedDB**(不引入 OPFS 分支)③熔断简化为"**重试一次 → 隔离 + 审计**" ④GitNexus 机制与数字**标"待核实"**,不作为性能承诺 ⑤派生预计算**复用现有 `Job` 系统**,不新建调度器。

**机制性改进(本轮)**:①**"确定性约束"先于"优化项"呈现**(先列不可突破项,再谈收益)②**优化项必须带优先级 + 阶段标签**③**对标数字必须标核实状态**(未核实不进权威口径)④**新增本地存储必须"定义 + 删除范围"成对登记**(治"加了 store 忘了删")。

---

## E19 第二轮专项收口处置(v19:10 P0 + 10 P1 + Redis;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第二轮评审**(命题 = "一致性、容灾、合规删除、迁移回滚、阶段落地五处仍有硬伤;建议**冻结新特性**,做专项收口")。**本轮只改文档、不动代码**;合并版**继续冻结**。

### E19.1 P0 十项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **主键一致性 vs 契约容差** | **分层**:uid/VID 属**主键类** → 必须两端 **bit-exact**;契约测试容差(Jaccard ≥0.95 / 边计数 ≤20%)**只允许用于非主键指标**(confidence / 边集合 / 入口点);新增**共享参考实现 + 测试向量**(uid 规范化/转义) | 主文 §12 + **附录 C2.1**(已落) | ✅ |
| P0-2 | **Nebula RPO 与 outbox 已 ack 即删** | outbox **已 ack 行保留 14 天(起步值,可配)**作为**重放窗口**;明确"**恢复 = 重放 + 校验**";**超窗口 → 只能全量重建,私有项目不可接受**(显式写明);**Nebula RPO 与保留期必须匹配** | **附录 F2 步骤 5** + 主文 §5.5(已落) | ✅ |
| P0-3 | **GDPR 删除范围不全 + 审计去标识化与 B10 冲突** | 附录 G 新增 **「删除影响表清单」**:逐表标注 **删除 / 匿名 / 保留 + 依据**,**覆盖全部新表**(`graph_migration_log`、`CodeAnalysisDerived`、`ProjectWriterLease`、`ProjectSyncState`、**`DeviceSession`(含吊销)**、`edge_type_count`、`Job`、`SearchIndex`、`GraphDeletion/GraphBackup`、`parse_cache`);**归档前先匿名化**;审计中 `file_path` **哈希 / 截断**(对齐 B10) | **附录 G1.1(新表)+ G5** + B10(已落) | ✅ |
| P0-4 | **`applied` 批次部分失败 → 幽灵新 VID** | **两条并用**:①回滚前按 **`batch_id` 做"实际写入探测"**,删除已存在的部分新 VID ②**`FALSE` 行保留到回滚校验完成**再清空(不再"立即清空") | **附录 D1.5.3**(两阶段行,已落) | ✅ |
| P0-5 | **迁移接力 × `lifecycle` 本地依赖** | **预填日志增存 `prev_lifecycle`**;**迁移成功时优先用日志值**,客户端上送**仅作对账** → 换设备 / 清 IndexedDB **不再复活 `hidden` 节点** | 附录 D1.5.3 + 主文 §3.3(已落;**日志增存 `prev_lifecycle`**) | ✅ |
| P0-6 | **阶段 0 与 v18 优化项矛盾**(我上轮引入) | v18 优化项(Worker 池 / `parse_cache` / Worker 内存上限 / 派生预计算)**从阶段 0 移到阶段 1**;阶段 0 恢复"**零风险、与新技术栈解耦**";**E18 保留 11 项勾选记录,只改"阶段归属"列** | 主文 §9 + **附录 E18**(已落:阶段列改「阶段 1」) | ✅ |
| P0-7 | **登录错误码矩阵可枚举账号** | **凭证校验通过前一律 `bad_credentials`**;`account_locked` / `weak_password` / `captcha_required` **只通过登录后或邮件**告知;**审计保留精确 reason** | **附录 B11**(已收窄)+ 主文 §8.1 | ✅ |
| P0-8 | **审计"不限流全落库"可被写爆** | 安全事件**保留完整语义**,但写入路径 **聚合 + 限速 + 磁盘配额 + 异步写**;超阈值 → **聚合计数 + 抽样明细**,**不丢告警** | 附录 B7(已落:容量保护)+ F1 | ✅ |
| P0-9 | **本地 IndexedDB 未按用户隔离** | 本地 store 全部按 **`user_id + project_id`** 命名空间;登出 / 切换账号**强制清理或锁定**;删账号时客户端**必须清理对应命名空间** | 主文 §3.3 + **附录 G1**(已落) | ✅ |
| P0-10 | **`parse_cache` 跨项目共享 vs 随项目联删** | **key 加 `project_id` 命名空间**(**放弃跨项目复用**),使"随项目删除联删"**语义成立**;备选(引用计数 / GC)在被否时采用 | 主文 §3.1.2 / §3.3 + **附录 G1**(已落) | ✅ |

### E19.2 P1 十项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | nGQL 参数化可行性未验证 | **服务端算 VID + 严格 hex 白名单**;**禁止客户端直传 VID**;**边类型枚举白名单** | 附录 B2(已落:白名单规则 4) | ✅ |
| P1-2 | B9.1 uid 字符集与 C++ 现实不匹配 | 补 **uid 规范化 / 转义规则**(覆盖 `~`、`<`、`>`、空格、模板符号),**先定义规则再校验** | 附录 B9.1(已落:规范化/转义) | ✅ |
| P1-3 | 测试期只上 PG 与 outbox / Nebula 脱节 | 定义 **PG 过渡期**:图存储 schema、outbox 消费目标、恢复流程 | 主文 §9(已落:过渡期定义) | ✅ |
| P1-4 | fail-open 对登录 / 注册风险过高 | **登录 / 注册 fail-closed** 到"**本地内存限流 + 账号锁定**";**只有读路径 fail-open** | 主文 §8.1(已落) | ✅ |
| P1-5 | 多标签页 `page_id` 刷新后旧租约无法主动释放 | 允许**同 session 新页面列出并撤销旧页面租约**(不必等 90s TTL);或 `page_id` 改为**服务端可控的会话子标识** | **附录 A3.1**(已落:第 8 条) | ✅ |
| P1-6 | 派生触发条件与 `index_lag == 0` 不等价 | 派生触发改为 **Nebula 目标全部 `acked`**(**不引用 `index_lag`**,因 `dead` 行被排除水位) | 主文 §5.5.1(已落:与 `index_lag` 解耦) + 附录 D9 | ✅ |
| P1-7 | `409 contract_incompatible` 旧前端兜底 | **静态资源版本化 + 引导页兜底**(旧前端不白屏) | 主文 §7(已落:引导页兜底) | ✅ |
| P1-8 | 恢复流程对服务端持源码项目不适用 | **公开 / 协作 / 超大项目恢复走服务端重解析**(不依赖客户端重传) | 附录 F2(已落:服务端持源码→重解析) | ✅ |
| P1-9 | 审计归档与删除联动缺失 | **归档前先匿名化**(与 P0-3 同源) | 附录 G5(已落:归档前匿名化) | ✅ |
| P1-10 | 文档过重、重复定义仍多 | **单一权威源**:§5.4 ↔ D7.1、§4.1 ↔ D1.5.3、A5.3 ↔ F1 **去重**,其余**强制引用** | 主文 §5.4 + 附录 D7/D1.5.3 + F1(已落:权威源标注) | ✅ |

### E19.3 Redis 落位(合并自上一轮计划)

| # | 项 | 口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| R1 | **限流后端定稿为 Redis** | §8.1 去掉"集中式"泛称:键设计(IP 主 + `dev_id` 二级 / 全键 TTL / 键数上限)、**fork 后丢弃继承连接**、**Redis 非权威边界** | 主文 §8.1(已落:定稿 Redis + 非权威边界) | ✅ |
| R2 | **新增 §8.2 共享缓存与频道层** | `CACHES` + `CHANNEL_LAYERS` 切 Redis(**频道层是功能性修复**:现状 InMemory → 多进程广播失效);**三类用途 namespace 隔离**;按用途区分**可驱逐 / 不可驱逐**;**不持久化**;**逐项降级**(认证链路 fail-closed、读路径 fail-open、缓存退查库、频道层失效 → 重连 / 重试) | 主文 §8.2(已落:三用途/淘汰/降级) | ✅ |
| R3 | **指标 + 功能性告警 + 自检清单** | F1 加 Redis 可用性 / 内存 / 连接数 / 命中率 / 延迟 / 驱逐 / 被拒;**"频道层不可用"列为功能性告警**;自检清单新增"**新增常驻服务必须登记 用途 / namespace / 淘汰与持久化 / 故障降级 / 监控项**" | 附录 F1(已落 5 行)+ 自检清单(已落 2 条) | ✅ |

**P2(本轮仅登记为待办,不实质改动)**:①旧名 → 新名**全局映射与弃用警告** ②**自检清单 + F3 瘦身**(只保留高价值黑名单)③**浏览器解析器 TS 重写单独立项**(先做 C/Python 语法级原型,验证 uid 一致性)④合并版过期标记(**已完成**)。

**待评审可否决的假设(7 条)**:①outbox 已 ack 保留 **14 天可配** ②回滚**"探测 + `FALSE` 保留"两并用** ③`parse_cache` key **加 `project_id`**(放弃跨项目复用)④登录 / 注册 **fail-closed 到本地限流** ⑤派生触发 = **Nebula 目标全 `acked`** ⑥Redis **同实例不同 namespace、不持久化** ⑦P2 不实质改动。

**本轮不动代码(硬边界)**:同 E17 / E18 —— `core/`、`accounts/`、`authapi/`、`projects/`、`codetools/`、`mysite/settings.py`、`requirements.txt`、`frontend/**` 一律不改;不重建 `dist`;**不重拼合并版**。

**机制性改进(本轮)**:①**主键类指标与统计类指标分层**(容差不得覆盖主键)②**「删除影响表清单」成为固定交付物**(新增表必须登记删除口径)③**容灾明确"重放窗口"**(保留期与 RPO 必须匹配)④**回滚以"实际写入探测"为准**(不再只信标记)⑤**阶段归属与"零风险"定义绑定**(避免把重活塞进阶段 0)。

---

## E20 v19 自洽性专项处置(v20:10 P0 + 13 P1 + F3 拆分;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第三轮评审**(命题 = "v19 把上一轮硬伤基本收口,但**收口过程引入了新的自相矛盾**;建议**不要再加条款**,做一次 **v19 自洽性专项**"). **本轮只改文档、不动代码**;合并版**继续冻结**。
> **本轮范围(用户裁定)**:10 P0 + **13** 条成立的 P1;**P2 只做 F3 拆分与改名映射配置**(其余 P2 留待下一轮)。

### E20.1 P0 十项(全部实测确认成立)

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **D9 触发条件与 §5.5.1 矛盾 + `dead` 永久冻结派生** | 统一为"**该项目 `nebula` target 行无 `pending`/`sent`/`failed`** → 图已落库追平";**`dead` 不阻塞**(`dead` 是终态,不是"没写完");**禁止"即 `index_lag == 0`"**;两处**逐字一致** | 主文 §5.5.1 + 附录 D9 | ✅ |
| P0-2 | **Redis namespace 隔离技术上不成立** | ⚠ `maxmemory-policy` 是**实例级**、`SELECT n` 分库**隔离不了策略** → **频道层独立实例/进程 = 硬约束**(不再"待评审可否决");给出实例 A/B 职责、`ch:` 侧 `noeviction`、**本机 ≤2GB 内存预算下限(64–128MB)** | 主文 §8.2 | ✅ |
| P0-3 | **不持久化 + 本地内存降级 = 暴力破解窗口** | **实例 A 开 AOF `everysec`** + **认证链路 IP 桶落库兜底**(带写入频控) + 实例 B 不持久化 + fail-closed 语义不变 | 主文 §8.2 | ✅ |
| P0-4 | **幽灵数据探测方法未定义** | 定稿算法:**清单来自日志(`batch_id` + `applied=FALSE`)** → **批量探测(≤500 VID/批,5s 超时)** → **仅删 `vid_op_rev == 本批 rev` 的行**(误删保护) → **删后复查**(TOCTOU 兜底) → **超回滚窗口即中止转人工** | 附录 D1.5.3(新增行) | ✅ |
| P0-5 | **outbox 14 天的存储与删除缺口** | ①**删项目时无条件清空全部行**(含窗口内),不等 14 天;②**F1 增表大小/行数/磁盘指标**;③评估 payload 压缩/仅存引用;④F2 步骤 5 补"已删项目不重放" | 主文 §5.5 + 附录 F1/F2 + 附录 G1.1 | ✅ |
| P0-6 | **B10 与 G1.1 的 `file_path` 口径冲突** | 权威源 = **B10**:"**实时表可存明文(访问受限)→ 归档前统一匿名化**";给出**可执行匿名化规则**(`file_path` = `sha256[:8]` + 扩展名,`uid` = `sha256[:8]`,`signature` 截断+哈希;**不可逆**);G1.1 改为**引用** | 附录 B10 + 附录 G1.1 | ✅ |
| P0-7 | **D6.2 空值 vs §7 合约降级"无 key"** | 区分**两种缺失**:**契约降级 → JSON 无该 key**(前端按"功能不可用")**业务空值 → 显式 `null`/空数组**(前端按"暂无");判定规则写进后端两处 + 前端契约 | 附录 D6.2 + 主文 §7 + `frontend-contract.md` | ✅ |
| P0-8 | **同 session 多标签页仲裁未定义** | 定稿:**服务端分配单调 `page_seq`** → **最大 `page_seq` 胜**(最后活跃者胜)→ 更小者**自动失效** → **旧页撤新页请求被拒(`409 stale_writer`)** → **不存在互撤死循环**;旧页降级只读 + `stale` 徽章;`page_seq` 作用域(每项目+每 session)与 `writer_epoch`/`lease_seq` **不得混用** | 附录 A3.1 ⑧ | ✅ |
| P0-9 | **§4.4 新 key 格式与主文 §5.4 示例不一致** | 示例占位改 **`proj_<32位小写hex>`**;端点行标注 **`<key>` 必须匹配 `^proj_[0-9a-f]{32}$`**(旧 key 按 §4.4.4 ① 宽放行) | 主文 §5.4 | ✅ |
| P0-10 | **E4"可进入阶段 0"措辞自相矛盾** | **拆为两门**:**可评审通过**(文档层面)/ **可进入阶段 0**(= 上一层 + **F3 代码级收口完成**);F3 7 项由"实施时先做"改为**前置条件**;**当前状态 = ⬜ 未达成** | 附录 E4 + 附录 E 顶部纪律 | ✅ |

### E20.2 P1 十三条(1 条经核查不成立,降级登记)

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | D9 触发"全部 acked"与 dead 阻塞 | 与 **P0-1 同源**,合并处置 | 主文 §5.5.1 / 附录 D9 | ✅ |
| P1-2 | **D2 小节编号跳跃**(D2.2 → D2.4 → D2.3) | **物理调整顺序**为 D2.3 → D2.4;**编号与内容一一对应不变**,既有交叉引用**仍有效**;加"编号顺序说明" | 附录 D2 | ✅ |
| P1-3 | 主文 §9 残留编辑痕迹(删除线 + 保留编号) | 去掉 `~~…~~`,保留编号 4,改述为"**已移出阶段 0(项清单见下)…在阶段 1 实施**" | 主文 §9 | ✅ |
| P1-4 | **B11 表格结构混乱** | **拆两张表**:表 A(真实状态码,只含真返回的码)/ 表 B(防枚举清单,含内部 `reason`);把被截断的两行归位;说明"HTTP 列不得写'不返回'" | 附录 B11 | ✅ |
| P1-5 | P0-8 审计抽样可被利用 | **安全事件明细不抽样** → 超配额时进"**高保真模式**"(安全明细独立通道、配额单列),**只对常规操作抽样**;抽样比例与模式**记审计 + 进 F1 告警** | 附录 B7 | ✅ |
| P1-6 | F2 步骤 5 与"删项目"交互未定义 | 补 ⑤:**已删项目 → outbox 全部行无条件清空、不重放**(重放 = 复活),记审计;以 G1.1 为准 | 附录 F2 | ✅ |
| P1-7 | §8.2 fork 后连接处理不具体 | 补**可执行约定**:子进程**不得复用父进程 `connection_pool`**,入口处重建(与 `child.discard_inherited_connections()` 同源),或用**每进程独立 `ConnectionPool`** | 主文 §8.2 | ✅ |
| P1-8 | `captcha_required` 泄露账号存在性 | **实测 `core/captcha.py`**:失败计数键 = **`IP \| dev_id`**,**与账号无关** → "可对外"成立;⚠ 但 `dev_id` 客户端可控 → 定稿**必须以 IP 为主键(IP 桶先满先拦),`dev_id` 只作二级叠加** | 附录 B11 | ✅ |
| P1-9 | **D7.1 完整性由谁保证** | 明确**两个角色**:契约权威 = **§5.4**;提取源 = **D7.1**(**人工维护,禁止脚本自动生成**);**双向断言** + 新增字段**必须同批登记**;进自检清单 | 附录 D7 + 附录 F3 | ✅ |
| P1-10 | P1-6 派生触发与 dead 行 | 与 **P0-1 同源** | 主文 §5.5.1 | ✅ |
| P1-11 | G1.1 缺 `edge_type_count` 处置 | ⚠ **核查后不成立**:`G1.1` **已写**"**删除**(按项目)"(与 `Job` 合并在同一行)→ 降级为**下一轮 P2「拆行」**,本轮不改。**降级理由(v21 P2-2 补写,防止下一轮重提)**:**①已写处置** —— `backend-data-lifecycle.md` 的 G1.1 行 `\| **\`edge_type_count\`** / \`Job\` \| 关系库 \| **删除**(按项目) \| 统计与队列 \|` 已明确标注"删除(按项目)",**不缺"删除 / 匿名 / 保留"三要素**;**②唯一缺陷是"与 `Job` 合并在同一行"**,属**可读性**问题而非**合规缺口**,不影响"逐表 0 行残留"的验收;**③故降级为 P2「拆行」**,**不是**"暂时查不出处置方式" —— 下一轮**不应**按合规缺口重提,如需改动仅为拆表 | 附录 G1.1 | ⬇ 降级(P2 拆行) |
| P1-12 | 归档匿名化缺验证步骤 | 补**五步验证**:计数断言 → 规则断言(抽样+正则,**≥1% 且 ≥100 行**)→ 不可逆断言 → **失败即删产物 + 作业 `error` + 告警** → 审计留痕;**绝不允许"匿名化失败但归档成功"** | 附录 G5 | ✅ |
| P1-13 | `max_length 64→48` 与"宽放行"不一致 | **两个层级分工**:列约束管"长度"、应用层宽放行管"形态";**前提已实测**(存量 9 项目、最长 **34** 字符)→ 可收窄;**再收窄必须重新实测** | 主文 §4.4.3(b) + §4.4.5 | ✅ |
| P1-14 | `parse_cache` 引用计数/GC 缺决策判据 | 定稿**默认不做**(key 含 `project_id` → 随项目联删);**仅当三条同时满足**才评估(跨项目命中率 ≥30% / 占用 ≥80% 配额 / 收益可量化);**禁止为"可能的复用"提前引入 GC** | 主文 §3.1.2 | ✅ |

### E20.3 P2(本轮只做两条)

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P2-4 | **F3 巨型脚本不可维护** | 第 7 项**拆为 4 个可独立运行的检查集**:7.1 契约/字段一致性 · 7.2 交叉登记 · 7.3 自检清单与计数 · 7.4 改名映射;每集**自带扫描范围声明**、可独立运行、**豁免可按集+按规则+附原因**;实现时落为**配置文件**(不新建文档) | 附录 F3 新增「7.x」 | ✅ |
| P2-5 | `device_id → writer_token` 无全局映射 | 在 **F3「7.4」内嵌映射表**(旧名 → 新名 + 历史行处理 + 搜索提示);**不新建文档** | 附录 F3 7.4 | ✅ |
| P2-1/2/3 | D1.5.3 表格拆子表 / E17–E19 列统一 / §0–§11 状态列统一 | **本轮不做**,留待下一轮(已登记) | — | ⏳ 下一轮 |

**机制性改进(本轮)**:①**"待评审"不得藏强制项** —— 强制约束(独立实例、持久化)必须直接写成硬约束,把"待评审可否决"留给**取舍**,不留给**前提**;②**同源项一律合并处置**(P1-1/P1-10 并入 P0-1),避免"同一根因改三处、三处各写一套";③**巨型检查脚本必须按检查类型拆分**,否则它自己成为下一个漂移源;④**"缺失"必须分两种**(契约降级 / 业务空值),否则前端无法编码;⑤**门槛措辞必须可判定** —— 凡是"✅ 但…"的写法都视为**未达成**。

---

## E21 v20 落地可行性核查处置(v21:5 P0 + 10 P1 + 6 P2;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第四轮评审**(命题 = "v20 把 v19 的自相矛盾基本收口,但**收口动作本身又引入 3 个新的跨文档冲突**,并暴露出几个**'写在文档里、但在实际约束下无法执行'**的规则;建议**不要继续加条款**,做一次 **v20 落地可行性核查**"). **本轮只改文档、不动代码**;合并版**继续冻结**。
> **本轮共性病因(机制性改进的直接来源)**:**规则缺"可执行三件套"** —— **显式值 / 优先级 / 责任人·验收点**。5 个 P0 全部命中:①②是"**规则在实现层不可执行**"(被更底层的约束挡住),③④是"**规则缺可执行值**",⑤是"**资源预算未评估**"。

### E21.1 P0 五项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **`max_length 64→48` 与"旧 key 宽放行"在 DB 层冲突** | **优先级写死**:**DB 列约束(48) > 应用层宽放行**;**§4.4.4 ① 删除"长度"豁免**,宽放行**仅限"≤48 字符的旧 key"**;**导入 / 外部同步路径必须显式拒绝 >48 的 key + 可读错误**(⚠ 实测 `.webapkg` 导入**尚未实现** → 对**未来任何"以既有 key 建项目"的入口**生效);"存量最长 34"作为**实测前提随结论一起写** | 主文 §4.4.3(b) + §4.4.4 ① + §4.4.5 第 5 条 | ✅ |
| P0-2 | **URL 层"严格校验"与"旧 key 可访问"在路由层冲突** | **职责分层**:**URL 层只做基本形态约束(长度 ≤48 + 字符白名单)且必须接受旧 key**(实测 `projects/urls.py` **33 条** `<str:project_key>`);**严格形态校验下沉到视图层**,`^proj_[0-9a-f]{32}$` **只作分支判定、不用于拒绝**;**404 收窄为"项目不存在 / 无权"** | 主文 §4.4.3(b) + §4.4.6 第 6 项 | ✅ |
| P0-3 | **B7 "高保真模式"要求独立表/文件但未登记 G1.1** | **G1.1 增加一行「安全事件高保真明细」**(首选表 `AuditSecurityDetail`),标注 **删除 / 匿名 / 保留 + 依据**;**若落独立文件**必须写明**物理位置 / 删除方式 / 是否进 F2 备份 / F1 监控项**;B7 侧加**指针**;自检清单补示例 | 附录 G1.1 + 附录 B7 + 主文自检清单 | ✅ |
| P0-4 | **"回滚窗口"从未定义(与 `migrating` 红线混用)** | **两个概念分开**:`migrating` 红线 **6 天** = 迁移未完成告警上限;**回滚窗口 = 24 小时(起步值,可配 `AppSetting.migration_rollback_window_h`)** = 单次回滚操作上限;**最大可回滚批次数**公式(500 VID/批、0.5–2s/批 → 24h 可回滚数万批);超窗 → **中止 + `inconsistent` + 人工**;F1 增**回滚时长**指标(黄 >30min / 红 > 窗口) | 附录 D1.5.3 + 附录 F1 | ✅ |
| P0-5 | **AOF + 限流高基数键的写放大未评估** | **关闭 AOF**(`appendonly no`):认证链路持久化已由 **DB 兜底**(IP 桶落库 + 账号锁定)覆盖 → AOF **冗余**;`rl:` 是**高基数短 TTL 键** → 持续 `SET`/`INCR`/`DEL` 会**持续增长 + 频繁重写**,**2GB 预算下得不偿失**;写明"**重启窗口由 DB 兜底**";**"将来再开"三样前置条件**(rewrite 参数 + F1 监控 + 内存预算重评估);F1 增**实例 A/B 内存占用** | 主文 §8.2 ② + 附录 F1 | ✅ |

### E21.2 P1 十项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | 回滚窗口未定义 | 与 **P0-4 同源**,合并处置 | 附录 D1.5.3 | ✅ |
| P1-2 | 高保真"独立文件"物理位置与删除方式未定义 | 并入 **P0-3**:四项要求(物理位置 / 删除方式 / 备份范围 / 监控项)写进 G1.1 行 | 附录 G1.1 | ✅ |
| P1-3 | **归档匿名化 `sha256[:8]` 在千万级行下必碰撞** | 提到 **`sha256[:16]`(64 bit)**;新增 **`anon_rule_v = 2`**;**显式声明"不保证唯一性、不得作身份标识"**;G5 增**位宽断言**(短于 16 位即失败) | 附录 B10 + 附录 G5 | ✅ |
| P1-4 | **F3 检查器构建未定义** | **列入 §9 阶段 0 第 5 条独立任务**:交付物(7.1–7.4 + 映射配置)、责任(后端文档维护者 + CI)、顺序(7.1/7.2 → 7.3 → 7.4)、**验收 = E4「可进入阶段 0」** | 主文 §9 + 附录 E4 | ✅ |
| P1-5 | **D7.1 维护职责不明确** | 定责:**PR 提交者同批改 §5.4 + D7.1**;**CI 7.1 集双向断言并阻断**;文档维护者只维护规则;**禁止脚本自动生成**;并声明"服务端分配字段(如 `page_seq`)不进 D7.1 但必须进 §4.1" | 附录 D7 + 附录 F3 7.1 | ✅ |
| P1-6 | **`page_seq` 未同步进 §4.1** | **§4.1 `DeviceSession` 补 `page_seq`** + A3.1 补**落库位置** + 自检清单补示例(⚠ 实测代码中 `DeviceSession` **尚未实现**,当前仅为设计表) | 主文 §4.1 + 附录 A3.1 + 自检清单 | ✅ |
| P1-7 | F1 的 outbox 阈值在业务高峰可能误报 | 改**分层**:绝对阈值 + **增速** + **单项目窗口内行数**;明确"**黄可由高峰正常触发,红才视为异常**" | 附录 F1 | ✅ |
| P1-8 | F2 缺"怎么判断项目已被删除"的判据 | 新增 **5b 步**:`SELECT key, graph_sync_status FROM project WHERE key = $key` → **无行 / `deleting`** → 不重放 + 清空 + 审计;另有 `JOIN` 兜底与不一致告警;**写入运维手册恢复章节** | 附录 F2 | ✅ |
| P1-9 | B11 的 `captcha` 维度需硬约束 | 升为**硬约束**:认证链路失败计数**必须以 `IP` 为主键**,`dev_id` **仅二级叠加**;**CI 断言键形态**(`ip\|dev_id`,`none\|dev_id` 非法);自检清单加条目 | 附录 B11 + 主文自检清单 | ✅ |
| P1-10 | `parse_cache` 埋点未写"何时/谁/看哪个面板" | 定稿:**阶段 1 起上线**、**责任人 = 前端作者**、**看 F1「跨项目命中率」**(本轮已加指标)、口径与复核周期写死 | 主文 §3.1.2 + 附录 F1 | ✅ |

### E21.3 P2 六项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P2-1 | `DeviceSession` 未同步 `page_seq` | 与 **P1-6 同源**,合并处置 | 主文 §4.1 | ✅ |
| P2-2 | E20.2 的 P1-11 降级理由要写清 | **补写降级理由**:①G1.1 **已写**"删除(按项目)"、三要素不缺 ②唯一缺陷是"与 `Job` 同行"属**可读性**而非**合规缺口** ③故降级为 P2「拆行」——**下一轮不应按合规缺口重提** | 附录 E20.2 | ✅ |
| P2-3 | 附录 E 顶部速查表门槛判定仍是一段文字 | **拆为两行表格**(与 E4 逐字一致);速查表补 **E20/E21** 计数 | 附录 E 顶部 | ✅ |
| P2-4 | F3 7.4 的改名映射表语义不匹配 | 加**定位说明**:它**本质是跨附录的文档元数据**,同时是检查器**配置文件**;改名必须同步更新本表 | 附录 F3 7.4 | ✅ |
| P2-5 | §4.4.5 第 5 条未写"何时生效" | 补:**随下一轮代码改动一起生效**(与新生成规则 / 目录规则 B 同批 migration);**本轮仅文档** | 主文 §4.4.5 | ✅ |
| P2-6 | §0 的 v20 轨迹未逐条列 P1 十三条 | **补齐 13 条逐条**(与 v19 写法一致,便于追溯) | 主文 §0 | ✅ |

**机制性改进(本轮)**:①**"规则可执行"才算关闭** —— 每条必须带 **显式值 / 优先级 / 责任人·验收点**;②**凡可能冲突处必须写优先级**(DB 列 vs 应用层 / URL 层 vs 视图层 / `migrating` 红线 vs 回滚窗口);③**"实测前提"必须随结论一起写**(如"存量最长 34""`.webapkg` 未实现""`DeviceSession` 未实现"),否则前提失效时无人重审;④**检查器的构建必须列为任务**,否则"前置条件"永远无人执行。

---

## E22 v21 交叉一致性核查处置(v22:5 P0 + 13 P1 + 4 P2;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第五轮评审**(命题 = "v21 把 5 个 P0 都落了口径,但**收口动作本身又留下 3 处跨文档冲突、2 处'规则缺可执行细节'、1 处自我循环**";建议做一次 **v21 交叉一致性核查**). **本轮只改文档、不动代码**;合并版**继续冻结**。
> **本轮共性病因**:**"修一处漏一处 / 只修一半 / 自我循环"** —— P0-1 是该老毛病**第 7 次复发**(改 §4.4.3(b) 漏 §5.4),P0-2 是"只加 G1.1 不加 §4.1",P0-3 是"门禁与任务互为前提"。

### E22.1 P0 五项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **§5.4 与 §4.4.3(b) 的 key 形态约束矛盾(v21 修一处漏一处)** | §5.4 端点行改为**与 §4.4.3(b) 逐字一致**:"URL 层只做**基本形态约束**(≤48 + 字符白名单,必须接受旧 key);严格形态 `^proj_[0-9a-f]{32}$` 在**视图层判定、不用于拒绝**";把"必须匹配"从**规则**降为**分支判定**语义 | 主文 §5.4 | ✅ |
| P0-2 | **`AuditSecurityDetail` 未登记主文 §4.1(v21 只加了一半)** | **§4.1 补一行**:字段(`id`/`audit_id`/`action`/`ip_hash`/`user_id`/`detail`(JSONB)/`created_at`/`anonymized`/`anon_rule_v`)、索引 `(action, created_at)`、**与 `AuditLog` = 同库独立表(主表/子表,`audit_id` 可选关联)**、保留期(2 年 → 归档前匿名化)、删除(随项目/账号)、**独立文件形态(JSONL / 按天轮转 / ≤100MB / 单写者 `O_APPEND`)**;**同步 F2 备份范围** | 主文 §4.1 + 附录 G1.1 | ✅ |
| P0-3 | **阶段 0 第 5 条与 E4 形成循环依赖** | **拆两道门**(取代单一"可进入阶段 0"):**门 1 可评审通过**(文档)→ **门 2 检查器就绪**(F3 7.1–7.4 跑通 + 改名映射配置落地)→ **两门都过才开工**;**第 5 条从阶段 0 任务清单移出**,改为**门 2 的交付物**;E4 与顶部速查表**同步拆三门** | 主文 §9 + 附录 E4 / 顶部速查 | ✅ |
| P0-4 | **`max_length 48` 的旁路入口与 >48 旧 key 处置缺失** | ①**旁路封死**:**直接 SQL 迁移 / 手工 `INSERT` / 第三方同步一律禁止**(旁路出现 → 记审计 + 视为配置违规);②**>48 旧 key**:不允许新增/导入;**若已存在 → 只读可访问、不可再保存**(读取不触发列约束);③**URL 层 >48 → 404**(超长串不构成合法路径),并声明**与"旧 key 宽放行"不冲突**(宽放行只针对 ≤48) | 主文 §4.4.4 ① + §4.4.6 第 6 项 | ✅ |
| P0-5 | **DB 兜底只给了原则(表结构/频控/切换/与 B7 关系四项全缺)** | **新建 `AuthRateCounter`**:唯一键 `(ip, bucket, window_start)`、索引 `(updated_at)`、**保留 2 个窗口**、**频控 = 同 IP ≤12 次/分钟写(超限进程内累加、60s 批量刷盘)**、**切换 = 不双写 / 即切即生效 / 不补写历史 / 恢复幂等合并(取 max)**、**与 B7 解耦(只计数,不进高保真通道、不占审计配额)** | 主文 §8.2 ② | ✅ ⚠ **v23 P1 已改写**(用户裁定 = **"Redis 侧计数 + DB 仅最终兜底"**):计数权威 / 固定窗口 / 全局 12 次口径 / 切换判据 / 备份范围**全部重写**,**实施以 v23 为准**(见 **E23.2** 与主文 §8.2 ②)|

### E22.2 P1 十三项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | **B10 日志脱敏 `[:8]` 与归档 `[:16]` 位宽不一致** | **统一为 `[:16]`**;立唯一总规则「**敏感字段哈希前缀 = 16 hex(64 bit)**」,运维日志 / 归档 / 前端上报**三处一致** | 附录 B10 | ✅ |
| P1-2 | **`anon_rule_v` 枚举缺失** | 补 **`v1` = 8 hex(历史,已废止)/ `v2` = 16 hex(现行)**;**检出 v1 即失败**;历史 v1 归档**不回溯重算**,分析按 `anon_rule_v` 分组 | 附录 B10 + G5 | ✅ |
| P1-3 | **F1 回滚时长黄区过宽** | 细分 **30min / 2h / 6h / 24h(窗口)** 四档 | 附录 F1 | ✅ |
| P1-4 | **`AppSetting` 无集中清单** | **主文新增 §4.1.1「`AppSetting` 配置项清单」**(10 项,含 `migration_rollback_window_h` / `auth_rate_db_write_per_min` / `anon_rule_v` 等)+ 维护规则(F3 7.2 可扩展交叉断言) | 主文 §4.1.1 | ✅ |
| P1-5 | **`page_seq` 序列实现未定义** | **`(project_id, session_id)` 分组 `MAX+1` + 同行锁**;**表 append-only**(登出/改密只写 `revoked_at`);清理**只整批删已吊销且过期**且**删前确认无活跃租约**;**若允许物理删除 → 必须改独立序列表**(走评审) | 附录 A3.1 ⑧ + 主文 §4.1 | ✅ ⚠ **该实现已在 v23 P0-1 撤回**(`MAX+1` + 行锁在**首次创建时无行可锁** → 两个标签页同时打开都拿到 `page_seq=1`)→ **改用独立序列表 `PageSequence`**,**实施以 v23 为准**(见 **E23.1** 与附录 A3.1 ⑥)|
| P1-6 | **`AuditSecurityDetail` 无 schema** | 并入 P0-2(字段/索引/关系 + 文件形态四要素) | 主文 §4.1 + 附录 G1.1 | ✅ |
| P1-7 | **`parse_cache` 跨项目命中率埋点可能违反 GDPR** | **"跨项目命中"仅限同一 `user_id` 内、绝不跨用户**;**只上报聚合数值**(命中率/chunk 数),**不上报 chunk 内容、路径、项目标识**;删除按附录 G 联动;**放宽到跨用户必须重做隐私评估并走评审** | 主文 §3.1.2 + 附录 F1 | ✅ |
| P1-8 | **"三件套"自身未落实(责任人缺失)** | **逐条补责任人**:导入拒绝 / URL 分层 = **后端 API 维护者**;回滚窗口运维 = **运维 owner**;DB 兜底 = **后端 API 维护者 + DBA**;检查器构建 = **文档维护者 + CI**;URL 分层并补**验收用例三条**(越权 / 旧 key 可访问 / 导入 >48 拒绝) | 主文 §4.4.3(b) / §8.2 / 附录 D1.5.3 / 附录 E4 | ✅ |
| P1-9 | **`captcha` 维度 CI 断言不可实现** | **分层断言**:**运行时断言为主(A 类,阻断)**(键形如 `ip\|dev_id` 且 IP 段非空);**静态检查为辅(启发式)**;**判不出降级 code review(C 类)**;**禁止因此删除断言** | 附录 B11 + 附录 F3 7.x | ✅ |
| P1-10 | **F1 outbox 三指标逻辑关系未定义** | **优先级 = 单项目行数 → 增速 → 绝对值**;**任一红即红、任一黄即黄**;告警文案**必须给出"建议先查哪一项"** | 附录 F1 | ✅ |
| P1-11 | **"0.5–2s/批"是估算而非实测** | 明确标注**估算值**,并写"**实施前必须实测并回填**"(与 v21 自定原则一致) | 附录 D1.5.3 | ✅ |
| P1-12 | **`migrating` 6 天红线 vs 回滚窗口 24h 无缓冲** | 补**回滚前置检查**:`(6 天 − 已 migrating 时长) ≥ 回滚窗口` 才允许自动回滚;否则**先告警 + 人工决策**(可显式批准超额,记审计);责任人 = 运维 owner | 附录 D1.5.3 + 附录 F1 | ✅ |
| P1-13 | **F3 检查器构建缺 fallback** | F3「7.x」新增**可实现性分级**:**A 自动断言(阻断)/ B 自动打印(辅助)/ C code review(必须签字)**;**C 类不得静默删除**;CI 输出**必须打印 A/B/C 条数** | 附录 F3 7.x | ✅ ⚠ **v23 P2-2:A/B/C 的定义与清单以附录 F3「7.x」为唯一权威,本行只引用** |

### E22.3 P2 四项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P2-1 | v21 §0 轨迹的 P1 条目与 E21.2 无映射 | **加编号 `P1-1…P1-10`**,与 E21.2 逐条对齐 | 主文 §0 | ✅ |
| P2-2 | §4.1 `DeviceSession` 行"假设 A 待评审"却补了字段 | 补说明:"**该表与其字段(含 `page_seq`)随"假设 A"一并待评审;此处仅登记,不建表**";假设未通过前,序列实现与清理策略**均不生效** | 主文 §4.1 | ✅ |
| P2-3 | F3 7.4 元数据可发现性 | **主文 §4.1.1 加"相关指针"**:改名映射见 F3 7.4(跨附录文档元数据);并汇总"表→§4.1 / 字段→§5.4+D7.1 / 端点→B3 / 删除口径→G1.1" | 主文 §4.1.1 | ✅ |
| P2-4 | `AuditSecurityDetail` 独立文件是否进 F2 备份 | **进**(与 PG 同 RPO);**表与文件两种落位都进**;归档匿名化后随 `AuditLog` 一并归档 | 附录 G1.1 | ✅ |

**机制性改进(本轮)**:①**"三件套"升级为对新增规则的验收清单** —— 每条新增规则必须逐条核对**显式值 / 优先级 / 责任人·验收点**;②**"修一处必须同步全部引用处"** —— P0-1 为该老毛病**第 7 次复发**,已在自检清单加"**同一规则不得存在第二种表述**"强制项;③**门禁不得是某个门禁的子项**(消除自我循环);④**"做不出来的断言"必须有 fallback 等级**,不允许静默删除。

---

## E23 v22 门禁前最后核查处置(v23:3 P0 + 10 P1 + 5 P2;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第六轮评审**(命题:"v22 把 v21 的 5 个 P0 都落了口径,但**收口动作本身又留下 3 处跨文档 / 新表未联动缺口、2 处'新机制的可执行细节缺失'、1 处并发正确性缺陷**";建议**不要再加条款**,做一次「**v22 门禁前最后核查**」,只做四件事)。**本轮只改文档、不动代码**;合并版**继续冻结**。
> **本轮共性病因**:①**"新表漏登记"这一老毛病在同一轮内复发**(v22 在主文 §8.2 建 `AuthRateCounter` 却没登记 G1.1);②**只写稳态路径、漏掉边界分支**(`MAX+1` 在"首次创建"时**无行可锁**);③**收口时只替换了一行、留下旧行**(门槛表)。

### E23.1 P0 三项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **`page_seq` 的"`MAX+1` + 行锁"在首次创建时存在并发缺陷**(真正的正确性问题) | **改用独立序列表 `PageSequence`**(主键 `(project_id, session_id)`):分配 = **单条 `INSERT … ON CONFLICT … DO UPDATE SET next_seq = next_seq + 1 RETURNING next_seq`**(语句内原子,无锁等待 / 无 `MAX` 扫描 / 无空集歧义);**撤销 v22 的"`DeviceSession` append-only"前提**;**清理前置条件(写死)** = 该 `(project_id, session_id)` **已无 `DeviceSession` 行且无活跃租约**;`DeviceSession.page_seq` 退化为"**已分配值快照**";**登记 §4.1 + 附录 G1.1**;⚠ **随"假设 A"一并待评审** | 附录 A3.1 ⑥ + 主文 §4.1 + 附录 G1.1 | ✅ ⚠ **v24 已补四项**(见 **E24**):主键改 **`(user_id, project_id, session_id)`**、**fallback(假设 A 不通过 → v19 旧行为)**、**随 PG 备份**、G1.1 前置条件**写进"删除"列并逐字一致** |
| P0-2 | **新增表 `AuthRateCounter` 未登记附录 G1.1**(v22 自己犯的同类错误) | **G1.1 增两行**(`AuthRateCounter` / `PageSequence` → **删除**);**F3「7.2」扫描范围扩到"主文全文(含 §4.1 / §4.1.1 / §8.2)+ 附录 A–G 全部代码块与表格"**,并新增断言"**任一处出现的 PG 表名必须同时在 §4.1 与 G1.1 各有一行**" | 附录 G1.1 + 附录 F3 7.2 | ✅ |
| P0-3 | **E4 / E 顶部速查表残留编辑重复行**(同一道门挂着 v20 / v21 / v22) | **删旧行**:门槛**只保留三道门**,速查表与 E4 **逐字一致**(**该动作的完整说明见 E 顶部速查表的 v23 P0-3 说明 —— v24 P2-4:本行不再重复描述**);自检清单加"**同一门只允许一种版本号**" | 附录 E 顶部速查 + E4 + 主文 §0 自检清单 | ✅ |

### E23.2 P1 十项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | `AuthRateCounter` 的"12 次/分钟·IP" **未定义是每进程还是全局** | **改"Redis 侧计数 + DB 仅最终兜底"**(用户裁定):Redis 正常时**计数权威在实例 A**,本表**不写**;**故障期本表为唯一计数权威**;**12 次为全局口径**(唯一键天然聚合,**无 `12N` 放大**);刷盘前崩溃丢失 ≤12 次/进程属**已知且接受** | 主文 §8.2 ② + §4.1.1 | ✅ ⚠ **v24 P0-1 已推翻该论证**(**"唯一键天然聚合"不限制写入次数**)→ **拆分"限流额度"与"DB 写入频率"两个概念**,见 **§8.2.1 ②** 与 **E24.1**;**实施以 v24 为准** |
| P1-2 | 兜底表**窗口类型未定义**(表结构暗示固定窗口,与 §8.1 滑动窗口口径不同) | 写死**固定窗口 60s(`window_start` = 分钟整点)**;显式声明**边界处允许 2× 突发**,属**已知取舍** | 主文 §8.2 ② + §4.1.1 | ✅ |
| P1-3 | "Redis 故障 → 立即切"**判据缺失** | 写死**连续 2 次 `PING`/`GET` 失败或超时(200ms)→ 切;成功后 1 次调用成功 → 切回**;**抖动期不双写**;切换 / 切回各记一条 `AuditLog` | 主文 §8.2 ② | ✅ |
| P1-4 | `AuthRateCounter` **未进 F2 备份范围** | 写明**随 PG 备份 / 恢复**(与 `AuditLog` 同级)—— 理由:PG 恢复后 Redis 可能仍未就绪,此时它是**唯一计数载体** | 附录 F2 | ✅ ⚠ **v24 P0-3 已扩到 `PageSequence`**(它漏了会**发号回退撞号**)|
| P1-5 | F1 **缺"兜底切换"指标** | 新增行 **Redis ↔ DB 兜底切换**:绿 **0 次** / 黄 抖动(≤5min 内切回)/ 红 **> 5min 或 ≥3 次/小时** | 附录 F1 | ✅ |
| P1-6 | `§4.1.1` 把 **`hash_algo` 列为 `AppSetting` 是语义错误**(**结构**问题:表缺"归属") | 表新增 **"归属"列**(**只改结构**);`hash_algo` 标 **`Project`(按项目一行)**;补说明"**实施者不要到 `AppSetting` 表里找它们**" | 主文 §4.1.1 | ✅ |
| P1-7 | `§4.1.1` **漏 `parser_v` / `uid_rule_v`**(**内容**问题:缺行) | 按归属列补两行:`parser_v` → `AppSetting`;`uid_rule_v` → **`Project`**;两者都标出是**迁移前置校验项** / `parse_cache` key 组件(**v24 P2-3**:与 P1-6 **同源但分工不同** —— P1-6 管**结构**、P1-7 管**内容**,故不合并;两行已按此分工改写,不再互为重复)| 主文 §4.1.1 | ✅ |
| P1-8 | `AuditSecurityDetail.anon_rule_v` **默认值未定义** | 写死 **`anonymized BOOLEAN NOT NULL DEFAULT FALSE`**、**`anon_rule_v SMALLINT NOT NULL DEFAULT 2`**(= 16 hex,与附录 B10 枚举一致;否则新行落 NULL → G5 位宽断言会把它当"旧规则"误杀) | 主文 §4.1 | ✅ |
| P1-9 | `§4.1.1` 的**维护规则与 F3 7.2 范围脱节**(只说"可扩展",范围里没写) | §4.1.1 维护规则改为"**7.2 已扩展两条断言,范围已含 §4.1.1 与 §8.2**";与 F3 7.2 同步 | 主文 §4.1.1 + 附录 F3 7.2 | ✅ |
| P1-10 | `AuditSecurityDetail` 的**独立文件形态过长**、挤在 §4.1 表备注里 | 文件形态(JSONL / 按天轮转 / ≤100MB / 单写者 `O_APPEND`)与四项运维要求**只在附录 G1.1 保留**;§4.1 **只留指针** | 主文 §4.1 + 附录 G1.1 | ✅ |

### E23.3 P2 五项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P2-1 | §0 的 v22 轨迹 P1 十三项**仍是括号压缩描述**(追溯性弱于 v21) | 补编号 **`P1-1 … P1-13`**(与 E22.2 逐条对齐),并**补上 v22 漏列的 P1-6**(`AuditSecurityDetail` schema 并入 P0-2) | 主文 §0 | ✅ |
| P2-2 | **E22 P1-13 与 F3 7.x 两处描述重复** | **F3「7.x」为唯一权威**(定义 / 清单 / 升级规则都在 7.x);E22 的 P1-13 **只引用**,不重复描述 | 附录 F3 7.x + E22.2 | ✅ |
| P2-3 | §4.1.1 维护规则说 7.2 可扩展,但 **7.2 范围未声明含 §4.1.1** | **并入 P1-9**(同步扩范围并显式声明) | 附录 F3 7.2 | ✅ |
| P2-4 | `AuditSecurityDetail` 文件形态过长 | **并入 P1-10**(§4.1 只留指针) | 主文 §4.1 + 附录 G1.1 | ✅ |
| P2-5 | **`page_seq` "生效条件"与 §4.1 "假设 A 待评审"措辞不一致** | 统一为"**随假设 A 一并待评审;此处仅登记、不建表**";并明确 **`PageSequence` 同样随假设 A**(表未建则不发号) | 主文 §4.1 + 附录 A3.1 ⑥ | ✅ |

**机制性改进(本轮)**:①**并发正确性必须把"无行可锁 / 空集 / 首次创建"这类边界一起写进规范**(v22 的错就错在只描述了稳态路径);②**"新表必须同时登记 §4.1 与 G1.1"改由检查器兜底**(7.2 范围扩到主文),**不靠人记**;③**同一道门只允许一种版本号**。

**门禁判定(v23)**:门 1 **可判"达成"**;**门 2 / 门 3 不应推进** —— 检查器尚未构建(见速查表与 E4);尤其 **`page_seq` 的并发缺陷若按 v22 直接实施,会是多标签页场景下可复现的竞态**。

---

## E24 v23 新机制收口处置(v24:5 P0 + 12 P1 + 5 P2;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第七轮评审**(命题:"v23 把 v22 的 3 个 P0 都落了口径,但**收口动作本身又留下 4 处'新机制语义 / 边界未定义'、2 处跨文档不一致、1 处'规则在实现层无法执行'**";建议**不要再加条款**,做一次「**v23 新机制收口**」,只做五件事)。**本轮只改文档、不动代码**;合并版**继续冻结**。
> **本轮共性病因**:①**用"结构性质"冒充"行为限制"**(唯一键 ⇒ 无写放大);②**新机制只写"怎么用"、不写"不成立时怎么办"**(假设 A 无 fallback);③**跨口径概念混用**(固定窗口去合并滑动窗口的"同窗口键")。

### E24.1 P0 五项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **`AuthRateCounter` 的"全局 12 次/分钟·IP"无法由唯一键强制执行**(**v23 的论证是错的**) | **拆成两个概念**(新 §8.2.1 ②):**限流额度 = 12 次/分钟·IP**(沿用 §8.1 失败桶阈值,**由 Redis 侧计数执行**;故障期由本表 **同一行 `SELECT … FOR UPDATE` 串行读改写**执行 → **真正全局**)+ **DB 写入频率 = 每进程每窗口 ≤1 次 UPSERT**(进程内累加、60s 批量刷盘);**明确"`12N` 放大由后者消除,不是由唯一键消除"**;删除"唯一键天然聚合"表述;配置项改名 `auth_rate_db_write_per_window`(见 F3 7.4) | 主文 §8.2 ② / **新 §8.2.1** + §4.1.1 + 附录 F3 7.4 | ✅ |
| P0-2 | **`PageSequence` 与"假设 A 待评审"死结**(假设不通过则**无 fallback**) | 补 **fallback(写死)**:假设 A **未通过** → 本表**不建**、`page_seq` 机制**整体不生效** → **回退 v19 旧行为**(`writer_epoch` + **90s TTL 自然过期**),UI 提示"**另一标签页可能正在写入**"(与"已接管"提示**不得混用**);**实现者不得自行二选一**,该路径与假设 A 的评审结论**绑定** | 附录 A3.1 ⑥ + ⑧② + 主文 §4.1 | ✅ |
| P0-3 | **`PageSequence` 未列入 F2 备份范围** | F2 的 PG 行补 **`PageSequence` 随 PG 一并备份 / 恢复**;理由:PG 恢复后发号**回退到 1** → 与历史 `page_seq` **撞号** → "最大者胜"**再次失效** | 附录 F2 + 主文 §4.1 | ✅ |
| P0-4 | **G1.1 的 `PageSequence` 删除口径与 A3.1 ⑥ 清理前置条件不一致**(条件是"位置"问题:原先只在**依据列**) | G1.1 该行**把前置条件写进"删除"列并逐字一致**:"该 `(user_id, project_id, session_id)` 下**已无 `DeviceSession` 行且无活跃租约**";不满足**不得删除** | 附录 G1.1 ↔ 附录 A3.1 ⑥ | ✅ |
| P0-5 | **"恢复合并"与 §8.1 滑动窗口语义冲突**(**合并逻辑原本无法实现**) | **改为**:Redis 恢复 → 把本表**当前 `window_start` 行的 `n`** **累加进 Redis 当前活跃的滑动窗口键**(先读后取 `max`,幂等);**显式承认边界突发叠加(最高约 2×)**;删除"同窗口键"表述 | 主文 **§8.2.1 ⑧** | ✅ |

### E24.2 P1 十二项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | `PageSequence` **主键无 `user_id`**(会话池 / 测试夹具复用会导致跨用户共享发号) | 主键改为 **`(user_id, project_id, session_id)`**,分配语句与清理条件同步 | 附录 A3.1 ⑥ + 主文 §4.1 + 附录 G1.1 | ✅ |
| P1-2 | `PageSequence` **未列入 F1 指标** | 新增 **「`PageSequence` 增长与发号健康」**:发号 P95 / 行数(与 `DeviceSession` 同阶)/ 单日增量 / 清理失败 | 附录 F1 | ✅ |
| P1-3 | **"进程内累加 60s 批量刷盘"与"12 次限制"语义冲突** | **并入 P0-1**(拆成"额度"与"写入频率"两列后冲突消失) | 主文 §8.2.1 ② | ✅ |
| P1-4 | **F1"抖动 ≤5min 内切回"与切换判据不一致**(判据是 1 次成功即切回,亚秒级) | 抖动改为"**切换后 60s 内因 1 次成功调用切回**"(与 §8.2.1 ⑦ 判据一致);并写明 `持续 >5min` 与 `≥3 次/小时` **不是组合条件,任一命中即红** | 附录 F1 | ✅ |
| P1-5 | **§4.1.1 表头与内容不匹配**(表头写"`AppSetting` 配置项清单",内容含 `Project` 项) | 表头**改名**为"**配置项清单(含归属)**";原名作为历史说明保留在标题括号内 | 主文 §4.1.1 | ✅ |
| P1-6 | **`auth_rate_db_window_s` / `auth_rate_switch_fail_threshold` 只在 §4.1.1,正文未引用** | §8.2.1 ⑥⑦ **显式引用配置项名**;并声明"**§8.2.1 = 正文权威、§4.1.1 = 登记索引**",配置项名**逐字一致** | 主文 §8.2.1 + §4.1.1 | ✅ |
| P1-7 | `PageSequence.next_seq` **无上限 / 无溢出策略** | 类型 `BIGINT`、**不设人为上限**,但**必须进 F1 监控**(异常增长即告警),**不靠"溢出"当兜底** | 附录 A3.1 ⑥ + 附录 F1 | ✅ |
| P1-8 | `AuthRateCounter.bucket` **枚举未定义** | 写死 5 项:`auth_fail` / `auth_success` / `auth_total`(**与 §8.1 登录三桶一一对应**)/ `captcha_fail` / `register_fail`;**新增枚举必须同时登记 §4.1 + §8.2.1** | 主文 §8.2.1 ④ | ✅ |
| P1-9 | **`anon_rule_v` 默认值 2 只写在 §4.1**,B10 未写 | B10 补"**新写入的归档行 `anon_rule_v` 默认取 `2`**"(与 §4.1 列默认值 `SMALLINT NOT NULL DEFAULT 2` **逐字一致**) | 附录 B10 + 主文 §4.1 | ✅ |
| P1-10 | **`AuditSecurityDetail` 独立文件的"单写者 `O_APPEND`"与多进程部署冲突** | 写死:多进程下"单写者"**必须靠"选主写入"实现**(独立审计写入进程独占文件;web / worker **不得直接写**),或按 **`天 + 进程角色` 分片**;**禁止多写者直接 `O_APPEND` 同一文件**;该约束进**部署前置条件** | 附录 G1.1 | ✅ |
| P1-11 | **`PageSequence` "只增不减"与 G1.1 "随项目/账号删除"在归档场景冲突** | 写死:**归档项目不清理 `PageSequence`**(归档 ≠ 删除,清了重新激活会**撞号**);仅**项目删除 / 账号删除**才按前置条件清理 | 附录 A3.1 ⑥ + 附录 G1.1 | ✅ |
| P1-12 | **`AuthRateCounter.ip` 来源未定义** | 写死:**默认 `REMOTE_ADDR`**;**仅当可信代理列表已配置**时才取 **`X-Forwarded-For` 的"最后一个非可信地址"**;**禁止取首项**;未配置 → **一律回退 `REMOTE_ADDR`**;实现落位 = `core/` 内**单一 IP 解析函数**(与 `dev_id` 侧共用) | 主文 §8.2.1 ⑤ | ✅ |

### E24.3 P2 五项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P2-1 | §0 的 **v23 轨迹 P1 仍为压缩描述** | v23 轨迹的 P1 **补编号 `P1-1 … P1-10`**(与 **E23.2** 逐条对齐) | 主文 §0 | ✅ |
| P2-2 | **§4.1.1 的"唯一索引"定位不准确**(混编 `AppSetting` 项与按项目项) | **改名 + 加说明**(**不拆两段** —— 用户上一轮已裁定"单表 + 归属列",本轮只修标题):表头改为"**配置项清单(含归属)**" | 主文 §4.1.1 | ✅ |
| P2-3 | **E23.2 的 P1-6 与 P1-7 内容重叠** | **不合并行号**(避免与 §0 编号错位),改为**按"结构 / 内容"分工改写**:P1-6 **只讲"归属列(结构)"**、P1-7 **只讲"补两行(内容)"**,并注明二者**同源但职责不同** | 附录 E23.2 | ✅ |
| P2-4 | **E23.1 的 P0-3 与顶部速查表重复描述"删旧行"** | E23.1 P0-3 保留**结论**,描述**指向速查表的 v23 P0-3 说明**(一处即可) | 附录 E23.1 | ✅ |
| P2-5 | **§8.2 ② 的 `AuthRateCounter` 段落过长**(八项挤一处) | 拆出**独立小节 §8.2.1**(完整规范),§8.2 ② **只留指针**;§8.2 ④ 的引用同步改指 §8.2.1 | 主文 §8.2 / **新 §8.2.1** | ✅ |

**机制性改进(本轮)**:①**"某限制天然成立"必须写出"谁执行它"**(唯一键 / 索引只防重复行,**不限制写入次数**);②**"随假设 X 待评审"的机制必须给 fallback**,否则实现者无路可走;③**跨口径转换(滑动 ↔ 固定窗口)必须写明落点**,**禁止写"同窗口键"这类不存在的概念**。

**门禁判定(v24)**:门 1 **可判"达成"**;**门 2 / 门 3 不应推进**(检查器未构建)。⚠ **v23 的"全局 12 次"若按字面实施,Redis 故障期会立刻出现 `12N` 写放大** —— 与 v21 P0-5 的初衷正好相反;v24 已把它拆成"**额度**"与"**写入频率**"两件事。

---

## E25 v24 新机制与跨文档一致性核查处置(v25:13 P0 + 15 P1 + 4 P2;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第八轮评审**(命题:"文档已非常完备,但**收口动作本身引入新矛盾**这条老毛病仍在,且有几处不是表述问题,而是**实施后会出现正确性 / 安全 / 合规事故的硬问题**";给出"最小修复顺序 9 项")。**本轮只改文档、不动代码**;合并版**继续冻结**。
> **本轮共性病因**:①**同一机制在多处各写一份**(租约三处并存 → 双写漂移);②**结构性质冒充行为约束**(可空列进主键、无关联列却声称"随主体删除");③**引用了不存在的概念**("假设 A"零定义、"同窗口键")。
> ⚠ **用户四项裁定**:①范围 = **32 项全收**;②`AuthRateCounter` 故障期 = **混合模式**;③租约 = `ProjectWriterLease(project_id PK)` + `ProjectSyncState(project_id, user_id)`;④只落 **`project_key` 改名**(含前端契约 + D71),**审计实时表保持现状**、登记为已知取舍(P0-11)。

### E25.1 P0 十三项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | **写者租约到底存哪张表,三处自相矛盾**(A3 / §4.1 / D4.1 / E6-P1-7) | **唯一权威 = `ProjectWriterLease`**;抢租约 / 续租 / 接管 / 回收**只更新本表**;`ProjectSyncState` **删全部租约列**;A3 / D4.1 改为**引用** | 附录 A3 + D4.1 + 主文 §4.1 | ✅ |
| P0-2 | **`ProjectSyncState` 主键含 `writer_token` 与"NULL = 无写者"冲突**(PG 主键不可 NULL) | 主键改 **`(project_id, user_id)`**(每项目每用户一行;换 token 不产生新行 → **顺带消除 P1-4 孤儿行**);`last_writer_token` 降为**快照列** | 主文 §4.1 + 自检清单 | ✅ |
| P0-3 | **`DeviceSession`/`PageSequence` 挂"假设 A"但安全底线依赖它们** | **从假设 A 拆出**:令牌签发 / 台账 / 吊销 = **不可否决的安全底线(A-1),必建**;多标签页仲裁 = **A-2,可裁决** | 主文 §4.1.2 + §4.1 + 附录 A3.1 | ✅ |
| P0-4 | **"假设 A"全文引用 19 处、零定义** | **新增 §4.1.2「假设 A 定义」**(唯一权威):A-1 / A-2 对比表 + 生效条件 + fallback;其余各处只引用 | 主文 §4.1.2 | ✅ |
| P0-5 | **`project_id` 字段与 `Project.key` 混淆**(请求体 `project_id` 的值是 key,VID 公式里的 `project_id` 是数字主键 → 两端 VID 不同、图分裂) | **请求体字段改名 `project_key`**;写死"**数字 `project_id` 由服务端元数据下发、客户端用它算 VID**;禁止用 key 代入";**F3 7.4 改名映射**;`frontend-contract.md` 同步 + **D71**(登记在 `frontend-decisions.md`) | 主文 §5.4 + 附录 D7.1 + F3 7.4 + `frontend-contract.md` | ✅ |
| P0-6 | **`files[]` 字段 `file_rev` vs `base_file_rev` 冲突**(A4/D7.1 vs D2) | 统一为**客户端只传 `base_file_rev`**;服务端分配的 `file_rev` **只在响应回传**;A4 示例与 D7.1 同步 | 附录 A4 + D7.1 + 主文 §5.4 | ✅ |
| P0-7 | **故障期"限流额度精确"与"写入频率 ≤1 次/窗口"不可兼得**(v24 自相矛盾) | **定稿 = 混合模式**:每进程每窗口 **1 次 `UPSERT … RETURNING`** 取回全局计数 + 本地增量判超限;**DB 写 = O(进程数)**;**误差上界 = Σ 其他进程同窗口未刷盘增量**(写清);禁止改为每请求 DB 写 | 主文 §8.2.1 ② | ✅ |
| P0-8 | **`DeviceSession` 与 `PageSequence` 数据模型不匹配**(无 `project_id` 却存"每项目序列"快照) | **删除 `DeviceSession.page_seq`**;`page_seq` **唯一持有者 = `PageSequence`** | 主文 §4.1 + 附录 A3.1 ⑧① | ✅ |
| P0-9 | **`PageSequence` 清理前置条件不可查询**(要求按项目查 `DeviceSession`,但该表无 `project_id`) | **重写**:①项目已删 / 账号已删 且 ②该 `project_id` **无活跃租约**(`ProjectWriterLease`);**普通清理作业不得删本表行**;**`session_id` 永不复用**(并入 P1-13) | 附录 A3.1 ⑥ + G1.1 | ✅ |
| P0-10 | **`AuthRateCounter`/`DeviceSession` 的删除口径不可执行**(无 `user_id`/`project_id` 却声称"随主体删除") | `AuthRateCounter` → **不随主体删除、只按 TTL 清理**(并**补登记 §4.1**,此前只在 G1.1 与 §8.2.1);`DeviceSession` → **只随账号删除**;验收断言口径同步 | 主文 §4.1 + G1.1 验收 | ✅ |
| P0-11 | **审计实时表明文与 GDPR 删除权冲突** | **用户裁定保持现状** → 在 **G5 登记"已知合规取舍"**(依据 / 代价 / 复核触发),不擅自改 B10 | 附录 G5 | ✅(登记取舍) |
| P0-12 | **`parse_cache` "跨项目命中率"判据与 key 含 `project_id` 矛盾**(恒为 0) | **删除该判据**;GC 评估判据降为两条;F1 指标改「**本地命中率**」(按项目聚合) | 主文 §3.1.2 + 附录 F1 | ✅ |
| P0-13 | **`graph_rev` 发号器未登记**(B43 要求"首选 PG 序列",但无对象) | **登记 `graph_rev_seq`(全局 PG 序列)**;`nextval` 发号、不锁 `Project` 行;与每项目列 `file_rev_seq` **分工写死**;允许空洞 | 主文 §4.1 + `frontend-contract.md` | ✅ |

### E25.2 P1 十五项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P1-1 | outbox"同 `graph_rev` 同 VID 单行"**未限定 `target`** | 改为 **`UNIQUE(project_id, graph_rev, vid, target)`**;A5 / D5.1 / §4.1 三处同步 | 附录 A5 + D5.1 + 主文 §4.1 | ✅ |
| P1-2 | `nebula_graph_rev` 定义不清("最新已写入" vs F2 的 `min(…)−1`) | 统一为**连续完成水位**(`min(未完成且非 dead) − 1`;队列空 = 当前 `graph_rev`),与 `indexed_graph_rev` **同构** | 主文 §5.5 | ✅ |
| P1-3 | 两把锁的**锁顺序未定义**(死锁风险) | 写死:**先 `Project` 后 `ProjectWriterLease`**,所有路径一致;进 code review 清单 | 附录 D4.2 | ✅ |
| P1-4 | `ProjectSyncState` **孤儿行** | **并入 P0-2**(主键改 `(project_id, user_id)`);徽章 = **用户视角**读一行 | 主文 §4.1 | ✅ |
| P1-5 | **迁移旧 VID 墓碑清理策略未定义** | 写死:**保留到【回滚窗口 24h 与 `graph_migration_log` 30 天】中较长者**;清理条件 = `vid_op_rev` 早于 `migration_ended_at − 30 天`;**不得早删** | 主文 §4.1 `graph_migration_log` 行 | ✅ |
| P1-6 | `deleting` 状态在删 `Project` 行后丢失(**幂等重入失去依据**) | **新增 `ProjectDeletion`**(步骤墓碑表);G3 增步骤 0(先写墓碑)与 9(最后删墓碑);**中断恢复 = 读 `step` 续跑** | 主文 §4.1 + 附录 G3 | ✅ |
| P1-7 | Redis 恢复合并"**累加**"与"**取 max**"矛盾 | 统一为 **`max(redis_current, db_n)`**(幂等、不制造突发;**代价 = 可能低估**,写清);合并落点 = 当前活跃滑动窗口键 | 主文 §8.2.1 ⑧ | ✅ |
| P1-8 | 固定窗口"分钟整点"公式与可配窗口冲突 | 改**通用对齐公式**(`floor(epoch / $W) × $W`);`$W = 60` 等价分钟整点;**改配置无需改公式** | 主文 §8.2.1 ⑥ | ✅ |
| P1-9 | `AuditSecurityDetail` 独立文件**无法按项目删除**("天 + 角色"分片一文件含多项目) | **改为按项目分片**:`secure/<project_id>/<YYYY-MM-DD>.jsonl`;非项目事件放 `_global/`;**删项目 = 删整目录**;多进程**选主写入**不变 | 附录 G1.1 | ✅ |
| P1-10 | `AuthRateCounter` **存明文 IP**、删除口径未闭环 | `ip` 改存 **`sha256(ip)[:16]`**;**只按 TTL 清理**(并入 P0-10);**随 PG 备份**不变 | 主文 §8.2.1 ⑤ + §4.1 | ✅ |
| P1-11 | `DeviceSession` **随项目删除不合理** | **只随账号删除 / 吊销**;G3 第 5 步**不删**;验收断言移到"账号删除"组 | 附录 G1.1 + G3 | ✅ |
| P1-12 | `PageSequence` 删除后 `session_id` 复用撞号 | **`session_id` 由服务端生成、永不复用**(写死);普通清理**不删**本表行 | 附录 A3.1 ⑥ | ✅ |
| P1-13 | `graph_migration_log` **字段类型 / 编码未定义** | 补**列类型表**(`vid_*` = `CHAR(32)` 小写 hex、`uid` = `TEXT`、`applied` = `BOOLEAN NOT NULL DEFAULT FALSE` 等),与 `graph_node_ref` 形态一致 | 主文 §4.1 | ✅ |
| P1-14 | `graph_node_ref` 迁移原地 UPDATE **可能撞唯一约束** | **迁移前全项目批量碰撞预检**;命中 → **中止 + 审计 + 不进 `migrating`**;**禁止临时降级 / 删除唯一约束** | 主文 §4.1 `graph_node_ref` 行 | ✅ |
| P1-15 | `AuditSecurityDetail.detail`(JSONB)**匿名化规则缺失** | **字段白名单**(8 个键)+ `*_hash` 用 `sha256[:16]` + **归档时键名正则扫描,非白名单键 → 归档失败**(进 G5 验证) | 附录 B10 | ✅ |

### E25.3 P2 四项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P2-1 | **附录 F1 指标表被引用块截断**(`index_lag` 之后十几行不渲染为表格) | 把"为什么不用墓碑数量做阈值"引用块**移到表格之后**;登记 v25 P2-29 修复说明 | 附录 F1 | ✅ |
| P2-2 | "假设 A"反复引用无定义 | **并入 P0-3/P0-4**(§4.1.2) | 主文 §4.1.2 | ✅ |
| P2-3 | **可信代理列表配置项未登记** | **登记 `trusted_proxy_cidrs`**(默认空 = 一律 `REMOTE_ADDR`) | 主文 §4.1.1 | ✅ |
| P2-4 | **多标签页 fallback 与安全底线冲突** | **并入 P0-3**(A-1 不可 fallback;fallback 只作用于 A-2) | 主文 §4.1.2 | ✅ |

**机制性改进(本轮)**:①**同一机制只允许一处权威定义**(租约三处并存 → 双写漂移);②**可空列不得进主键;无关联列不得声称"随主体删除"**(结构必须支撑行为);③**引用了不存在的概念 = 缺陷**("假设 A"、"同窗口键"都曾是零定义引用)。

**门禁判定(v25)**:门 1 **可判"达成"**;**门 2 / 门 3 不应推进**(检查器未构建)。⚠ 若按 v24 直接实施,最先出事故的会是:**多标签页写权仲裁**(主键不可空)、**客户端 VID 计算不一致**(`project_id` 混淆)、**删项目清理**(验收永远失败)、**Redis 故障期认证限流**(自相矛盾的口径)。

---

## E26 v25 收口核查处置(v26:8 P0 + 12 P1 + 10 P2;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**第九轮评审**(命题:"v25 方向正确,但**落地执行层仍有结构性缺口** —— 一份**声称新增的权威节在主文里不存在**、一处**删列带来的查询能力缺口没补**、一处新表**没类型没 F2**、一处公式**字面重复计数**";并给出"最小修复顺序 8 项")。**本轮只改文档、不动代码**;合并版**继续冻结**。
> **用户四项裁定**:①范围 = **全收**;②仲裁映射 = **方案 B(`PageAssignment`)**;③故障期 = **写 1 次/窗口 + 读每请求**;④IP = **两侧统一哈希**。

### E26.0 经核查**不成立 / 无需动作**的条目

| # | 评审意见 | 核查结论 |
|---|---|---|
| **#1 / #21** | "**§4.1.2「假设 A 定义」在主文里根本不存在**",至少 4 处引用悬空 | ❌ **不成立**:§4.1.2 **存在**(`backend-design.md` §4.1.2;A-1/A-2 对比表在同一节),全文 **10 处**引用;**节序 = §4.1.1 → §4.1.2 → §4.2**。评审读到的是**搬位前的快照** —— 本轮曾先把该节插进 §4.1.1 标题与其内容之间,**随后删除并重建**。同理:`graph_rev_seq` 与 `file_rev_seq` 的**分工已写**,`D71` **已登记**在 `frontend-decisions.md`(README 有索引)。**处置**:不改这三处文案,**只补文件指针**(自检清单新增"引用他人决策必须带文件") |
| **#25** | `graph_outbox` 唯一约束三处表述不一致 | ✅ 评审**自证一致**(A5 / D5.1 / §4.1 三处已统一为 `(project_id, graph_rev, vid, target)`);**无需动作** |
| #22 | "E25 声称与主文不一致" | ⚠ **除 #1 外均成立**;P0-11(G5 取舍)与 P2-1(F1 表格)经核**确已落** |

### E26.1 P0 八项

| # | 项 | 收口口径 | 目标文件 | 状态 |
|---|---|---|---|---|
| P0-1 | §4.1.2 不存在 / 引用悬空 | **见 E26.0**(不成立);补文件指针 | 主文 §0 自检清单 | ✅ |
| P0-2 | **多标签页仲裁的 `writer_token → page_seq` 映射无落点**("最大者胜"无法实现) | **新建 `PageAssignment`**(用户裁定方案 B):`(user_id, project_id, session_id, page_id) → page_seq` + `writer_token` **单列索引**;`PageSequence` 只发号、`DeviceSession` 定位不变;**A3.1 ⑧ 给出可执行仲裁伪代码**(接管 / 撤销 / 可比较范围);**发号时机 = 抢租约时**;**初值 1/1**;**只在同 session 内可比**;登记 **§4.1 + G1.1 + F2** | 主文 §4.1 / §4.1.2 + 附录 A3.1 ⑧ + G1.1 | ✅ |
| P0-3 | **`ProjectSyncState` 主键迁移路径缺失** | 写死:`INSERT … SELECT JOIN device_session` + **`MAX(last_synced_graph_rev) GROUP BY (project_id, user_id)`**;**反查不到 `user_id` 的行丢弃并记审计**;**迁移期停写**(维护窗口 / 双写);完成后建唯一约束 | 主文 §4.1 | ✅ |
| P0-4 | **故障期公式重复计数 + 上界不可审计** | **改"写 1 次/窗口 + 读每请求"**(用户裁定):写 = 每进程每窗口 ≤1 次;判定 = **每请求 `SELECT n`** → **依据当前全局值、误差 = 0**;**撤回** `n_global + 本地增量`;**禁止改成每请求 DB 写** | 主文 §8.2.1 ② | ✅ |
| P0-5 | **`ProjectDeletion` 与 `deleting` 关系模糊 + F2 未覆盖** | 字段**类型定稿**(`state`/`step` 枚举、`error` = 错误码);**本表 = 唯一权威**,`deleting` 只是**读路径门禁标志**;**读进度顺序**(先本表,无行再退回 `Project`);**F2 步骤 5b 增第 ⑤ 条**;F2.0 覆盖 | 主文 §4.1 + 附录 G3 + F2 | ✅ |
| P0-6 | **全局 `graph_rev_seq` 的三处语义变化未写清** | 写死"**全局单调、项目内严格递增、允许空洞**":①客户端**会看到跳跃**(UI 需说明)②**`nebula_graph_rev` 是"查询时点"的活值**(允许抖动)③**`index_lag` 非逐 1** | 主文 §5.4 | ✅ |
| P0-7 | **`ProjectWriterLease` 首次创建没有 INSERT** | **A3 补齐 `INSERT … ON CONFLICT (project_id) DO UPDATE … WHERE … RETURNING` 完整语句**(首次创建与过期接管同一条);**初值 1/1** | 附录 A3 + 主文 §4.1 | ✅ |
| P0-8 | **F2 恢复表清单漏关键表** | **新增 §F2.0「恢复后需重置的同步状态表」**:`ProjectWriterLease` **清空** / `ProjectSyncState` **回退** / `DeviceSession` **保留 + `401` 重签、不得全量吊销** / `ProjectDeletion` **保留** / `PageSequence`+`PageAssignment` **保留**;**备份表清单写死** | 附录 F2 | ✅ |

### E26.2 P1 十二项

| # | 项 | 收口口径(简) | 状态 |
|---|---|---|---|
| P1-1(#9) | **`session_id` "永不复用"实现悖论** | 改为**可执行约定**:`session_id = 服务端生成的 128-bit 随机值`(碰撞可忽略);`PageSequence`/`PageAssignment` 随项目 / 账号删除;**极端撞号**由 `writer_epoch` 兜底 | ✅ |
| P1-2(#10) | **`trusted_proxy_cidrs` 生效算法未给** | §8.2.1 ⑤ 给出 **`core/ip_resolve.py` 确定性伪代码**(默认回退 `REMOTE_ADDR`;仅直连方可信时自右向左取"第一个非可信地址") | ✅ |
| P1-3(#11) | **`migration_ended_at` 写入粒度** | **整个迁移作业完成 / 回滚时**,对**所有 `applied = TRUE` 且 `migration_ended_at IS NULL`** 的行**同一条 `UPDATE` 写同一时间戳** → 保留期 = `migration_ended_at + 30 天` | ✅ |
| P1-4(#12) | **`detail` 嵌套键处理** | **只扫顶层键**;白名单键的**值必须为标量**,构造端禁止写入对象 / 数组;**不做递归扫描** | ✅ |
| P1-5(#13/#27) | **`ProjectDeletion` 字段类型 / `error` 格式** | 与 P0-5 合并处置(`state`/`step` 枚举、`error` = 错误码 + 简述) | ✅ |
| P1-6(#14) | **`D71` 悬空** | ❌ **不成立**(已登记 `frontend-decisions.md`);**补文件指针** | ✅ |
| P1-7(#15) | **哈希 IP 的运维盲区** | §8.2.1 ⑤ 写明:排查**走 `AuditLog`**(实时表可存明文),`AuthRateCounter` **只做计数、不可反查** | ✅ |
| P1-8(#16) | **碰撞预检实现细节** | 附录 D:时机(**outbox 已排空之后、进 `migrating` 之前**)、**PG 临时表 + 唯一索引**、**同事务窗口**、**失败 → 中止 + 审计 + 不进 `migrating`**、禁止降级唯一约束 | ✅ |
| P1-9(#17) | **过渡期 `target = pg` 切换语义** | 枚举扩展为 **`pg \| nebula \| opensearch` 并存**;**不改写已有 `pg` 行**(避免撞唯一约束);切换 = 停消费 → 全量重灌 → 新行写 `nebula` → `pg` 行按 14 天窗口自然过期 | ✅ |
| P1-10(#18) | **跨用户抢租约 = 踢出语义** | 主文 §4.1 写死:**设计意图**;A 收 `409 stale_writer` 降级只读,UI 提示"已在其它设备 / 用户处接管写权" | ✅ |
| P1-11(#19) | **`page_seq` 与跨用户抢租约的关系** | §4.1.2 + A3.1 ⑧:`page_seq` **只在同 `(project_id, session_id)` 内可比**;跨 session / 跨用户**走 `writer_epoch + lease_seq`** | ✅ |
| P1-12(#20) | **`writer_epoch` / `lease_seq` 初值** | **均为 1**(与 A3 首次 INSERT 语句一致),写进 §4.1.2 与 A3 | ✅ |

### E26.3 P2 十项

| # | 项 | 收口口径(简) | 状态 |
|---|---|---|---|
| P2-1(#21) | 多处引用 §4.1.2 悬空 | ❌ 不成立(见 E26.0) | ✅ |
| P2-2(#22) | E25 声称与主文不一致 | 除 #1 外均成立并已落,见 E26.0 | ✅ |
| P2-3(#23) | **`page_seq` 分配时机未说** | **发号时机 = 服务端签发 `writer_token` / 抢租约时**,响应回传 `page_seq`;**不得每次上传发号** | ✅ |
| P2-4(#24) | **`last_writer_token` 语义冗余** | **改名为 `last_writer_user_id`**(UI 要显示"最近写者"需要的是**用户**,token 不可读) | ✅ |
| P2-5(#26) | **`updated_at` 与 `window_start` 混用** | 清理**按 `window_start + 2 × 窗口 < now()`**;`updated_at` **只作审计展示、不参与清理** | ✅ |
| P2-6(#28) | **`batch_id` 生成规则未说** | `<migration_job_id>:<batch_no>`(**作业内单调批次序号**);同批次所有行**共用同一 `batch_id`** | ✅ |
| P2-7(#29) | **Redis 与 DB 的 IP 键空间不一致**(合并不可执行) | **两侧统一用哈希**(用户裁定):§8.1 键改 **`sha256(ip)[:16]\|dev_id`**;**禁止一侧明文一侧哈希** | ✅ |
| P2-8(#30) | **`parse_cache` GC 判据不可执行** | 改为二选一:ⓐ**同 `(user_id, project_id, path)` 下 key 数 > N(起步 5)**;ⓑ**加 `last_used_at` 按 LRU 清理** | ✅ |
| P2-9 | (随 #24) `ProjectSyncState` 列语义 | 见 P2-4;`last_writer_user_id` 标为**快照列,不参与判定** | ✅ |
| P2-10 | 其余文档卫生 | 版本号统一 **v26**;E26 登记 30 条并标注不成立项;**自检清单 +3 条**(删除字段须检查查询能力 / 跨存储键必须两侧一致 / 引用他人决策必须带文件) | ✅ |

**机制性改进(本轮)**:①**删除字段必须同时检查"查询能力"**(删列不得连带删掉机制依赖的查询路径);②**跨存储协作的键必须两侧逐字一致**;③**引用他人决策必须带文件**(避免"编号悬空"误判)。

**门禁判定(v26)**:门 1 **可判"达成"**;**门 2 / 门 3 不应推进**(检查器未构建)。⚠ 提醒:本轮**不改**评审报告中被误判的三处(§4.1.2 / 分工 / D71)—— 它们本就已落,改文案反而会掩盖"评审读了旧快照"这一事实。

---

## E27 v27 减法重构(v26 → v27;⚠ 范围 = 仅文档)

> **决策来源**:用户提供的**减法评审**(命题:"v26 已过度设计 —— **稳定性来自少状态、单权威、固定策略、短租约、幂等、失败即拒绝**,而不是来自多套 fencing / 多张状态表 / 多套降级路径 / 多套迁移逻辑 / 多套文档检查器";要求**砍掉 50% 状态、30% 字段 / 表**)。**用户四项裁定**:①**就地改 8 份文档**、不新建文档、被删机制标「v27 移除」;②**两个安全机制都删**(明确承认回归);③outbox 改**快照 + 短窗口**;④**治理纯减法**(门 2 取消)。
> **总原则**:**一次只留一个权威、一个状态机、一个序列、一条路径**;凡"为了更安全 / 更精确"而**增加持久状态或第二权威**的机制,**一律删除或后置**。

### E27.1 直接删除(9 类机制)

| # | 删除对象 | 替代做法 | 状态 |
|---|---|---|---|
| 1 | **哈希算法迁移全链**:`Project.hash_algo` / `hash_algo_migrating_from` / `graph_migration_log` / `migrating` 状态 / `/hash-migration/` 端点 / 迁移期请求体 / 6 天红线 / 7 天自动回滚 / 回滚窗口 / 碰撞预检 / 两阶段 `applied` | **固定 `sha256_16`、禁止迁移**;换算法 = **新建项目 / 新图空间 + 旧项目只读导出后重建**;`graph_node_ref.hash_algo` / `migrating` / `migrated_at` 一并删;附录 D 的 **D1.5.1–D1.5.3**(能力检测 / 降级 / 迁移)已删 | ✅ |
| 2 | **多标签页 `page_seq` 仲裁**:`PageSequence` / `PageAssignment` / `page_seq` / `page_id` / "最大者胜" / 撤销校验 / fallback | **单写者租约 + `409 stale_writer` + UI 提示**;`writer_token = HMAC(server_key, user_id ‖ session_id)`;§4.1.2 缩减为 **A-1**;附录 A3.1 ⑧ 整段删除 | ✅ |
| 3 | **`AuthRateCounter` 认证限流 DB 兜底**(窗口类型 / 频控 / 切换判据 / 恢复合并 / 两侧 IP 哈希统一 / **§8.2.1 整节**) | 限流 = **Redis + 本地内存降级 + 账号锁定**;认证链路 `fail-closed`、读路径 `fail-open`;统一 `429 + Retry-After + rate_limited`;§8.1 键**恢复明文 `ip\|dev_id`**;`ip` 来源与代理判定**收回 §8.2 ⑤** | ✅ |
| 4 | **`AuditSecurityDetail` 高保真独立通道**(独立文件 / 按项目分片 / 选主写入 / `detail` 白名单) | 审计 = **`AuditLog` 单表 + 访问受限 + 归档前匿名化 + 磁盘配额 + 告警**;安全事件**不限流、不抽样** | ✅ |
| 5 | **`ProjectDeletion` 删除墓碑表** | 删除 = **一个异步 `Job`**(`Job.state` + `payload.step` 记进度);⚠ **必须显式把"删除作业自身"排除在"按项目删除"之外**(否则进度记录会被自己删掉 —— 与原 `Project` 行悖论**同型**);`graph_sync_status = deleting` **只作读路径门禁** | ✅ |
| 6 | **`graph_migration_log`** | 随 #1 一并删除 | ✅ |
| 7 | **14 天 outbox 全量 payload 重放窗口** | **成功即删**(`done`);窗口缩到 **`N` 小时(起步 1h,可配)**;超窗口 → **从 `GraphBackup` 快照(已有机制)+ 客户端增量重传** | ✅ |
| 8 | **自检清单 + F3 7.1–7.4 + 改名映射** | **纯减法**:全部删除;**门 2「检查器就绪」取消** → **门 1 = 门 3**;**E1–E27 降为历史归档,不作权威** | ✅ |
| 9 | **`parse_cache` GC / 跨项目命中率 / Worker 池复杂化** | 缓存 = **文件级 AST 缓存**(key = `file_sha256 + parser_v`,**随项目删除**);Worker 池**固定 4**、按文件分发、失败重试一次、**不预热**;后置:预热 / 动态池 / chunk 字节预算 | ✅ |

### E27.2 合并(减少多套权威与 fencing)

| # | 合并前 | 合并后 | 状态 |
|---|---|---|---|
| 1 | **四套 fencing**:`active_fence` / `writer_epoch` / `lease_seq` / `page_seq` | **一套 `lease_epoch`**(每项目单调);所有写请求带 `lease_epoch`,CAS = `UPDATE … WHERE lease_epoch < $new`;**`Job.active_job` 不参与 fencing** | ✅ |
| 2 | 租约列三处并存 | **唯一租约表 `ProjectWriterLease(project_id PK, writer_token, lease_until, lease_epoch)`**;`ProjectSyncState(project_id, user_id, last_synced_graph_rev, last_synced_at)` **不含租约列**;`last_writer_user_id` 仅供 UI | ✅ |
| 3 | **项目状态 7 态** | **3 态 `ok \| degraded \| deleting`**;`pending_repair` → outbox `dead` + 卡片提示;`inconsistent` → 作业失败 + 告警;`migrating` 删除;`paused` → **运维标志**(不进状态机) | ✅ |
| 4 | **outbox 5 态** | **3 态 `pending \| done \| dead`**;成功即删,超限 `dead` | ✅ |
| 5 | 项目标识多套 | 外部 = **`Project.key`**(URL + 请求体 `project_key`);内部 = **数字 `project_id`**(仅 VID 公式与外键) | ✅ |
| 6 | 版本 9 个 | 保留 **`graph_rev` / `file_rev` / `last_synced_graph_rev` / `indexed_graph_rev`**;`vid_op_rev = graph_rev`(仅同 VID 比较);**删 `based_on_graph_rev`**;**`nebula_graph_rev` 并入 `indexed_graph_rev`**(只留一个"服务端落库水位") | ✅ |

### E27.3 后置(不是一期稳定性必需)

Nebula / OpenSearch 正式接入 · 查询归一化内部路由 · 边查询流式 / `cursor` · 派生预计算(`CodeAnalysisDerived` / `based_on_graph_rev` / `derived_stale`) · Worker 池预热 · Redis 频道层(需要 WebSocket 广播时再独立实例) · 合约**宽限期降级**(`X-Contract-Degraded` / 字段缺失语义 / 引导页兜底) · `parse_cache` GC。

### E27.4 ⚠ 明确承认的两处安全回归(用户裁定:接受)

| 回归 | 具体影响 | 兜底 |
|---|---|---|
| **删 `AuthRateCounter`** | **Redis 与进程同时重启**的窗口内,认证限流**仅剩本地内存 + 账号锁定**(这正是 v20 P0-3 当初用 DB 兜底要堵的洞) | **账号锁定**(DB)+ 登录后 / 邮件的弱密码提示;⚠ **若该窗口被实际利用 → 必须重新评审 DB 兜底** |
| **删 `AuditSecurityDetail`** | 放弃 B7「攻击期间**明细不得抽样**」的**高保真通道**(v21 P0-3 曾要求保留) | `AuditLog` 单表 + **访问受限** + **磁盘配额 + 告警**;归档前**统一匿名化**(规则不变) |

### E27.5 不可删的安全底线(与减法评审一致,逐条保留)

权限强制注入(`ctx.project_id` + DAO 出口断言)· nGQL / DSL **全参数化** · 路径安全(NFKC + `realpath` + `commonpath` + `O_NOFOLLOW`)· **`writer_token` 服务端签发 / 绑会话 / 可吊销** · **VID 固定 `sha256_16`、禁止运行时降级** · 请求体 Schema + 大小限制 + 净化 · **审计单表 + 访问控制 + 归档脱敏** · 删除**异步 / 可重入 / 逐表验证** · 限流统一 **`429 + Retry-After + rate_limited`** · 备份与恢复基本流程(PG 每日全量 + WAL,图快照,索引可重建)。

**门禁判定(v27)**:✅ **可开工** —— **门 1 = 门 3**(文档定稿即通过;检查器取消)。⚠ **本轮是本项目唯一一次"减少条款"的修订**:后续若再加机制,应按本附录的判断标准逐条自问(**是否新增持久状态 / 是否引入第二权威 / 是否产生跨文档同步 / 是否增加可配置降级 / 是否让恢复依赖多张表**)。

---

## E4 门槛判定(给评审的一句话状态)

| 门 | 条件 | 当前状态 |
|---|---|---|
| **门 1 · 可评审通过(v27:即为可开工)** | 主文 **v27** + 附录 A–G 一致 | ✅ **达成**(v27:**E27 减法重构已落**) |
| **门 2 · 可开工(v27:与门 3 合并)** | **原"检查器就绪"取消**(治理纯减法,见 E27)→ **门 1 通过即等于可进入阶段 0** | ✅ **达成**(与门 1 同) |
| **可进入阶段 1** | E1.1 收口 + E2 中"⏳ 待实现时收敛"项给出方案 | ⬜ 未开始 |
| **可进入阶段 2(上 Nebula/OpenSearch)** | 上一门 + 安全用例(越权/注入/路径/滥用)全绿 | ⬜ 未开始 |
