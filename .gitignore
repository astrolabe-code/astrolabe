# =============================================================================
# Astrolabe · 忽略清单
#
# ⚠★ **写这份文件放在 `git init` 之前，是有意的** ——
#   git 历史**很难彻底清除**：★ 一旦把机密提交进去，
#   ★★ 即使事后删掉文件，★ **历史里那一条提交仍然留着它**
#   （⚠ 得 rewrite history / filter-repo 才能清，且会改掉所有后续哈希）。
#   ⇒ ★★ 所以**先挡住，再提交**。
# =============================================================================

# ---------------------------------------------------------------- ⚠★ 机密
# ★★ `.env` 里是 SECRET_KEY / POSTGRES_PASSWORD / OAuth client_secret —— **绝不入库**
.env
.env.*
# ⚠★ 注意：这里【故意没有】 `!.env.example` 例外 ——
#   决策者判断：★ 模板对读者**没有价值**（暴露技术栈与依赖），
#   ⇒ ★ **整类 `.env*` 一律不入库**。
*.env.bak
*.pem
*.key
*.crt
*secret*
*credential*
id_rsa*
.ssh/

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# ---------------------------------------------------------------- Django
staticfiles/
media/
*.log

# ---------------------------------------------------------------- ★ 运行数据（不入库）
# ★ 这些是**运行时产生的**，★ 不属于"源代码/设计" ⇒ ⚠ 入库只会让仓库变脏
data/backups/
data/repos/
data/uploads/
data/static/
app/data/
logs/*
!logs/.gitkeep

# ---------------------------------------------------------------- 编辑器 / 系统
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# ---------------------------------------------------------------- ⚠★ 不公开的文档
# ★★ `docs/DEPLOYMENT.md` —— **运维手册**，含宿主真实路径 / 端口映射 /
#    `env_file` 位置 / 数据库访问方式。
# ★ 决策者裁定（`B158`）：★ **对读者零价值、对攻击者是线索** ⇒ 不入库。
# ⚠★ 注意：`.gitignore` 对【已跟踪】文件无效 —— 本次是靠重做提交让它从未进入历史。
docs/DEPLOYMENT.md

# ---------------------------------------------------------------- 临时文件
*.tmp
*.bak
.tmp-*
.~*

# ⚠★ 一次性备份 —— **不入库**
# ★ 原因不只是"占空间"：★ 它里面是一份**内容重复（每条决策出现两次）**的
#   `backend-decisions.md` 旧副本 ⇒ ★★ 若一并入库，
#   ⚠ **将来会看到两份不一样的 decisions 文件** ⇒ ★ **反而削弱证据力**
#   （★ 谁都会问："哪份才是权威的？"）
.backup-decisions-*.md
